---
phase: 27-seo-ai-search-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/seo.ts
  - src/layouts/Base.astro
  - public/llms.txt
  - src/pages/en/methodology/index.astro
  - src/pages/it/metodologia/index.astro
  - src/pages/es/metodologia/index.astro
  - src/pages/fr/methodologie/index.astro
  - src/pages/pt/metodologia/index.astro
autonomous: true
requirements: [SEO27-01, SEO27-02, SEO27-03, SEO27-04, SEO27-05, SEO27-06]

must_haves:
  truths:
    - "Country meta descriptions show rounded scores like 7.4 not 7.406320589270102"
    - "All JSON-LD schemas include dateModified and datePublished"
    - "Organization schema includes logo, sameAs, and foundingDate"
    - "FAQ schema uses natural language question format"
    - "llms.txt uses correct ISO3 country URLs"
    - "All pages include meta robots max-snippet:-1 and preload hints for critical assets"
  artifacts:
    - path: "src/lib/seo.ts"
      provides: "Updated JSON-LD builders with dateModified/datePublished, complete Organization schema, natural FAQ questions"
      contains: "dateModified"
    - path: "src/layouts/Base.astro"
      provides: "meta robots directive and preload hints"
      contains: "max-snippet"
    - path: "public/llms.txt"
      provides: "Fixed country URLs with ISO3 codes"
      contains: "/en/country/ita/"
  key_links:
    - from: "src/lib/seo.ts"
      to: "src/pages/*/methodology/*"
      via: "buildFaqPageJsonLd called with natural language questions"
      pattern: "What does the .* category measure"
    - from: "src/layouts/Base.astro"
      to: "all pages"
      via: "meta robots and preload in head"
      pattern: "max-snippet:-1"
---

<objective>
Fix SEO metadata across all pages: round scores in meta descriptions, add dateModified/datePublished to JSON-LD, complete Organization schema, rewrite FAQ questions in natural language, fix llms.txt URLs, add meta robots max-snippet and preload hints.

Purpose: These are the highest-impact SEO fixes that affect every page's search snippet quality and structured data validity.
Output: Updated seo.ts, Base.astro, llms.txt, and methodology pages across all 5 languages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-seo-ai-search-optimization/27-CONTEXT.md
@src/lib/seo.ts
@src/layouts/Base.astro
@public/llms.txt
@src/pages/en/methodology/index.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix seo.ts — score rounding, dateModified/datePublished, Organization completion, FAQ natural language</name>
  <files>src/lib/seo.ts, src/pages/en/methodology/index.astro, src/pages/it/metodologia/index.astro, src/pages/es/metodologia/index.astro, src/pages/fr/methodologie/index.astro, src/pages/pt/metodologia/index.astro</files>
  <read_first>src/lib/seo.ts, src/pages/en/methodology/index.astro, src/i18n/ui.ts (lines 1-100 for translation key patterns), src/pages/en/index.astro, src/pages/en/country/[slug].astro</read_first>
  <action>
1. In `buildCountryMetaDescription()` (seo.ts line 22): change `${score}/10` to `${(score).toFixed(1)}/10` — the `country.score` value is already on 1-10 scale but may have many decimal places. This ensures meta descriptions show e.g. "7.4/10" not "7.406320589270102/10".

2. Add a `dateModified` parameter to all JSON-LD builder functions. Update each function signature to accept an optional `dateModified?: string` param (ISO 8601 date string). When provided, add `"dateModified": dateModified` and `"datePublished": "2026-03-19"` (site launch date) to the WebPage node. Update these functions:
   - `buildCountryJsonLd` — add to the WebPage node in @graph
   - `buildHomepageJsonLd` — add to root object
   - `buildGlobalSafetyJsonLd` — add to root object
   - `buildWebPageJsonLd` — add to root object

3. Update `buildOrganizationJsonLd()` to include:
   ```
   logo: `${siteUrl}/favicon.svg`
   sameAs: ['https://github.com/RiccardoDominici/IsItSafeToTravel']
   foundingDate: '2026'
   ```

4. In all 5 methodology pages, change the FAQ question generation from using pillar title translation keys to natural language questions. Replace:
   ```
   const faqQuestions = weightsConfig.pillars.map(pillar => ({
     question: t(`methodology.pillar.${pillar.name}.title` as any),
     answer: t(`methodology.pillar.${pillar.name}.description` as any),
   }));
   ```
   with a hardcoded natural language question array per language. For English:
   ```
   const faqQuestions = [
     { question: 'What does the Conflict category measure in the safety score?', answer: t('methodology.pillar.conflict.description' as any) },
     { question: 'What does the Crime category measure in the safety score?', answer: t('methodology.pillar.crime.description' as any) },
     { question: 'What does the Health category measure in the safety score?', answer: t('methodology.pillar.health.description' as any) },
     { question: 'What does the Governance category measure in the safety score?', answer: t('methodology.pillar.governance.description' as any) },
     { question: 'What does the Environment category measure in the safety score?', answer: t('methodology.pillar.environment.description' as any) },
   ];
   ```
   For Italian: "Cosa misura la categoria Conflitto nel punteggio di sicurezza?", etc.
   For Spanish: "Que mide la categoria Conflicto en la puntuacion de seguridad?", etc.
   For French: "Que mesure la categorie Conflit dans le score de securite ?", etc.
   For Portuguese: "O que a categoria Conflito mede na pontuacao de seguranca?", etc.

5. Pass `dateModified` to JSON-LD calls in all pages that build JSON-LD. Use a build-time timestamp: `new Date().toISOString().split('T')[0]` as the dateModified value. Update the methodology pages, country pages, homepage, global-safety, and compare pages to pass this value.
  </action>
  <verify>
    <automated>cd /Users/riccardo/Developer/VibeCoding/Isitsafetotravel && grep -c "toFixed(1)" src/lib/seo.ts && grep -c "dateModified" src/lib/seo.ts && grep -c "foundingDate" src/lib/seo.ts && grep -c "sameAs" src/lib/seo.ts && grep "What does the" src/pages/en/methodology/index.astro | head -1</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/seo.ts contains `.toFixed(1)` applied to the score value in buildCountryMetaDescription
    - src/lib/seo.ts contains `dateModified` in at least 4 function signatures
    - src/lib/seo.ts buildOrganizationJsonLd contains `foundingDate: '2026'`
    - src/lib/seo.ts buildOrganizationJsonLd contains `sameAs:` with GitHub URL
    - src/lib/seo.ts buildOrganizationJsonLd contains `logo:` with favicon.svg
    - src/pages/en/methodology/index.astro contains `What does the` in FAQ questions
    - All 5 methodology pages have language-appropriate natural language FAQ questions
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>All JSON-LD schemas have dateModified/datePublished, Organization schema is complete, FAQ uses natural language questions, country meta descriptions show rounded scores</done>
</task>

<task type="auto">
  <name>Task 2: Fix Base.astro (meta robots, preload hints) and llms.txt URLs</name>
  <files>src/layouts/Base.astro, public/llms.txt</files>
  <read_first>src/layouts/Base.astro, public/llms.txt, src/pages/en/country/[slug].astro (to confirm route pattern uses ISO3 lowercase)</read_first>
  <action>
1. In Base.astro `<head>`, add meta robots directive after the twitter card tags:
   ```html
   <meta name="robots" content="max-snippet:-1, max-image-preview:large, max-video-preview:-1" />
   ```

2. In Base.astro `<head>`, add preload hints for the two critical font families before the dark mode script. The fonts are imported via `@fontsource-variable/inter` and `@fontsource-variable/dm-sans`. Add preload for the CSS (Astro bundles fonts into CSS, so preloading the generated CSS is handled by Astro). Instead, add preload for the favicon:
   ```html
   <link rel="preload" href="/favicon.svg" as="image" type="image/svg+xml" />
   ```

3. Fix public/llms.txt: Replace all country URLs that use country names (like `/en/country/italy/`) with ISO3 codes (like `/en/country/ita/`). The actual routes use ISO3 lowercase codes as confirmed by the `[slug].astro` page which generates `params: { slug: country.iso3.toLowerCase() }`. Fix these sample countries:
   - `/en/country/italy/` -> `/en/country/ita/`
   - `/en/country/united-states/` -> `/en/country/usa/`
   - `/en/country/japan/` -> `/en/country/jpn/`
   - `/en/country/brazil/` -> `/en/country/bra/`
   - `/en/country/south-africa/` -> `/en/country/zaf/`
  </action>
  <verify>
    <automated>cd /Users/riccardo/Developer/VibeCoding/Isitsafetotravel && grep "max-snippet" src/layouts/Base.astro && grep "preload" src/layouts/Base.astro && grep "/en/country/ita/" public/llms.txt && ! grep "/en/country/italy/" public/llms.txt && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/layouts/Base.astro contains `<meta name="robots" content="max-snippet:-1, max-image-preview:large, max-video-preview:-1"`
    - src/layouts/Base.astro contains `<link rel="preload" href="/favicon.svg"`
    - public/llms.txt contains `/en/country/ita/` (not `/en/country/italy/`)
    - public/llms.txt contains `/en/country/usa/` (not `/en/country/united-states/`)
    - public/llms.txt contains `/en/country/jpn/` (not `/en/country/japan/`)
    - public/llms.txt contains `/en/country/bra/` (not `/en/country/brazil/`)
    - public/llms.txt contains `/en/country/zaf/` (not `/en/country/south-africa/`)
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>All pages serve meta robots max-snippet:-1, preload hints in head, llms.txt has correct ISO3 country URLs</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- `grep "dateModified" src/lib/seo.ts` shows at least 4 matches
- `grep "max-snippet" src/layouts/Base.astro` shows the robots meta tag
- `grep "/en/country/ita/" public/llms.txt` finds the corrected URL
- `grep "What does the" src/pages/en/methodology/index.astro` shows natural language FAQ
</verification>

<success_criteria>
- All 6 SEO metadata fixes implemented and building successfully
- JSON-LD validated with dateModified/datePublished on all page types
- Organization schema complete with logo, sameAs, foundingDate
- FAQ schema uses natural language questions in all 5 languages
- llms.txt uses correct ISO3-based URLs
- meta robots max-snippet:-1 on all pages via Base.astro
</success_criteria>

<output>
After completion, create `.planning/phases/27-seo-ai-search-optimization/27-01-SUMMARY.md`
</output>
