---
phase: 27-seo-ai-search-optimization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/en/about/index.astro
  - src/pages/it/chi-siamo/index.astro
  - src/pages/es/acerca-de/index.astro
  - src/pages/fr/a-propos/index.astro
  - src/pages/pt/sobre/index.astro
  - src/i18n/ui.ts
  - src/components/Header.astro
  - src/components/Footer.astro
  - src/components/country/PillarBreakdown.astro
  - src/pages/en/index.astro
  - src/pages/it/index.astro
  - src/pages/es/index.astro
  - src/pages/fr/index.astro
  - src/pages/pt/index.astro
autonomous: true
requirements: [SEO27-07, SEO27-08, SEO27-09, SEO27-10, SEO27-11]

must_haves:
  truths:
    - "User can navigate to an About page in all 5 languages from both header and footer"
    - "About page shows author bio (Riccardo Dominici), project mission, data sources, open-source mention, contact"
    - "All pages include Author/Person JSON-LD schema"
    - "Country detail pages have a static HTML table showing all 5 pillar scores"
    - "Homepage has a collapsed details element listing all 248 countries as crawlable links"
  artifacts:
    - path: "src/pages/en/about/index.astro"
      provides: "English About page with E-E-A-T content"
      contains: "Riccardo Dominici"
    - path: "src/components/country/PillarBreakdown.astro"
      provides: "Static HTML table before D3 chart"
      contains: "<table"
    - path: "src/pages/en/index.astro"
      provides: "Country listing in details/summary element"
      contains: "<details"
    - path: "src/components/Header.astro"
      provides: "About link in navigation"
      contains: "about"
    - path: "src/components/Footer.astro"
      provides: "About link in footer"
      contains: "about"
  key_links:
    - from: "src/components/Header.astro"
      to: "src/pages/*/about/*"
      via: "nav link using routes[lang].about"
      pattern: "r\\.about"
    - from: "src/components/Footer.astro"
      to: "src/pages/*/about/*"
      via: "footer link using routes[lang].about"
      pattern: "r\\.about"
    - from: "src/pages/en/index.astro"
      to: "src/pages/en/country/[slug].astro"
      via: "country listing links"
      pattern: "country.*iso3"
---

<objective>
Create About page in 5 languages for E-E-A-T, add Author/Person schema to all pages, add static HTML pillar table to country pages, and add crawlable country listing to homepage.

Purpose: Establish author expertise/trust signals for Google E-E-A-T, and provide crawlable static content for search engines that cannot execute JavaScript (pillar data and country links).
Output: About pages, updated Header/Footer with About links, static pillar table, homepage country listing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-seo-ai-search-optimization/27-CONTEXT.md
@src/i18n/ui.ts
@src/components/Header.astro
@src/components/Footer.astro
@src/components/country/PillarBreakdown.astro
@src/pages/en/index.astro
@src/pages/en/country/[slug].astro
@src/lib/seo.ts
</context>

<interfaces>
From src/i18n/ui.ts:
```typescript
export type Lang = 'en' | 'it' | 'es' | 'fr' | 'pt';
export const routes = {
  en: { about: 'about', ... },
  it: { about: 'chi-siamo', ... },
  es: { about: 'acerca-de', ... },
  fr: { about: 'a-propos', ... },
  pt: { about: 'sobre', ... },
};
```

From src/lib/seo.ts:
```typescript
export function buildWebPageJsonLd(title: string, description: string, canonicalUrl: string, lang: Lang): Record<string, unknown>;
```

From src/components/country/PillarBreakdown.astro:
```typescript
interface Props {
  pillars: PillarScore[];
  lang: Lang;
}
// PillarScore = { name: PillarName; score: number; dataCompleteness: number; partial: boolean }
```

From src/pages/en/index.astro:
```typescript
// Uses Base layout, SafetyMap, GlobalScoreBanner
// Scores loaded via loadLatestScores() from src/lib/scores.ts
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create About page in 5 languages with Author/Person schema, update Header and Footer nav</name>
  <files>src/pages/en/about/index.astro, src/pages/it/chi-siamo/index.astro, src/pages/es/acerca-de/index.astro, src/pages/fr/a-propos/index.astro, src/pages/pt/sobre/index.astro, src/i18n/ui.ts, src/components/Header.astro, src/components/Footer.astro, src/lib/seo.ts</files>
  <read_first>src/pages/en/legal/index.astro (reference for static page pattern), src/components/Header.astro, src/components/Footer.astro, src/i18n/ui.ts (lines 1-100 for translation key patterns), src/lib/seo.ts, src/layouts/Base.astro</read_first>
  <action>
1. Add translation keys to src/i18n/ui.ts for all 5 languages:
   - `'nav.about': 'About'` (en), `'Chi siamo'` (it), `'Acerca de'` (es), `'A propos'` (fr), `'Sobre'` (pt)
   - `'about.title': 'About IsItSafeToTravel'` and translations
   - `'about.description': 'Learn about the team, mission, and methodology behind IsItSafeToTravel safety scores.'` and translations
   - `'footer.about': 'About'` and translations

2. Add `buildPersonJsonLd()` function to src/lib/seo.ts:
   ```typescript
   export function buildPersonJsonLd(): Record<string, unknown> {
     return {
       '@context': 'https://schema.org',
       '@type': 'Person',
       name: 'Riccardo Dominici',
       jobTitle: 'Independent developer and data analyst',
       url: 'https://github.com/RiccardoDominici',
     };
   }
   ```

3. Create src/pages/en/about/index.astro following the same pattern as legal/index.astro:
   - `export const prerender = true;`
   - Import Base, Breadcrumb, useTranslations, buildWebPageJsonLd, buildPersonJsonLd
   - JSON-LD: @graph with WebPage + Person nodes
   - Breadcrumb: Home > About
   - Content sections (in a `max-w-3xl mx-auto px-4 py-8` container):
     a. **About the Project** — "IsItSafeToTravel is a free, open-source travel safety platform that provides composite safety scores for 248 countries worldwide. Scores are computed daily from 9+ trusted public sources including the Global Peace Index, INFORM Risk Index, GDELT, WHO Disease Outbreak News, ReliefWeb, GDACS, and government travel advisories from the US, UK, Canada, and Australia."
     b. **Mission** — "Our mission is to make travel safety information transparent, accessible, and data-driven. Every score is backed by publicly verifiable sources, and our methodology is fully documented."
     c. **Author** — "IsItSafeToTravel is created and maintained by Riccardo Dominici, an independent developer and data analyst. The project is built with a focus on data transparency, automated pipelines, and open-source principles."
     d. **Data Sources** — Brief list: "We aggregate data from 9+ public sources updated daily. See our Methodology page for full details on how scores are calculated." with link to methodology.
     e. **Open Source** — "This project is fully open source. View the source code, report issues, or contribute on GitHub." with link to https://github.com/RiccardoDominici/IsItSafeToTravel
     f. **Contact** — "For questions, feedback, or data inquiries, reach out via GitHub Issues."
   - Use Tailwind classes consistent with legal page styling (headings: `text-xl font-heading font-bold`, paragraphs: `text-sand-600 dark:text-sand-300`, sections separated by `space-y-6`)

4. Create the 4 non-English About pages (it, es, fr, pt) with translated content following the same structure. Each uses its own `lang` constant and route-appropriate directory:
   - it: src/pages/it/chi-siamo/index.astro
   - es: src/pages/es/acerca-de/index.astro
   - fr: src/pages/fr/a-propos/index.astro
   - pt: src/pages/pt/sobre/index.astro

5. Add About link to Header.astro desktop nav and mobile nav (between Home and Methodology):
   ```html
   <a href={`/${lang}/${r.about}/`} class="text-sm text-sand-600 dark:text-sand-300 hover:text-sand-900 dark:hover:text-sand-50 transition-colors inline-flex items-center min-h-[44px] px-2">
     {t('nav.about')}
   </a>
   ```
   Add to both the desktop `<nav>` and mobile `<nav>` sections.

6. Add About link to Footer.astro in the links section (between Methodology and Legal):
   ```html
   <a href={`/${lang}/${r.about}/`} class="text-sm text-sand-600 dark:text-sand-300 hover:text-terracotta-500 dark:hover:text-terracotta-400 transition-colors">
     {t('footer.about')}
   </a>
   ```
  </action>
  <verify>
    <automated>cd /Users/riccardo/Developer/VibeCoding/Isitsafetotravel && test -f src/pages/en/about/index.astro && test -f src/pages/it/chi-siamo/index.astro && test -f src/pages/es/acerca-de/index.astro && test -f src/pages/fr/a-propos/index.astro && test -f src/pages/pt/sobre/index.astro && grep "nav.about" src/components/Header.astro && grep "about" src/components/Footer.astro && grep "buildPersonJsonLd" src/lib/seo.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/en/about/index.astro exists and contains "Riccardo Dominici"
    - src/pages/it/chi-siamo/index.astro exists and contains "Riccardo Dominici"
    - src/pages/es/acerca-de/index.astro exists
    - src/pages/fr/a-propos/index.astro exists
    - src/pages/pt/sobre/index.astro exists
    - src/lib/seo.ts contains `export function buildPersonJsonLd`
    - src/components/Header.astro contains `r.about` in both desktop and mobile nav
    - src/components/Footer.astro contains `r.about`
    - src/i18n/ui.ts contains `'nav.about'` key for all 5 languages
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>About page accessible in all 5 languages from header and footer, Person schema present in JSON-LD</done>
</task>

<task type="auto">
  <name>Task 2: Add static HTML pillar table to country pages and crawlable country listing to homepage</name>
  <files>src/components/country/PillarBreakdown.astro, src/pages/en/index.astro, src/pages/it/index.astro, src/pages/es/index.astro, src/pages/fr/index.astro, src/pages/pt/index.astro</files>
  <read_first>src/components/country/PillarBreakdown.astro, src/pages/en/index.astro, src/lib/scores.ts (first 30 lines for loadLatestScores signature), src/pipeline/types.ts (first 30 lines for ScoredCountry type)</read_first>
  <action>
1. In PillarBreakdown.astro, add a static HTML `<table>` BEFORE the existing SVG bar chart `<div>` (inside the same `<section>`). This table is for search engine crawlers and provides the same data as the visual chart:
   ```html
   <table class="w-full text-sm mb-4 border-collapse">
     <thead>
       <tr class="border-b border-sand-200 dark:border-sand-700">
         <th class="text-left py-2 text-sand-600 dark:text-sand-400">{t('country.pillar_category')}</th>
         <th class="text-right py-2 text-sand-600 dark:text-sand-400">{t('country.score_label')}</th>
         <th class="text-right py-2 text-sand-600 dark:text-sand-400">{t('country.pillar_risk')}</th>
       </tr>
     </thead>
     <tbody>
       {sorted.map((pillar) => {
         const label = t(pillarKeys[pillar.name] as any);
         const displayScore = (pillar.score * 10).toFixed(1);
         const riskLevel = pillar.score >= 0.7 ? t('country.risk_low') : pillar.score >= 0.4 ? t('country.risk_moderate') : t('country.risk_high');
         return (
           <tr class="border-b border-sand-100 dark:border-sand-800">
             <td class="py-2 text-sand-700 dark:text-sand-300">{label}</td>
             <td class="py-2 text-right text-sand-700 dark:text-sand-300">{displayScore}/10</td>
             <td class="py-2 text-right text-sand-700 dark:text-sand-300">{riskLevel}</td>
           </tr>
         );
       })}
     </tbody>
   </table>
   ```
   Add these translation keys to ui.ts for all 5 languages:
   - `'country.pillar_category': 'Category'` / `'Categoria'` / `'Categoria'` / `'Categorie'` / `'Categoria'`
   - `'country.pillar_risk': 'Risk Level'` / `'Livello di Rischio'` / `'Nivel de Riesgo'` / `'Niveau de Risque'` / `'Nivel de Risco'`
   - `'country.risk_low': 'Low'` / `'Basso'` / `'Bajo'` / `'Faible'` / `'Baixo'`
   - `'country.risk_moderate': 'Moderate'` / `'Moderato'` / `'Moderado'` / `'Modere'` / `'Moderado'`
   - `'country.risk_high': 'High'` / `'Alto'` / `'Alto'` / `'Eleve'` / `'Alto'`

2. In all 5 homepage index.astro files, add a `<details>` element after the SafetyMap section (after the closing `</section>` tag, before `</Base>`). This provides crawlable country links:
   ```html
   <details class="max-w-6xl mx-auto px-4 py-6">
     <summary class="cursor-pointer text-sm font-medium text-sand-600 dark:text-sand-400 hover:text-sand-800 dark:hover:text-sand-200">
       {t('home.all_countries')} (248)
     </summary>
     <div class="mt-4 columns-2 sm:columns-3 md:columns-4 gap-4">
       {countries.map(c => (
         <a href={`/${lang}/country/${c.iso3.toLowerCase()}/`}
            class="block text-sm text-sand-600 dark:text-sand-400 hover:text-terracotta-500 dark:hover:text-terracotta-400 py-0.5">
           {c.name[lang]}
         </a>
       ))}
     </div>
   </details>
   ```
   Import `loadLatestScores` at the top of each homepage file and load the countries:
   ```typescript
   import { loadLatestScores } from '../../lib/scores';
   const countries = loadLatestScores().sort((a, b) => a.name[lang].localeCompare(b.name[lang]));
   ```
   Add translation key: `'home.all_countries': 'All Countries'` / `'Tutti i Paesi'` / `'Todos los Paises'` / `'Tous les Pays'` / `'Todos os Paises'`
  </action>
  <verify>
    <automated>cd /Users/riccardo/Developer/VibeCoding/Isitsafetotravel && grep "<table" src/components/country/PillarBreakdown.astro && grep "<details" src/pages/en/index.astro && grep "loadLatestScores" src/pages/en/index.astro && grep "home.all_countries" src/i18n/ui.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/country/PillarBreakdown.astro contains `<table` with thead and tbody
    - src/components/country/PillarBreakdown.astro table appears before the SVG chart div
    - src/pages/en/index.astro contains `<details` element
    - src/pages/en/index.astro imports `loadLatestScores`
    - All 5 homepage files have the `<details>` country listing
    - src/i18n/ui.ts contains `'home.all_countries'` for all 5 languages
    - src/i18n/ui.ts contains `'country.pillar_category'` for all 5 languages
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>Country pages have static crawlable pillar table, all homepages have collapsed country listing with 248 clickable links</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- About page exists and renders in all 5 languages
- Header and Footer both link to About page
- Country detail pages have a `<table>` element with pillar scores
- Homepage has a `<details>` element with country links
</verification>

<success_criteria>
- About page accessible at /{lang}/about/ (and localized equivalents) in all 5 languages
- Author bio mentions Riccardo Dominici
- Person JSON-LD schema present on About pages
- Static HTML table visible on all country detail pages with 5 pillar rows
- Homepage country listing collapsed by default, contains 248 country links
- Build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/27-seo-ai-search-optimization/27-02-SUMMARY.md`
</output>
