---
phase: 24-gdelt-stability-fetcher
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - src/pipeline/fetchers/index.ts
  - src/pipeline/scoring/normalize.ts
  - src/pipeline/config/weights.json
autonomous: true
requirements: [SRC-01]

must_haves:
  truths:
    - "GDELT fetcher runs as part of the pipeline alongside all other sources"
    - "GDELT instability values are normalized to 0-1 range (inverse: higher instability = lower safety)"
    - "GDELT contributes no more than 15% of the conflict pillar via indicatorWeights"
    - "Pipeline completes end-to-end with 8 total data sources"
  artifacts:
    - path: "src/pipeline/fetchers/index.ts"
      provides: "fetchGdelt registration in fetchAllSources"
      contains: "fetchGdelt"
    - path: "src/pipeline/scoring/normalize.ts"
      provides: "gdelt_instability normalization range"
      contains: "gdelt_instability"
    - path: "src/pipeline/config/weights.json"
      provides: "GDELT indicator in conflict pillar with capped weight"
      contains: "gdelt_instability"
  key_links:
    - from: "src/pipeline/fetchers/index.ts"
      to: "src/pipeline/fetchers/gdelt.ts"
      via: "import { fetchGdelt }"
      pattern: "fetchGdelt"
    - from: "src/pipeline/config/weights.json"
      to: "src/pipeline/scoring/engine.ts"
      via: "indicatorWeights for conflict pillar"
      pattern: "gdelt_instability.*0\\.15"
---

<objective>
Wire the GDELT fetcher into the pipeline: register in fetchAllSources, add normalization range, add to conflict pillar weights with 15% cap via indicatorWeights, and verify end-to-end pipeline run.

Purpose: Completing the integration so GDELT instability scores flow into the conflict pillar with media bias contained by the 15% weight cap.
Output: Updated pipeline config files and successful end-to-end run with 8 data sources.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-gdelt-stability-fetcher/24-CONTEXT.md
@.planning/phases/24-gdelt-stability-fetcher/24-01-SUMMARY.md
@.planning/phases/23-reliefweb-and-gdacs-fetchers/23-02-SUMMARY.md

<interfaces>
<!-- Current state of files to modify -->

From src/pipeline/fetchers/index.ts (current):
```typescript
import { fetchReliefweb } from './reliefweb.js';
import { fetchGdacs } from './gdacs.js';
// ... 7 sources in Promise.allSettled
```

From src/pipeline/scoring/normalize.ts (current ranges):
```typescript
// Last entries:
gdacs_disaster_alerts: { min: 0, max: 5, inverse: true },
```

From src/pipeline/config/weights.json (current v5.1.0):
```json
{
  "version": "5.1.0",
  "pillars": [
    {
      "name": "conflict",
      "weight": 0.30,
      "indicators": ["wb_political_stability", "gpi_overall", "gpi_safety_security", "gpi_militarisation", "acled_fatalities", "acled_events"]
    },
    ...
  ]
}
```

From src/pipeline/scoring/engine.ts (line 172):
```typescript
const subWeight = pillarDef.indicatorWeights?.[ind.name]
  ?? (1.0 / pillarDef.indicators.length);
```
Engine supports indicatorWeights with fallback to equal weights.

From src/pipeline/config/source-tiers.json (already has gdelt entry):
```json
"gdelt": { "tier": "signal", "maxAgeDays": 14, "decayHalfLifeDays": 3 }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register GDELT fetcher and add normalization range</name>
  <files>src/pipeline/fetchers/index.ts, src/pipeline/scoring/normalize.ts</files>
  <read_first>
    - src/pipeline/fetchers/index.ts (current imports and Promise.allSettled array)
    - src/pipeline/scoring/normalize.ts (current INDICATOR_RANGES)
    - src/pipeline/fetchers/gdelt.ts (confirm export name)
  </read_first>
  <action>
**In `src/pipeline/fetchers/index.ts`:**
1. Add import: `import { fetchGdelt } from './gdelt.js';`
2. Add export: `export { fetchGdelt } from './gdelt.js';`
3. Add `fetchGdelt(date)` to the `Promise.allSettled([...])` array
4. Add `'gdelt'` to the `sourceNames` array at the matching index
5. Update the JSDoc comment to include GDELT (8 total sources)

**In `src/pipeline/scoring/normalize.ts`:**
1. Add to INDICATOR_RANGES:
```typescript
// GDELT instability score (higher = more unstable = less safe)
gdelt_instability: { min: 0, max: 1.0, inverse: true },
```
Note: GDELT Stability Timeline returns instability ratio in 0-1 range natively, so min=0, max=1.0. Inverse because higher instability = less safe.

**Verify:** `npx tsc --noEmit` passes.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "fetchGdelt" src/pipeline/fetchers/index.ts
    - grep -q "gdelt_instability" src/pipeline/scoring/normalize.ts
    - grep -q "'gdelt'" src/pipeline/fetchers/index.ts
  </acceptance_criteria>
  <done>
    - GDELT fetcher registered in fetchAllSources (8 sources total)
    - Normalization range for gdelt_instability added (0-1, inverse)
    - TypeScript compiles with no errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Add GDELT to conflict pillar with 15% weight cap and run pipeline</name>
  <files>src/pipeline/config/weights.json</files>
  <read_first>
    - src/pipeline/config/weights.json (current conflict pillar)
    - src/pipeline/scoring/engine.ts (confirm indicatorWeights handling at line ~172)
  </read_first>
  <action>
**In `src/pipeline/config/weights.json`:**
1. Bump version to `"5.2.0"`
2. Add `"gdelt_instability"` to the conflict pillar's `indicators` array
3. Add `indicatorWeights` to the conflict pillar to cap GDELT at 15%:
```json
{
  "name": "conflict",
  "weight": 0.30,
  "indicators": ["wb_political_stability", "gpi_overall", "gpi_safety_security", "gpi_militarisation", "acled_fatalities", "acled_events", "gdelt_instability"],
  "indicatorWeights": {
    "wb_political_stability": 0.17,
    "gpi_overall": 0.17,
    "gpi_safety_security": 0.14,
    "gpi_militarisation": 0.14,
    "acled_fatalities": 0.12,
    "acled_events": 0.11,
    "gdelt_instability": 0.15
  }
}
```
The 6 existing indicators share 0.85 (the remaining after GDELT's 0.15). Values sum to 1.0. Baseline indicators (wb, gpi) get slightly more weight than signal indicators (acled) because they are more stable. GDELT is explicitly capped at 0.15 to contain media bias.

**Run the pipeline to verify end-to-end:**
```bash
npx tsx src/pipeline/run.ts
```
Confirm output shows 8 sources attempted, GDELT results appear, and 248 countries scored.

Note: GDELT fetching may take 2-3 minutes due to sequential per-country requests. Some sources (GPI, ACLED, ReliefWeb) may fail due to missing credentials -- that is expected and does not affect this verification.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "gdelt_instability" src/pipeline/config/weights.json
    - grep -q "5.2.0" src/pipeline/config/weights.json
    - grep -q "indicatorWeights" src/pipeline/config/weights.json
    - grep -q "0.15" src/pipeline/config/weights.json
  </acceptance_criteria>
  <done>
    - weights.json v5.2.0 with gdelt_instability in conflict pillar
    - indicatorWeights caps GDELT at 15% of conflict pillar
    - All indicatorWeights sum to 1.0
    - Pipeline runs end-to-end (GDELT fetcher attempts all countries)
    - TypeScript compiles with no errors
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `grep -c "gdelt" src/pipeline/fetchers/index.ts` returns at least 3 (import, export, call)
- `grep "gdelt_instability" src/pipeline/config/weights.json` shows the indicator
- `node -e "const w = require('./src/pipeline/config/weights.json'); const c = w.pillars.find(p => p.name === 'conflict'); console.log(Object.values(c.indicatorWeights).reduce((a,b)=>a+b,0))"` outputs 1.0 (weights sum check)
</verification>

<success_criteria>
GDELT fetcher is fully integrated into the pipeline: registered in fetchAllSources, normalization range defined, conflict pillar weights updated with 15% cap, and pipeline runs end-to-end with 8 data sources.
</success_criteria>

<output>
After completion, create `.planning/phases/24-gdelt-stability-fetcher/24-02-SUMMARY.md`
</output>
