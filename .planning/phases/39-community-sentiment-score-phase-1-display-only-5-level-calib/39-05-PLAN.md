---
phase: 39
plan: 05
name: Build-time sentiment loader + SentimentPillar display card (the "6th pillar")
type: execute
wave: 2
depends_on:
  - "39-02"
  - "39-03"
files_modified:
  - src/lib/sentiment.ts
  - src/lib/__tests__/sentiment.test.ts
  - src/components/country/SentimentPillar.astro
autonomous: true
requirements:
  - D-04
  - D-06
  - D-07
  - D-09
  - D-14
must_haves:
  truths:
    - "loadSentimentForCountry(iso3) reads data/sentiment/latest.json at build and returns the country's SentimentEntry or null when the file/country is absent (never throws)"
    - "SentimentPillar renders a bar/label/value row visually identical to the other 5 pillars, using pillarToColor at the perceived value"
    - "The card shows a neutral 'Community' badge, the signed correction ('+0.4 vs official score'), the vote count, and a persistent 'does not affect the safety score' disclaimer"
    - "When the entry is null or count < MIN_VOTE_FLOOR (5), the card renders the localized 'not enough votes yet' state instead of a bar+value, and still shows the vote CTA slot"
    - "The component adds NO JSON-LD and is not a member of country.pillars (display-only, D-06)"
  artifacts:
    - path: "src/lib/sentiment.ts"
      provides: "Build-time loader + MIN_VOTE_FLOOR constant"
      exports: ["loadSentimentForCountry", "MIN_VOTE_FLOOR"]
    - path: "src/components/country/SentimentPillar.astro"
      provides: "Display-only 6th-pillar card + slot for the vote widget"
      contains: "slot"
    - path: "src/lib/__tests__/sentiment.test.ts"
      provides: "Graceful-load test (missing file ⇒ null)"
      contains: "loadSentimentForCountry"
  key_links:
    - from: "src/components/country/SentimentPillar.astro"
      to: "src/lib/colors.ts pillarToColor"
      via: "bar fill at perceived value"
      pattern: "pillarToColor"
    - from: "SentimentPillar.astro"
      to: "sentiment entry (perceived/correction/count)"
      via: "count >= MIN_VOTE_FLOOR gate"
      pattern: "MIN_VOTE_FLOOR|count"
---

<objective>
Provide the build-time read path and the display-only "6th pillar" card. `src/lib/sentiment.ts` mirrors `src/lib/scores.ts` (fs read with existsSync guard ⇒ graceful null per D-14). `SentimentPillar.astro` renders the perceived value as a bar row visually identical to `PillarBreakdown` (D-04) plus the community badge, signed correction (D-07), vote count, and the persistent "does not affect the safety score" disclaimer, with a below-floor empty state (D-09). It exposes a `<slot/>` where the country page injects the vote widget (39-06), keeping the two components decoupled.

Purpose: The visible surface of the feature. Structurally independent of `country.pillars` so it can never touch the total score (D-06).
Output: `src/lib/sentiment.ts`, `src/lib/__tests__/sentiment.test.ts`, `src/components/country/SentimentPillar.astro`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/39-community-sentiment-score-phase-1-display-only-5-level-calib/39-CONTEXT.md
@.planning/phases/39-community-sentiment-score-phase-1-display-only-5-level-calib/39-UI-SPEC.md
@.planning/phases/39-community-sentiment-score-phase-1-display-only-5-level-calib/39-PATTERNS.md
@.planning/phases/39-community-sentiment-score-phase-1-display-only-5-level-calib/39-02-SUMMARY.md

<interfaces>
<!-- From 39-02 (src/pipeline/types.ts) -->
```typescript
import type { SentimentEntry } from '../pipeline/types';
// SentimentEntry = { iso3, count, avgDelta, correction, perceived, official }
```
<!-- Baked file this lib reads (from 39-04 pipeline; may be absent on first build ⇒ null) -->
// data/sentiment/latest.json = { generatedAt: string, countries: Record<iso3, SentimentEntry> }

<!-- Component contract consumed by 39-08 country wiring -->
// <SentimentPillar sentiment={SentimentEntry|null} official={number} lang={Lang}> <slot/> </SentimentPillar>
</interfaces>

Analogs (READ): `src/lib/scores.ts` (lines 1-36: DATA_DIR/loadLatestSnapshot/fs.existsSync graceful pattern — copy for a `data/sentiment/latest.json` reader), `src/components/country/PillarBreakdown.astro` (full — row anatomy: `w-28` label, `viewBox="0 0 200 24"` `h-6` SVG bar with track+value rects, `w-14` value; imports `pillarToColor` from `src/lib/colors.ts`), the "updated daily" badge geometry in `src/pages/en/country/[slug].astro:88-91` (badge chip class to mirror as the neutral sand "Community" badge). Exact visual/interaction spec: 39-UI-SPEC.md §"Component Inventory 1" + §"Copywriting Contract" (use the i18n keys from 39-03: `country.pillar.sentiment`, `sentiment.badge/disclaimer/correction_label/vote_count/empty_title/empty_body`).
Do NOT import `PillarScore` or add sentiment to `country.pillars` (39-PATTERNS.md architectural constraint). Do NOT emit JSON-LD (D-21/D-22).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/lib/sentiment.ts — build-time loader + MIN_VOTE_FLOOR (+ graceful test)</name>
  <files>src/lib/sentiment.ts, src/lib/__tests__/sentiment.test.ts</files>
  <read_first>
    - src/lib/scores.ts (lines 1-36 — DATA_DIR, loadLatestSnapshot, existsSync guard, `?? []`/null returns)
    - src/pipeline/types.ts (SentimentEntry / SentimentSnapshot from 39-02)
  </read_first>
  <behavior>
    - loadSentimentForCountry('ITA') returns the ITA SentimentEntry when data/sentiment/latest.json exists and contains it.
    - Returns null when the file is missing (first build / degraded pipeline — D-14).
    - Returns null when the file exists but the country is absent.
    - Never throws on malformed JSON (catch ⇒ null).
    - MIN_VOTE_FLOOR === 5 exported (mirrors SENTIMENT_MIN_VOTES; the below-floor display gate).
  </behavior>
  <action>
    Create `src/lib/sentiment.ts` with `const DATA_DIR = path.join(process.cwd(),'data','sentiment')`, `export const MIN_VOTE_FLOOR = 5;`, and `export function loadSentimentForCountry(iso3: string): SentimentEntry | null` that `fs.existsSync`-guards `data/sentiment/latest.json`, parses inside try/catch, and returns `parsed.countries?.[iso3] ?? null`. Import the `SentimentEntry` type from `../pipeline/types`. Mirror the exact style of `src/lib/scores.ts` (node:fs + node:path). Create `src/lib/__tests__/sentiment.test.ts` (node:test): assert null when no file present (run in a cwd/temp where the file is absent, OR point the read at a non-existent fixture by asserting the missing-file branch), assert an entry is returned for a temp fixture written to a tmp dir and read via a small injectable path — if injecting a path is awkward, assert the two null branches (missing file, missing country) which are the D-14-critical cases.
  </action>
  <acceptance_criteria>
    - `node --import tsx --test src/lib/__tests__/sentiment.test.ts` exits 0
    - loadSentimentForCountry returns null (not throw) when data/sentiment/latest.json is absent
    - `grep -q "export const MIN_VOTE_FLOOR = 5" src/lib/sentiment.ts`
    - Uses fs.existsSync guard and a try/catch (no unguarded readFileSync/JSON.parse)
  </acceptance_criteria>
  <verify>
    <automated>node --import tsx --test src/lib/__tests__/sentiment.test.ts && grep -q "MIN_VOTE_FLOOR = 5" src/lib/sentiment.ts && grep -q "existsSync" src/lib/sentiment.ts && echo PASS</automated>
  </verify>
  <done>The loader resolves the per-country entry or null, never throwing, with MIN_VOTE_FLOOR exported and the graceful path tested.</done>
</task>

<task type="auto">
  <name>Task 2: SentimentPillar.astro — 6th-pillar card, correction/count/disclaimer, below-floor state, widget slot</name>
  <files>src/components/country/SentimentPillar.astro</files>
  <read_first>
    - src/components/country/PillarBreakdown.astro (full — copy the row anatomy + pillarToColor import + bar SVG structure)
    - src/lib/colors.ts (pillarToColor signature)
    - src/pages/en/country/[slug].astro:84-92 (badge chip class to mirror for the neutral "Community" badge)
    - .planning/phases/39-.../39-UI-SPEC.md §"Component Inventory 1" + §"Widget interaction states" (empty/below-floor) + §"Copywriting Contract"
    - src/lib/sentiment.ts (MIN_VOTE_FLOOR from Task 1)
  </read_first>
  <action>
    Create `src/components/country/SentimentPillar.astro`. Props: `sentiment: SentimentEntry | null`, `official: number`, `lang: Lang`. Import `useTranslations` from `../../i18n/utils`, `pillarToColor` from `../../lib/colors`, `MIN_VOTE_FLOOR` from `../../lib/sentiment`, and the `SentimentEntry` type. Render ONE bordered panel `rounded-lg border border-sand-200 dark:border-sand-700 p-4 bg-white dark:bg-sand-800` placed as a `<section>` (the country page positions it right after `#pillars`). Compute `hasEnough = sentiment !== null && sentiment.count >= MIN_VOTE_FLOOR`. Header: `t('country.pillar.sentiment')` H2 (`text-xl md:text-2xl font-heading font-bold text-sand-800 dark:text-sand-100`) plus a neutral "Community" badge (`inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-xs font-semibold bg-sand-100 dark:bg-sand-700 text-sand-700 dark:text-sand-200`, text `t('sentiment.badge')`). If `hasEnough`: render the pillar row exactly like PillarBreakdown — `w-28` right-aligned label (reuse `t('country.pillar.sentiment')`), an SVG `viewBox="0 0 200 24"` `h-6` with a track rect (`text-sand-200 dark:text-sand-700`) and a value rect of width `Math.max(sentiment.perceived/10*180, 2)` filled `pillarToColor(sentiment.perceived/10)`, `role="img"` + localized `aria-label` ("Sentiment: X.X/10 (community estimate)"), and a `w-14 text-sm font-medium` value `sentiment.perceived.toFixed(1)/10`; below the row show the signed correction via `t('sentiment.correction_label')` with `{delta}` replaced by a signed one-decimal string (e.g. `+0.4`, `−0.7`), the vote count via `t('sentiment.vote_count')` with `{count}`, and the persistent disclaimer `t('sentiment.disclaimer')` (`text-xs text-sand-500 dark:text-sand-400`, plain — not red). If NOT `hasEnough`: render the empty state (`t('sentiment.empty_title')` + `t('sentiment.empty_body')`) in place of the bar/value. In BOTH cases, after a `border-t border-sand-200 dark:border-sand-700` divider, emit `<slot />` for the vote widget (39-06 is injected here by the country page). Use pillarToColor's existing 0–1 domain (pass perceived/10). Emit NO `<script type="application/ld+json">` and no schema. Interpolate with the site's `.replace('{x}', …)` idiom.
  </action>
  <acceptance_criteria>
    - `src/components/country/SentimentPillar.astro` imports `pillarToColor` and `MIN_VOTE_FLOOR`, and contains a `<slot />`
    - It branches on `count >= MIN_VOTE_FLOOR`: a bar+value+correction+count path and an empty-state path
    - It renders the disclaimer `t('sentiment.disclaimer')` in the has-data path (persistent, not tooltip-only)
    - The correction string is signed (contains logic producing a leading + or − for the delta)
    - No `application/ld+json` and no import of `PillarScore`
    - `npx astro check` reports no new errors attributable to this file
  </acceptance_criteria>
  <verify>
    <automated>grep -q "pillarToColor" src/components/country/SentimentPillar.astro && grep -q "MIN_VOTE_FLOOR" src/components/country/SentimentPillar.astro && grep -q "<slot" src/components/country/SentimentPillar.astro && grep -q "sentiment.disclaimer" src/components/country/SentimentPillar.astro && ! grep -q "application/ld+json" src/components/country/SentimentPillar.astro && ! grep -q "PillarScore" src/components/country/SentimentPillar.astro && echo PASS</automated>
  </verify>
  <done>SentimentPillar renders the community-perceived value as a 6th-pillar bar with correction/count/disclaimer and a below-floor empty state, exposes a slot for the widget, and adds no JSON-LD.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| baked latest.json → SSG render | Server-computed numeric aggregates cross into HTML at build |
| component output → country @graph | Must add NO JSON-LD (D-21/D-22) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-17 | Tampering (XSS) | Rendered sentiment values | mitigate | Only numeric fields (perceived/correction/count) rendered; Astro auto-escapes; no user strings from D1 reach the DOM |
| T-39-18 | (integrity) | SEO @graph regression | mitigate | Component emits no JSON-LD; verified by grep + phase-gate validate:seo (D-22) |
| T-39-19 | Availability | Missing baked file crashes build | mitigate | loadSentimentForCountry returns null; component renders empty state (D-9/D-14) |
| T-39-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed; no install task |
</threat_model>

<verification>
- `node --import tsx --test src/lib/__tests__/sentiment.test.ts` passes.
- `npx astro check` shows no new errors from the two new files.
- Grep gate in Task 2 passes (slot present, disclaimer present, no JSON-LD, no PillarScore import).
- Phase gate (after 39-08 wiring): `npx astro build && npm run validate:seo` all-pass (D-22).
</verification>

<success_criteria>
- The perceived value renders as a bar identical in shape to the other 5 pillars (D-04), with badge/correction/count/disclaimer (D-07) and a below-floor state (D-09).
- The loader degrades to null and the card to the empty state when there is no data (D-14).
- No JSON-LD, no membership in country.pillars (D-06/D-21/D-22).
</success_criteria>

<output>
Create `.planning/phases/39-community-sentiment-score-phase-1-display-only-5-level-calib/39-05-SUMMARY.md` when done.
</output>
