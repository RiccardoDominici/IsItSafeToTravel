---
phase: 39
plan: 03
name: i18n — sentiment.* + country.pillar.sentiment + methodology/privacy keys across all 7 locales
type: execute
wave: 1
depends_on: []
files_modified:
  - src/i18n/ui.ts
autonomous: true
requirements:
  - D-02
  - D-05
  - D-07
  - D-09
  - D-17
  - D-19
  - D-20
must_haves:
  truths:
    - "Every one of the 7 ui locale blocks (en, it, es, fr, pt, zh, de) contains the identical sentiment.* key set, the country.pillar.sentiment key, and the methodology.sentiment_* + legal.privacy_votes_* keys, with locale-appropriate translations"
    - "The 5 calibration option labels map to the correct signed deltas per D-02 (way-too-high = -2 … way-too-low = +2)"
    - "Interpolation tokens {country}, {delta}, {count} are present in the strings that need them"
    - "No key exists in one locale but is missing from another (parity across all 7 blocks)"
  artifacts:
    - path: "src/i18n/ui.ts"
      provides: "Translated sentiment + methodology + privacy strings for the Sentiment feature, all 7 locales"
      contains: "country.pillar.sentiment"
  key_links:
    - from: "src/i18n/ui.ts sentiment.* keys"
      to: "SentimentPillar/SentimentVote components + methodology/legal pages"
      via: "useTranslations(lang) lookup"
      pattern: "sentiment\\."
---

<objective>
Add every user-facing string for the Sentiment feature to the single i18n source of truth `src/i18n/ui.ts`, translated across all 7 locales (D-05). This is the ONLY plan that edits `ui.ts` (avoids merge conflicts with the parallel component/doc plans). Downstream plans 39-05 (pillar), 39-06 (widget), and 39-07 (docs) consume these keys.

Purpose: `ui.ts` is the one place both HTML hreflang and the sitemap derive strings from; every locale block is an independent flat Record with identical key sets, so a missing key silently degrades that locale.
Output: `src/i18n/ui.ts` with new keys in all 7 `ui.*` blocks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/39-community-sentiment-score-phase-1-display-only-5-level-calib/39-CONTEXT.md
@.planning/phases/39-community-sentiment-score-phase-1-display-only-5-level-calib/39-UI-SPEC.md
@.planning/phases/39-community-sentiment-score-phase-1-display-only-5-level-calib/39-PATTERNS.md

Canonical English copy is the 39-UI-SPEC.md "Copywriting Contract" table. Locale blocks and the existing `country.pillar.*` cluster to mirror are documented in 39-PATTERNS.md §"src/i18n/ui.ts" (en block starts ~line 22; per-locale `country.pillar.*` clusters near en:58, it:538; zh block ~2414, de block ~2888). Every key below MUST be added to all 7 blocks in the same relative position (immediately after the `country.pillar.environment` line for the pillar label, and as a new adjacent cluster for the rest).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the sentiment.* + country.pillar.sentiment key cluster to all 7 locale blocks</name>
  <files>src/i18n/ui.ts</files>
  <read_first>
    - src/i18n/ui.ts (locate all 7 ui.* blocks and the country.pillar.* cluster in each; confirm interpolation is done via .replace('{x}', …) elsewhere)
    - .planning/phases/39-.../39-UI-SPEC.md §"Copywriting Contract" (canonical en strings) + §"Component Inventory" (semantics of each string)
  </read_first>
  <action>
    In EACH of the 7 `ui` locale blocks add: `'country.pillar.sentiment'` (label "Community Sentiment"), and the `sentiment.*` cluster — `sentiment.badge` ("Community"), `sentiment.disclaimer` ("Community estimate — does not affect the safety score."), `sentiment.correction_label` ("{delta} vs official score"), `sentiment.vote_count` ("Based on {count} votes"), `sentiment.question` ("Does this safety score feel right for {country}?"), the five calibration option labels `sentiment.level_way_high` (delta -2, "Way too high"), `sentiment.level_high` (-1, "Too high"), `sentiment.level_right` (0, "About right"), `sentiment.level_low` (+1, "Too low"), `sentiment.level_way_low` (+2, "Way too low"), `sentiment.submit` ("Submit vote"), `sentiment.empty_title` ("Not enough votes yet"), `sentiment.empty_body` ("Be the first to say whether this score feels right — use the calibration below."), `sentiment.thanks_title` ("Thanks for your input!"), `sentiment.thanks_body` ("Your calibration was recorded. The community estimate updates once a day."), and `sentiment.error` ("Couldn't record your vote right now — please try again later."). Translate each value idiomatically for it/es/fr/pt/zh/de (English shown is canonical; keep the {country}/{delta}/{count} tokens intact and the delta→label mapping from D-02). Match the tone of the existing verdict/advisory copy (warm, plain, non-alarming per UI-SPEC).
  </action>
  <acceptance_criteria>
    - `grep -c "country.pillar.sentiment'" src/i18n/ui.ts` returns 7
    - `grep -c "sentiment.level_way_high'" src/i18n/ui.ts` returns 7 AND same for level_high, level_right, level_low, level_way_low
    - `grep -c "sentiment.question'" src/i18n/ui.ts` returns 7 and each contains the `{country}` token
    - `grep -c "sentiment.correction_label'" src/i18n/ui.ts` returns 7 and each contains `{delta}`; `sentiment.vote_count'` returns 7 and each contains `{count}`
    - No sentiment.* key appears a number of times other than 7
  </acceptance_criteria>
  <verify>
    <automated>for k in "country.pillar.sentiment" "sentiment.badge" "sentiment.disclaimer" "sentiment.correction_label" "sentiment.vote_count" "sentiment.question" "sentiment.level_way_high" "sentiment.level_high" "sentiment.level_right" "sentiment.level_low" "sentiment.level_way_low" "sentiment.submit" "sentiment.empty_title" "sentiment.empty_body" "sentiment.thanks_title" "sentiment.thanks_body" "sentiment.error"; do n=$(grep -c "'$k'" src/i18n/ui.ts); if [ "$n" -ne 7 ]; then echo "FAIL $k=$n"; exit 1; fi; done; echo PASS-ALL-7</automated>
  </verify>
  <done>All sentiment widget/pillar strings exist in all 7 locales with correct interpolation tokens and delta mapping.</done>
</task>

<task type="auto">
  <name>Task 2: Add methodology.sentiment_* and legal.privacy_votes_* keys to all 7 locale blocks</name>
  <files>src/i18n/ui.ts</files>
  <read_first>
    - src/i18n/ui.ts (locate existing methodology.* and legal.privacy_* key clusters in each of the 7 blocks — the new keys sit alongside them)
    - .planning/phases/39-.../39-UI-SPEC.md §"Methodology page section" + §"Privacy page addition"
    - .planning/phases/39-.../39-CONTEXT.md D-19 (does NOT affect total) + D-20 (salted hash, no raw IP, localStorage note)
  </read_first>
  <action>
    In EACH of the 7 `ui` blocks add: `methodology.sentiment_title` ("Community Sentiment") and `methodology.sentiment_text` — a paragraph covering how community calibration works, the 5 levels (D-02), the ±1.0-point cap (D-08), and an explicit statement that in Phase 1 it does NOT affect the total safety score (D-19). Also add `legal.privacy_votes_title` ("Community votes") and `legal.privacy_votes_text` — a short paragraph covering salted-hash dedupe with no raw IP retention, Turnstile only when enabled, and the localStorage soft-dedupe note kept honest against the zero-cookie claim (D-20). Translate idiomatically per locale; keep the "does NOT affect the total" statement explicit in every language.
  </action>
  <acceptance_criteria>
    - `grep -c "methodology.sentiment_title'" src/i18n/ui.ts` == 7 and `methodology.sentiment_text'` == 7
    - `grep -c "legal.privacy_votes_title'" src/i18n/ui.ts` == 7 and `legal.privacy_votes_text'` == 7
    - The en methodology.sentiment_text explicitly contains a "does not affect" style clause about the total score
  </acceptance_criteria>
  <verify>
    <automated>for k in "methodology.sentiment_title" "methodology.sentiment_text" "legal.privacy_votes_title" "legal.privacy_votes_text"; do n=$(grep -c "'$k'" src/i18n/ui.ts); if [ "$n" -ne 7 ]; then echo "FAIL $k=$n"; exit 1; fi; done; echo PASS-ALL-7</automated>
  </verify>
  <done>Methodology + privacy strings for the Sentiment feature exist in all 7 locales; the "does not affect the total" statement is present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static i18n strings → rendered HTML | All values are author-controlled build-time constants; no user input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-11 | Information Disclosure | Misleading privacy copy | mitigate | legal.privacy_votes_text must be honest about salted-hash dedupe, no raw IP, and localStorage vs the zero-cookie claim (D-20) — reviewed against CONTEXT decisions |
| T-39-12 | Tampering | Locale key drift (missing key ⇒ fallthrough) | mitigate | Automated grep gates assert every key count == 7 across locales |
| T-39-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed; no install task |
</threat_model>

<verification>
- Both grep parity gates return PASS-ALL-7.
- `npx astro check` shows no new type errors from ui.ts (keys are plain string literals).
</verification>

<success_criteria>
- All Sentiment-related i18n keys exist in all 7 locales with correct interpolation tokens (D-05).
- Calibration labels map to the D-02 signed deltas; methodology states no total-score impact (D-19); privacy copy is honest (D-20).
</success_criteria>

<output>
Create `.planning/phases/39-community-sentiment-score-phase-1-display-only-5-level-calib/39-03-SUMMARY.md` when done.
</output>
