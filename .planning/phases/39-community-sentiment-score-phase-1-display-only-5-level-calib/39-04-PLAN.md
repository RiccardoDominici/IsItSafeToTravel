---
phase: 39
plan: 04
name: Daily pipeline sentiment stage — D1 fetch, snapshot/history bake, run.ts Stage 6, workflow wiring
type: execute
wave: 2
depends_on:
  - "39-02"
files_modified:
  - src/pipeline/sentiment/fetch-votes.ts
  - src/pipeline/sentiment/snapshot.ts
  - src/pipeline/run.ts
  - .github/workflows/data-pipeline.yml
  - src/pipeline/sentiment/__tests__/fetch-votes.test.ts
autonomous: true
requirements:
  - D-10
  - D-13
  - D-14
  - D-16
must_haves:
  truths:
    - "The daily pipeline runs a Stage 6 that reads votes from D1 over the HTTP query API, aggregates them, and writes data/sentiment/latest.json + data/sentiment/{date}.json + data/sentiment/history-index.json (D-10, D-13, D-16)"
    - "If the D1 API is unreachable or the CF token/env is missing, the pipeline completes normally and the last-known sentiment file is preserved (never overwritten with empty) (D-14)"
    - "On a first-ever run with no prior file, an empty-but-valid latest.json is written so the build always resolves"
    - "Sentiment history is written to its own small data/sentiment/history-index.json — NEVER appended to data/scores/history-index.json"
    - "The daily workflow commits data/sentiment/ and passes CLOUDFLARE_ACCOUNT_ID + CLOUDFLARE_API_TOKEN to the pipeline step"
    - "A pipeline run with sentiment disabled/failing does not change the overall PipelineResult.success flag (D-14)"
  artifacts:
    - path: "src/pipeline/sentiment/fetch-votes.ts"
      provides: "D1 HTTP query client returning {ok, rows}; never throws"
      exports: ["fetchVotes", "SENTIMENT_D1_DATABASE_ID"]
    - path: "src/pipeline/sentiment/snapshot.ts"
      provides: "writeSentimentSnapshot + writeSentimentHistoryIndex + getSentimentDir"
      exports: ["writeSentimentSnapshot", "writeSentimentHistoryIndex", "getSentimentDir"]
    - path: "src/pipeline/run.ts"
      provides: "Stage 6 sentiment aggregation, wrapped so it never fails the run"
      contains: "Stage 6"
    - path: ".github/workflows/data-pipeline.yml"
      provides: "CF env + git add data/sentiment/"
      contains: "data/sentiment"
    - path: "src/pipeline/sentiment/__tests__/fetch-votes.test.ts"
      provides: "Graceful-degradation test (missing env / thrown fetch ⇒ {ok:false, rows:[]})"
      contains: "ok"
  key_links:
    - from: "src/pipeline/run.ts Stage 6"
      to: "aggregateVotes + writeSentimentSnapshot"
      via: "try/catch that only warns on failure"
      pattern: "aggregateVotes|writeSentimentSnapshot"
    - from: "src/pipeline/sentiment/fetch-votes.ts"
      to: "api.cloudflare.com/client/v4/.../d1/database/.../query"
      via: "fetch POST with Bearer token"
      pattern: "d1/database"
---

<objective>
Wire the aggregation core (from 39-02) into the existing daily pipeline as "Stage 6: Sentiment": pull windowed vote rows from D1 over the HTTP query API (D-13), aggregate with `aggregateVotes`, and bake `data/sentiment/{latest,{date},history-index}.json` — a NEW small directory kept off the 73 MB `data/scores/history-index.json` (D-10, Pitfall 3). Every failure path degrades gracefully so the daily run and the site build are never broken (D-14). Extend `data-pipeline.yml` to pass the CF credentials and commit the new data.

Purpose: Turns raw votes into the static artifact the country page reads at build time, on the existing 06:00 UTC cadence.
Output: `src/pipeline/sentiment/fetch-votes.ts`, `src/pipeline/sentiment/snapshot.ts`, edits to `src/pipeline/run.ts` + `.github/workflows/data-pipeline.yml`, and a degradation test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/39-community-sentiment-score-phase-1-display-only-5-level-calib/39-CONTEXT.md
@.planning/phases/39-community-sentiment-score-phase-1-display-only-5-level-calib/39-RESEARCH.md
@.planning/phases/39-community-sentiment-score-phase-1-display-only-5-level-calib/39-PATTERNS.md
@.planning/phases/39-community-sentiment-score-phase-1-display-only-5-level-calib/39-02-SUMMARY.md

<interfaces>
<!-- Consumed from 39-02 (src/pipeline/sentiment/aggregate.ts + src/pipeline/types.ts) -->
```typescript
import { aggregateVotes, SENTIMENT_HALF_LIFE_DAYS } from './aggregate.js';
import type { VoteRow, SentimentSnapshot } from '../types.js';
// aggregateVotes(rows: VoteRow[], officialScores: Map<string,number>, nowMs: number): SentimentSnapshot

// fetch-votes.ts contract:
export const SENTIMENT_D1_DATABASE_ID = '83acaffe-32ff-43fc-b68f-5343d01000d5';
export async function fetchVotes(sinceEpochSeconds: number): Promise<{ ok: boolean; rows: VoteRow[] }>;
```
</interfaces>

Analogs (READ): `src/pipeline/fetchers/gdacs.ts` (fetch + try/catch/degrade shape, returns success flag), `src/pipeline/scoring/snapshot.ts` (writeSnapshot dated+latest pattern), `src/pipeline/scoring/history.ts` (writeHistoryIndex consolidation — but write a SEPARATE small file, NOT history-index.json), `src/pipeline/utils/fs.ts` (writeJson/readJson/existsSync-style helpers; add a `getSentimentDir()` returning `join(process.cwd(),'data','sentiment')`), `src/pipeline/run.ts` (Stage 4/5 registration style, lines 80-90; overall success = `sourcesSucceeded > 0`). D1 HTTP query call shape is in RESEARCH §"Pattern 2". Windowed WHERE (Pitfall 8): default window 180 days.
Env read by the pipeline (already repo secrets used by deploy.yml): `process.env.CLOUDFLARE_ACCOUNT_ID`, `process.env.CLOUDFLARE_API_TOKEN`. The token likely lacks D1 read until a manual grant (deferred) — so `fetchVotes` MUST return `{ok:false, rows:[]}` on auth failure, not throw.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fetch-votes.ts — D1 HTTP query client that never throws + degradation test</name>
  <files>src/pipeline/sentiment/fetch-votes.ts, src/pipeline/sentiment/__tests__/fetch-votes.test.ts</files>
  <read_first>
    - src/pipeline/fetchers/gdacs.ts (try/catch/degrade; AbortSignal.timeout usage)
    - .planning/phases/39-.../39-RESEARCH.md §"Pattern 2" (exact D1 query POST + response shape `{result:[{results:[...]}]}`)
    - src/pipeline/sentiment/aggregate.ts (VoteRow shape it must return)
  </read_first>
  <behavior>
    - Missing CLOUDFLARE_ACCOUNT_ID or CLOUDFLARE_API_TOKEN ⇒ returns {ok:false, rows:[]} without calling fetch.
    - fetch throws / non-2xx / malformed JSON ⇒ returns {ok:false, rows:[]} (logs one warning).
    - Successful response ⇒ returns {ok:true, rows} mapping D1 result rows to VoteRow{iso3, delta, created_at}.
    - Successful response with zero rows ⇒ {ok:true, rows:[]} (distinct from ok:false).
  </behavior>
  <action>
    Create `src/pipeline/sentiment/fetch-votes.ts` exporting `SENTIMENT_D1_DATABASE_ID` (constant `'83acaffe-32ff-43fc-b68f-5343d01000d5'`) and `async fetchVotes(sinceEpochSeconds)`. Read account id + token from `process.env`; if either is missing, log `[Sentiment] CF credentials absent — skipping D1 read` and return `{ok:false, rows:[]}`. Otherwise POST to `https://api.cloudflare.com/client/v4/accounts/${accountId}/d1/database/${SENTIMENT_D1_DATABASE_ID}/query` with `Authorization: Bearer ${token}`, JSON body `{ sql: 'SELECT iso3, delta, created_at FROM votes WHERE created_at > ? ORDER BY created_at DESC;', params: [sinceEpochSeconds] }`, and `AbortSignal.timeout(30000)`. Parse `json.result?.[0]?.results ?? []` into VoteRow[]. Wrap the whole network path in try/catch returning `{ok:false, rows:[]}` on any error. Never throw (D-14). Write `fetch-votes.test.ts` (node:test) stubbing `globalThis.fetch` to cover the four <behavior> cases and asserting env-absent short-circuits without a fetch call (temporarily unset the two env vars in the test).
  </action>
  <acceptance_criteria>
    - `node --import tsx --test src/pipeline/sentiment/__tests__/fetch-votes.test.ts` exits 0
    - With env vars unset, fetchVotes resolves {ok:false, rows:[]} and does NOT invoke fetch
    - With fetch stubbed to reject, fetchVotes resolves {ok:false, rows:[]} (no throw)
    - With fetch stubbed to a valid D1 payload, rows map to {iso3, delta, created_at}
    - `grep -q "83acaffe-32ff-43fc-b68f-5343d01000d5" src/pipeline/sentiment/fetch-votes.ts`
  </acceptance_criteria>
  <verify>
    <automated>node --import tsx --test src/pipeline/sentiment/__tests__/fetch-votes.test.ts && grep -q "d1/database" src/pipeline/sentiment/fetch-votes.ts && echo PASS</automated>
  </verify>
  <done>fetchVotes reads D1 over HTTP and degrades to {ok:false, rows:[]} on any missing-env/network/parse failure, covered by tests.</done>
</task>

<task type="auto">
  <name>Task 2: snapshot.ts (write latest/date/history) + run.ts Stage 6 (never fails the run)</name>
  <files>src/pipeline/sentiment/snapshot.ts, src/pipeline/run.ts</files>
  <read_first>
    - src/pipeline/scoring/snapshot.ts (writeSnapshot: dated + latest.json pattern)
    - src/pipeline/scoring/history.ts (writeHistoryIndex consolidation shape — replicate for a SEPARATE small file)
    - src/pipeline/utils/fs.ts (writeJson/readJson; add getSentimentDir here or in snapshot.ts)
    - src/pipeline/run.ts (Stage 4/5 at lines 80-90; success flag at 92-97 must stay `sourcesSucceeded > 0`)
    - .planning/phases/39-.../39-RESEARCH.md §"Pitfall 3" (history file must be separate) + §"Pitfall 4" (keep last-known, ship empty valid on first run)
  </read_first>
  <action>
    Create `src/pipeline/sentiment/snapshot.ts` exporting `getSentimentDir()` (`join(process.cwd(),'data','sentiment')`), `writeSentimentSnapshot(date, snapshot: SentimentSnapshot)` (writeJson to `data/sentiment/${date}.json` AND `data/sentiment/latest.json`), and `writeSentimentHistoryIndex()` (iterate the dated `data/sentiment/YYYY-MM-DD.json` files — mirror scoring/history.ts's listing/consolidation — and write a compact `data/sentiment/history-index.json` of `{ [iso3]: [{date, correction, count}] }`; this is a SMALL file — never touch `data/scores/history-index.json`). Edit `src/pipeline/run.ts`: after Stage 5 (`writeHistoryIndex()`), add a "Stage 6: Sentiment" block wrapped in try/catch. Inside: compute `cutoff = Math.floor(Date.now()/1000) - 180*86400`; `const { ok, rows } = await fetchVotes(cutoff)`; `const officialScores = new Map(scoredCountries.map(c => [c.iso3, c.score]))`. If `ok` → `writeSentimentSnapshot(date, aggregateVotes(rows, officialScores, Date.now()))` then `writeSentimentHistoryIndex()`. If `!ok` → if `data/sentiment/latest.json` already exists, log a warning and keep it (do NOT overwrite); if it does not exist, write an empty-but-valid snapshot `{ generatedAt: new Date().toISOString(), countries: {} }` so the build resolves (Pitfall 4). The entire Stage 6 catch only `console.warn`s — it must NOT alter the returned `PipelineResult.success` (D-14). Import `aggregateVotes` from `./sentiment/aggregate.js`, `fetchVotes` from `./sentiment/fetch-votes.js`, and the snapshot writers from `./sentiment/snapshot.js` (ESM `.js` extensions per repo convention).
  </action>
  <acceptance_criteria>
    - `src/pipeline/run.ts` contains a "Stage 6" comment/log and calls aggregateVotes + writeSentimentSnapshot inside a try/catch whose catch only warns
    - The Stage 6 block does not modify `sourcesSucceeded` or the returned success expression (still `sourcesSucceeded > 0`)
    - `writeSentimentHistoryIndex` writes `data/sentiment/history-index.json` and references NO path under `data/scores/`
    - `snapshot.ts` writes both `${date}.json` and `latest.json` under getSentimentDir()
    - `npm run test:pipeline` still passes (no scoring regression)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Stage 6" src/pipeline/run.ts && grep -q "aggregateVotes" src/pipeline/run.ts && grep -q "history-index.json" src/pipeline/sentiment/snapshot.ts && ! grep -q "data/scores/history-index" src/pipeline/sentiment/snapshot.ts && npm run test:pipeline && echo PASS</automated>
  </verify>
  <done>run.ts bakes sentiment in Stage 6, writing a separate small history file, and never fails the run on sentiment errors.</done>
</task>

<task type="auto">
  <name>Task 3: data-pipeline.yml — pass CF credentials + commit data/sentiment/</name>
  <files>.github/workflows/data-pipeline.yml</files>
  <read_first>
    - .github/workflows/data-pipeline.yml (the "Run data pipeline" env block at lines 39-44; the "Commit and push data" git add at lines 192-207)
    - .planning/phases/39-.../39-PATTERNS.md §".github/workflows/data-pipeline.yml" (secret passthrough + git add extension)
  </read_first>
  <action>
    In the "Run data pipeline" step's `env:` block, add `CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}` and `CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}` alongside the existing `RELIEFWEB_APPNAME` (these secrets already exist for deploy.yml). Keep `continue-on-error: true`. In the "Commit and push data" step, add `git add data/sentiment/ 2>/dev/null || true` next to the existing `git add data/history/ 2>/dev/null || true` line so the daily-baked sentiment persists to the repo (RESEARCH "Additional required workflow edit"). Do NOT add a public/sentiment.json copy — the country page reads data/sentiment/latest.json at build time via the lib (39-05); Phase 1 needs no client-side sentiment JSON (D-21). Do NOT edit deploy.yml.
  </action>
  <acceptance_criteria>
    - `grep -c "CLOUDFLARE_ACCOUNT_ID" .github/workflows/data-pipeline.yml` >= 1 AND `grep -c "CLOUDFLARE_API_TOKEN" .github/workflows/data-pipeline.yml` >= 1
    - `grep -q "git add data/sentiment/" .github/workflows/data-pipeline.yml`
    - `RELIEFWEB_APPNAME` line is preserved and the step still has `continue-on-error: true`
    - No `public/sentiment.json` reference introduced
  </acceptance_criteria>
  <verify>
    <automated>grep -q "CLOUDFLARE_API_TOKEN" .github/workflows/data-pipeline.yml && grep -q "git add data/sentiment/" .github/workflows/data-pipeline.yml && grep -q "RELIEFWEB_APPNAME" .github/workflows/data-pipeline.yml && echo PASS</automated>
  </verify>
  <done>The daily workflow supplies CF credentials to the pipeline and commits data/sentiment/ on every run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI pipeline → D1 HTTP API | Server-to-server read with a repo-secret Bearer token |
| D1 rows → baked static JSON | User-supplied deltas (already validated at ingestion) become server-computed aggregates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-13 | Information Disclosure | CF token leakage in logs | mitigate | Token read from process.env only; never printed; warnings log no secrets |
| T-39-14 | Denial of Service | Pipeline broken by sentiment failure | mitigate | Stage 6 try/catch only warns; success stays `sourcesSucceeded > 0`; fetchVotes never throws (D-14) |
| T-39-15 | Denial of Service | Free-tier D1 read exhaustion | mitigate | Windowed `WHERE created_at > ?` (180d) + idx_votes_iso3_created; single daily read (D-16, Pitfall 8) |
| T-39-16 | Tampering | Sentiment history bloating scores history | mitigate | Separate `data/sentiment/history-index.json`; snapshot.ts references no data/scores path (Pitfall 3) |
| T-39-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed; no install task |
</threat_model>

<verification>
- `node --import tsx --test src/pipeline/sentiment/__tests__/fetch-votes.test.ts` passes.
- `npm run test:pipeline` still passes.
- Local degradation check (manual/optional): run `npx tsx src/pipeline/run.ts <date>` with CF env unset → completes, Stage 6 logs a skip, an empty-but-valid data/sentiment/latest.json exists, no data/scores/history-index.json change.
</verification>

<success_criteria>
- Stage 6 reads D1, aggregates, and bakes data/sentiment/{latest,date,history-index}.json (D-13, D-10).
- Every sentiment failure degrades gracefully; the run and build never break (D-14).
- The daily workflow commits data/sentiment/ and passes CF credentials (D-13/D-16).
</success_criteria>

<output>
Create `.planning/phases/39-community-sentiment-score-phase-1-display-only-5-level-calib/39-04-SUMMARY.md` when done.
</output>
