---
phase: 39
plan: 02
name: Sentiment types + recency-weighted aggregation math (pure, TDD) + score-invariance guard
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/pipeline/types.ts
  - src/pipeline/sentiment/aggregate.ts
  - src/pipeline/sentiment/__tests__/aggregate.test.ts
  - src/pipeline/sentiment/__tests__/score-invariance.test.ts
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-06
  - D-07
  - D-08
  - D-09
must_haves:
  truths:
    - "Aggregation computes a recency-weighted mean of signed deltas, scales it, and hard-clamps the correction to ±1.0 point"
    - "Perceived value = clamp(officialScore + correction, 1, 10) on the same 1–10 scale as the other pillars"
    - "A country with fewer than SENTIMENT_MIN_VOTES (5) votes is reported as insufficient (no correction/perceived surfaced)"
    - "The Sentiment types are structurally independent of PillarName / ScoredCountry.pillars — sentiment can never enter the scoring geometric mean (D-06)"
    - "The scoring engine still produces exactly 5 pillars per country regardless of any sentiment data"
  artifacts:
    - path: "src/pipeline/types.ts"
      provides: "SentimentEntry + SentimentSnapshot + VoteRow types (sibling to, not part of, PillarScore)"
      contains: "SentimentEntry"
    - path: "src/pipeline/sentiment/aggregate.ts"
      provides: "aggregateVotes() + tunable named constants"
      exports: ["aggregateVotes", "SENTIMENT_HALF_LIFE_DAYS", "SENTIMENT_DELTA_SCALE", "SENTIMENT_CORRECTION_CAP", "SENTIMENT_MIN_VOTES"]
    - path: "src/pipeline/sentiment/__tests__/aggregate.test.ts"
      provides: "Unit tests for weighting, cap, floor, perceived mapping (Wave 0)"
      contains: "SENTIMENT_CORRECTION_CAP"
    - path: "src/pipeline/sentiment/__tests__/score-invariance.test.ts"
      provides: "Guard asserting sentiment never perturbs the 5-pillar scoring"
      contains: "pillars"
  key_links:
    - from: "aggregate.ts aggregateVotes"
      to: "official scores map"
      via: "perceived = clamp(official + correction, 1, 10)"
      pattern: "clamp|Math.min.*Math.max"
    - from: "score-invariance.test.ts"
      to: "computeAllScores"
      via: "asserts pillars.length === 5"
      pattern: "pillars.length"
---

<objective>
Define the Sentiment data model as a structurally-independent sibling type (NEVER appended to `country.pillars` or the closed `PillarName` union — see 39-PATTERNS.md top finding) and implement the pure aggregation math: recency-weighted mean of signed vote deltas → scaled correction → hard ±1.0 cap (D-08), perceived = clamp(official + correction, 1, 10) (D-07), and a vote floor (D-09). Lock the display-only guarantee (D-06) with a test asserting scoring still yields exactly 5 pillars irrespective of sentiment.

Purpose: This is the tunable, testable core the pipeline (39-04) and the display (39-05) both depend on. TDD because every rule is `expect(f(input)).toBe(output)`.
Output: types in `src/pipeline/types.ts`, `src/pipeline/sentiment/aggregate.ts`, and two Wave 0 test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/39-community-sentiment-score-phase-1-display-only-5-level-calib/39-CONTEXT.md
@.planning/phases/39-community-sentiment-score-phase-1-display-only-5-level-calib/39-RESEARCH.md
@.planning/phases/39-community-sentiment-score-phase-1-display-only-5-level-calib/39-PATTERNS.md

<interfaces>
<!-- Add to src/pipeline/types.ts, in their own clearly-commented "Sentiment (display-only, NOT a scoring pillar)" block.
     These MUST NOT reference PillarName or be added to ScoredCountry.pillars. -->
```typescript
export interface VoteRow {
  iso3: string;
  delta: number;        // -2..+2
  created_at: number;   // unix epoch seconds
}

export interface SentimentEntry {
  iso3: string;
  count: number;        // total votes considered
  avgDelta: number;     // recency-weighted mean of deltas (unclamped, for transparency)
  correction: number;   // clamp(avgDelta * SCALE, -CAP, +CAP), score points
  perceived: number;    // clamp(official + correction, 1, 10)
  official: number;     // official score used at aggregation time
}

export interface SentimentSnapshot {
  generatedAt: string;               // ISO 8601
  countries: Record<string, SentimentEntry>;  // keyed by ISO3, only countries with >= 1 vote
}
```
</interfaces>

Analog to mirror: `src/pipeline/scoring/freshness.ts` (`freshnessWeight` — exponential half-life `e^(-ln2 * age/halfLife)`; reuse the same decay shape for recency weighting, computing ageDays from `created_at`). Test idiom: `src/pipeline/scoring/__tests__/engine.test.ts` + `freshness.test.ts`.
Scoring boundary to protect: `src/pipeline/types.ts:42` `PillarName` is a CLOSED union; `computeAllScores` (`src/pipeline/scoring/engine.ts`) iterates only `weights.json` pillars. Do NOT open engine.ts or weights.json.
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Add Sentiment types + implement aggregateVotes with tunable constants (RED→GREEN)</name>
  <files>src/pipeline/types.ts, src/pipeline/sentiment/aggregate.ts, src/pipeline/sentiment/__tests__/aggregate.test.ts</files>
  <read_first>
    - src/pipeline/types.ts (append the Sentiment block; do NOT touch PillarName/PillarScore/ScoredCountry)
    - src/pipeline/scoring/freshness.ts (recency-decay pattern to reuse)
    - src/pipeline/scoring/__tests__/freshness.test.ts (test style for a pure math module)
    - .planning/phases/39-.../39-RESEARCH.md §"Pattern 5" (constants + formula) and §"Pattern 4" (perceived/data model)
  </read_first>
  <behavior>
    - SENTIMENT_HALF_LIFE_DAYS=30, SENTIMENT_DELTA_SCALE=0.5, SENTIMENT_CORRECTION_CAP=1.0, SENTIMENT_MIN_VOTES=5 exported as named constants.
    - weight_i = 0.5 ** (ageDays_i / SENTIMENT_HALF_LIFE_DAYS); avgDelta = Σ(w·delta)/Σw. A vote at now has weight 1; a vote one half-life old has weight 0.5.
    - correction = clamp(avgDelta * SENTIMENT_DELTA_SCALE, -CAP, +CAP). Example: all votes delta=+2, fresh ⇒ avgDelta≈2 ⇒ raw 1.0 ⇒ clamped 1.0 (at cap). delta=-2 fresh ⇒ correction=-1.0.
    - perceived = clamp(official + correction, 1, 10). official=8.3, correction=-0.4 ⇒ 7.9. official=1.2, correction=-1.0 ⇒ 1.0 (floor). official=9.8, correction=+1.0 ⇒ 10 (ceil).
    - A country with count < SENTIMENT_MIN_VOTES is OMITTED from snapshot.countries OR flagged count<5 (choose: OMIT below-floor countries so the display lib treats "absent" == "insufficient", matching D-09/D-14 graceful default). Countries with 0 votes never appear.
    - Empty input ⇒ SentimentSnapshot with countries = {} and a valid generatedAt.
  </behavior>
  <action>
    Append the `<interfaces>` types to `src/pipeline/types.ts` under a comment banner marking them display-only and NOT part of scoring. Create `src/pipeline/sentiment/aggregate.ts` exporting the four constants and `aggregateVotes(rows: VoteRow[], officialScores: Map<string, number>, nowMs: number): SentimentSnapshot`. Group rows by iso3, apply the half-life recency weighting to each row's delta (ageDays = (nowMs/1000 − created_at)/86400, guard negatives → weight 1), compute avgDelta, correction (clamped to ±SENTIMENT_CORRECTION_CAP), and perceived (clamp(official+correction,1,10) using `officialScores.get(iso3)`; if no official score for a country, skip it). Omit any country with count < SENTIMENT_MIN_VOTES. Add a small local `clamp(v, lo, hi)` helper. Write `aggregate.test.ts` FIRST (RED) covering every bullet in <behavior>, then implement until GREEN. Use ESM `.js`-style relative imports for types (`from '../../types.js'`) to match repo convention.
  </action>
  <acceptance_criteria>
    - `node --import tsx --test src/pipeline/sentiment/__tests__/aggregate.test.ts` exits 0
    - Test proves ±1.0 clamp (input forcing raw correction >1 yields exactly 1.0; < -1 yields -1.0)
    - Test proves perceived floor/ceil (official near 1 or 10 with a saturating correction clamps to [1,10])
    - Test proves recency weighting (a fresh +2 outweighs an old -2, moving avgDelta positive)
    - Test proves below-floor omission (a country with 4 votes is absent from countries; 5 votes present)
    - `src/pipeline/types.ts` contains `SentimentEntry` and does NOT add any member to `PillarName`
  </acceptance_criteria>
  <verify>
    <automated>node --import tsx --test src/pipeline/sentiment/__tests__/aggregate.test.ts && grep -q "SentimentEntry" src/pipeline/types.ts && ! grep -E "PillarName =.*sentiment" src/pipeline/types.ts && echo PASS</automated>
  </verify>
  <done>aggregateVotes produces correct recency-weighted, capped, floored, clamped sentiment entries with all rules covered by passing tests; Sentiment types added without touching the closed pillar union.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Score-invariance guard (D-06 — sentiment never perturbs the 5-pillar geometric mean)</name>
  <files>src/pipeline/sentiment/__tests__/score-invariance.test.ts</files>
  <read_first>
    - src/pipeline/scoring/__tests__/engine.test.ts (how computeAllScores is invoked with fixture RawSourceData + WeightsConfig)
    - src/pipeline/config/weights.json (confirm it has exactly 5 pillars)
    - src/pipeline/scoring/engine.ts (computeAllScores signature + that it reads only weightsConfig.pillars)
  </read_first>
  <behavior>
    - weights.json defines exactly 5 pillars (conflict, crime, health, governance, environment).
    - For a representative fixture, every ScoredCountry returned by computeAllScores has pillars.length === 5.
    - No PillarScore in the output has name === 'sentiment' (sentiment is not a PillarName).
  </behavior>
  <action>
    Create `src/pipeline/sentiment/__tests__/score-invariance.test.ts` (node:test) that: (a) loads `src/pipeline/config/weights.json` and asserts `pillars.length === 5` and the pillar names are exactly the closed set; (b) builds a minimal fixture (reuse the shape from engine.test.ts) and asserts every scored country's `pillars.length === 5` and no pillar name equals 'sentiment'. This is the automated proof of D-06 by construction — sentiment lives entirely outside the scoring types, so introducing sentiment data cannot change any country's total `score`.
  </action>
  <acceptance_criteria>
    - `node --import tsx --test src/pipeline/sentiment/__tests__/score-invariance.test.ts` exits 0
    - Test fails if a 6th pillar is ever added to weights.json or a country's pillars array grows beyond 5
    - Test asserts absence of a 'sentiment' pillar name in scoring output
  </acceptance_criteria>
  <verify>
    <automated>node --import tsx --test src/pipeline/sentiment/__tests__/score-invariance.test.ts</automated>
  </verify>
  <done>An automated guard fails the build if sentiment ever leaks into the scoring pillars, locking the display-only guarantee (D-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| D1 vote rows → aggregation | Untrusted user-supplied deltas already validated at ingestion; re-validated implicitly by the {-2..2} math |
| aggregation output → baked static JSON | Server-computed numbers only (no strings from users) cross into the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-08 | Tampering | Manipulated aggregate shown as fact | mitigate | Hard ±1.0 correction cap (SENTIMENT_CORRECTION_CAP) + vote floor (SENTIMENT_MIN_VOTES) bound influence; correction is a nudge, not an override (D-03/D-08) |
| T-39-09 | Tampering | Out-of-range delta reaching math | mitigate | Ingestion validates delta ∈ {-2..2} (39-01); aggregation additionally tolerates any numeric delta without crashing (mean stays finite) |
| T-39-10 | (integrity) | Sentiment leaking into total score | mitigate | Structurally-independent types + score-invariance.test.ts guard (Task 2) — D-06 |
| T-39-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed (RESEARCH audit empty); no install task |
</threat_model>

<verification>
- `node --import tsx --test src/pipeline/sentiment/__tests__/aggregate.test.ts` passes.
- `node --import tsx --test src/pipeline/sentiment/__tests__/score-invariance.test.ts` passes.
- `npm run test:pipeline && npm run test:freshness` still pass (no scoring regression).
</verification>

<success_criteria>
- SentimentEntry/SentimentSnapshot/VoteRow exist as siblings to (not members of) the scoring types (D-06 by construction).
- aggregateVotes implements recency weighting + ±1.0 cap (D-08), perceived mapping (D-07), and the 5-vote floor (D-09), all covered by passing tests.
- Score-invariance guard passes and would fail on any pillar-count regression.
</success_criteria>

<output>
Create `.planning/phases/39-community-sentiment-score-phase-1-display-only-5-level-calib/39-02-SUMMARY.md` when done.
</output>
