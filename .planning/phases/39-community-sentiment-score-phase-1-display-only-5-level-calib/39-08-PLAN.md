---
phase: 39
plan: 08
name: Wire SentimentPillar + SentimentVote into all 7 country pages + phase SEO gate
type: execute
wave: 3
depends_on:
  - "39-05"
  - "39-06"
files_modified:
  - src/pages/en/country/[slug].astro
  - src/pages/it/paese/[slug].astro
  - src/pages/es/pais/[slug].astro
  - src/pages/fr/pays/[slug].astro
  - src/pages/pt/pais/[slug].astro
  - src/pages/zh/country/[slug].astro
  - src/pages/de/land/[slug].astro
autonomous: true
requirements:
  - D-04
  - D-06
  - D-17
  - D-21
  - D-22
must_haves:
  truths:
    - "All 7 country page templates render <SentimentPillar> immediately after <PillarBreakdown>, with <SentimentVote> nested in its slot"
    - "getStaticPaths in each of the 7 templates threads a `sentiment` prop from loadSentimentForCountry(country.iso3)"
    - "The total score / ScoreHero / PillarBreakdown are unchanged — sentiment is a sibling section, not a member of country.pillars (D-06)"
    - "`npx astro build` succeeds for all ~1892 pages and `npm run validate:seo` stays all-pass (country @graph = WebPage+Place+FAQPage+TouristDestination+Dataset intact) (D-22)"
    - "Sentiment appears ONLY on country pages (+ methodology/privacy from 39-07) — no map/comparison/hub/llms/OG changes (D-21)"
  artifacts:
    - path: "src/pages/en/country/[slug].astro"
      provides: "Country template wiring (template for the other 6)"
      contains: "SentimentPillar"
  key_links:
    - from: "country [slug].astro getStaticPaths"
      to: "loadSentimentForCountry (39-05)"
      via: "props.sentiment"
      pattern: "loadSentimentForCountry"
    - from: "<SentimentPillar>"
      to: "<SentimentVote> (slot child)"
      via: "composition in the template"
      pattern: "SentimentVote"
---

<objective>
Compose the two components (39-05 pillar, 39-06 widget) into all 7 localized country page templates: import them + `loadSentimentForCountry`, add a `sentiment` prop to `getStaticPaths` and the `Props` interface, and render `<SentimentPillar sentiment={sentiment} official={country.score} lang={lang}><SentimentVote iso3={country.iso3} officialScore={country.score} countryName={getLocalizedCountryName(country, lang)} lang={lang} /></SentimentPillar>` immediately after `<PillarBreakdown …/>` (D-04/D-17). This is the final wave and carries the phase SEO gate (D-22).

Purpose: Make the display-only 6th pillar + vote widget live on every country page in every language, without perturbing the total score or the SEO @graph.
Output: 7 edited `[slug].astro` templates; a green `astro build` + `validate:seo`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/39-community-sentiment-score-phase-1-display-only-5-level-calib/39-CONTEXT.md
@.planning/phases/39-community-sentiment-score-phase-1-display-only-5-level-calib/39-PATTERNS.md
@.planning/phases/39-community-sentiment-score-phase-1-display-only-5-level-calib/39-05-SUMMARY.md
@.planning/phases/39-community-sentiment-score-phase-1-display-only-5-level-calib/39-06-SUMMARY.md

<interfaces>
<!-- From 39-05 / 39-06 -->
// import SentimentPillar from '../../../components/country/SentimentPillar.astro';
// import SentimentVote from '../../../components/country/SentimentVote.astro';
// import { loadSentimentForCountry } from '../../../lib/sentiment';   // sibling of loadLatestScores
// import type { SentimentEntry } from '../../../pipeline/types';
// getStaticPaths props += sentiment: loadSentimentForCountry(country.iso3)
// Props interface += sentiment: SentimentEntry | null
</interfaces>

All 7 templates are near-identical (differ only in `const lang` and localized dir slug); import depth is uniformly `../../../` (see `src/pages/en/country/[slug].astro:1-31`). Insertion point: right after `<PillarBreakdown pillars={country.pillars} lang={lang} />` (en line 96). getLocalizedCountryName is already imported in each template (from `../../../lib/scores`). 39-PATTERNS.md §"Country page templates ×7" enumerates the exact prop-threading and insertion.
Scope guard (D-21): edit ONLY these 7 files — do NOT touch the map, comparison page, hub/ranking pages, llms generators, or OG scripts.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread the sentiment prop + render the components in all 7 country templates</name>
  <files>src/pages/en/country/[slug].astro, src/pages/it/paese/[slug].astro, src/pages/es/pais/[slug].astro, src/pages/fr/pays/[slug].astro, src/pages/pt/pais/[slug].astro, src/pages/zh/country/[slug].astro, src/pages/de/land/[slug].astro</files>
  <read_first>
    - src/pages/en/country/[slug].astro (full — imports 1-31, getStaticPaths 34-52, Props 54-62, insertion point line 96)
    - src/components/country/SentimentPillar.astro (39-05 — props + slot contract)
    - src/components/country/SentimentVote.astro (39-06 — props contract)
    - 39-PATTERNS.md §"Country page templates ×7" (per-file uniformity note)
  </read_first>
  <action>
    In EACH of the 7 templates: (1) add imports `SentimentPillar` and `SentimentVote` from `../../../components/country/`, `loadSentimentForCountry` from `../../../lib/sentiment`, and the `SentimentEntry` type from `../../../pipeline/types`. (2) In `getStaticPaths`, add `sentiment: loadSentimentForCountry(country.iso3)` to the returned `props` object. (3) Add `sentiment: SentimentEntry | null;` to the `Props` interface and include `sentiment` in the `Astro.props` destructure. (4) Immediately after `<PillarBreakdown pillars={country.pillars} lang={lang} />`, insert `<SentimentPillar sentiment={sentiment} official={country.score} lang={lang}><SentimentVote iso3={country.iso3} officialScore={country.score} countryName={getLocalizedCountryName(country, lang)} lang={lang} /></SentimentPillar>`. Keep each file's existing `const lang` and slug-specific import depths (all are `../../../`). Do NOT modify `country.pillars`, `buildCountryJsonLd`, `buildCountryMetaDescription`, ScoreHero, or any other section (D-06/D-21/D-22). Do NOT edit any file outside these 7.
  </action>
  <acceptance_criteria>
    - All 7 templates import SentimentPillar, SentimentVote, and loadSentimentForCountry
    - All 7 templates have `sentiment: loadSentimentForCountry(` in getStaticPaths and `SentimentPillar` rendered with SentimentVote nested inside it
    - `grep -rl "SentimentPillar" <the 7 country templates>` lists exactly 7
    - No changes to `country.pillars`, `buildCountryJsonLd`, or any component other than the inserted ones (diff limited to imports + getStaticPaths + Props + the one insertion per file)
  </acceptance_criteria>
  <verify>
    <automated>files="src/pages/en/country/[slug].astro src/pages/it/paese/[slug].astro src/pages/es/pais/[slug].astro src/pages/fr/pays/[slug].astro src/pages/pt/pais/[slug].astro src/pages/zh/country/[slug].astro src/pages/de/land/[slug].astro"; np=$(grep -rl "SentimentPillar" $files | wc -l | tr -d ' '); nl=$(grep -rl "loadSentimentForCountry" $files | wc -l | tr -d ' '); test "$np" -eq 7 && test "$nl" -eq 7 && echo PASS || echo "FAIL pillar=$np load=$nl"</automated>
  </verify>
  <done>All 7 country templates render the display-only Sentiment pillar with the nested vote widget, fed by a graceful sentiment prop, without touching the scoring or SEO paths.</done>
</task>

<task type="auto">
  <name>Task 2: Phase build + SEO gate (astro build all pages, validate:seo all-pass)</name>
  <files>src/pages/en/country/[slug].astro</files>
  <read_first>
    - CLAUDE.md (build guidance: prefer `npx astro build` + `npm run validate:seo` when iterating; full `npm run build` is ~400s due to OG)
    - scripts/validate-seo.ts (the invariant it asserts: country @graph = WebPage+Place+FAQPage+TouristDestination+Dataset + description uniqueness)
  </read_first>
  <action>
    Run `npx astro build` (OG/llms already on disk, so skip the slow generate steps) to render all locales including the new Sentiment sections, then `npm run validate:seo`. Confirm the build succeeds for the full page count (~1892) and validate:seo reports the SAME all-pass count as before this phase (no new failures) — D-22. If validate:seo reports any failure, it means the Sentiment markup perturbed the @graph or a meta description; fix by ensuring SentimentPillar/SentimentVote emit no JSON-LD and no duplicate descriptions (they should not), then re-run. Do not commit until both are green. This task adds no new files — it is the phase acceptance gate.
  </action>
  <acceptance_criteria>
    - `npx astro build` exits 0 and reports building the full country-page set across 7 locales
    - `npm run validate:seo` exits 0 with all checks passing (no regression vs the pre-phase count)
    - No sentiment JSON-LD appears in built country pages (grep the dist output for the pillar section without an ld+json block)
  </acceptance_criteria>
  <verify>
    <automated>npx astro build && npm run validate:seo</automated>
  </verify>
  <done>The full site builds and validate:seo is all-pass with the Sentiment feature live (D-22).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| baked sentiment data → SSG country pages | Server-computed numeric aggregates render at build; no runtime user input on these pages |
| new markup → country @graph | Must not alter the asserted JSON-LD types (D-22) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-26 | (integrity) | @graph/description regression breaking SEO | mitigate | Components emit no JSON-LD; buildCountryJsonLd untouched; Task 2 validate:seo gate blocks commit on any failure (D-22) |
| T-39-27 | (integrity) | Sentiment leaking into total score via template | mitigate | Only a sibling section added; country.pillars/ScoreHero untouched; score-invariance guard (39-02) + diff scope (D-06) |
| T-39-28 | (scope) | Sentiment bleeding into map/comparison/hub/llms/OG | mitigate | Edits limited to the 7 country templates; no generator/other-page files touched (D-21) |
| T-39-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed; no install task |
</threat_model>

<verification>
- Task 1 grep gate returns 7/7.
- `npx astro build` succeeds; `npm run validate:seo` all-pass (D-22).
- Manual (post-deploy): the Sentiment card + widget appear on a country page in each locale; the total score is visually unchanged.
</verification>

<success_criteria>
- Display-only Sentiment pillar + vote widget live on all 7 locale country pages (D-04/D-17).
- Total score unchanged; sentiment is a sibling section, not a scoring pillar (D-06).
- validate:seo all-pass; scope contained to country + methodology + privacy (D-21/D-22).
</success_criteria>

<output>
Create `.planning/phases/39-community-sentiment-score-phase-1-display-only-5-level-calib/39-08-SUMMARY.md` when done.
</output>
