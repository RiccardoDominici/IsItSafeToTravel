---
phase: 32-tier-3-complex-sources-batch-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline/fetchers/advisories.ts
  - src/pipeline/types.ts
  - src/pipeline/normalize/advisory-levels.ts
  - src/pipeline/config/sources.json
autonomous: true
requirements: [CPLX-01, CPLX-02, CPLX-03, CPLX-04, CPLX-05, CPLX-06]

must_haves:
  truths:
    - "AdvisoryInfoMap accepts it/es/kr/tw/cn/in keys without TypeScript errors"
    - "ScoredCountry.advisories accepts it/es/kr/tw/cn/in keys without TypeScript errors"
    - "Six normalization functions exist for Italian, Spanish, Korean, Taiwanese, Chinese, Indian text"
    - "sources.json has advisories_tier3a entry"
  artifacts:
    - path: "src/pipeline/fetchers/advisories.ts"
      provides: "AdvisoryInfoMap with tier3a source keys"
      contains: "in?: AdvisoryInfo"
    - path: "src/pipeline/types.ts"
      provides: "ScoredCountry.advisories with tier3a source keys"
      contains: "in?: AdvisoryInfo"
    - path: "src/pipeline/normalize/advisory-levels.ts"
      provides: "6 normalization functions for tier3a sources"
      exports: ["normalizeItLevel", "normalizeEsLevel", "normalizeKrLevel", "normalizeTwLevel", "normalizeCnLevel", "normalizeInLevel"]
    - path: "src/pipeline/config/sources.json"
      provides: "advisories_tier3a source entry"
      contains: "advisories_tier3a"
  key_links:
    - from: "src/pipeline/normalize/advisory-levels.ts"
      to: "advisory-levels consumers"
      via: "named exports"
      pattern: "export function normalize(It|Es|Kr|Tw|Cn|In)"
---

<objective>
Extend types, normalization functions, and config for 6 Tier 3a complex advisory sources (Italy, Spain, South Korea, Taiwan, China, India).

Purpose: Establish the type contracts and normalization logic before creating the fetcher file.
Output: Extended AdvisoryInfoMap/ScoredCountry types, 6 normalize functions, sources.json entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-tier-3-complex-sources-batch-1/32-RESEARCH.md

@src/pipeline/fetchers/advisories.ts
@src/pipeline/types.ts
@src/pipeline/normalize/advisory-levels.ts
@src/pipeline/config/sources.json

<interfaces>
<!-- Existing type contract to extend -->

From src/pipeline/fetchers/advisories.ts (lines 14-41):
```typescript
export type AdvisoryInfoMap = Record<string, {
  us?: AdvisoryInfo;
  uk?: AdvisoryInfo;
  // ... existing keys through ar? ...
}>;
```
Must add: it?, es?, kr?, tw?, cn?, in?

From src/pipeline/types.ts (lines 87-114):
```typescript
advisories: {
  us?: AdvisoryInfo;
  // ... existing keys through ar? ...
};
```
Must add same 6 keys.

From src/pipeline/normalize/advisory-levels.ts:
```typescript
export type UnifiedLevel = 1 | 2 | 3 | 4;
// Pattern: export function normalizeXxLevel(text: string): UnifiedLevel
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend type definitions and sources config</name>
  <files>src/pipeline/fetchers/advisories.ts, src/pipeline/types.ts, src/pipeline/config/sources.json</files>
  <read_first>src/pipeline/fetchers/advisories.ts, src/pipeline/types.ts, src/pipeline/config/sources.json</read_first>
  <action>
1. In `src/pipeline/fetchers/advisories.ts`, add 6 new optional keys to the AdvisoryInfoMap type after the `// Tier 2b` block. Add a `// Tier 3a` comment and these keys:
   - `it?: AdvisoryInfo;`
   - `es?: AdvisoryInfo;`
   - `kr?: AdvisoryInfo;`
   - `tw?: AdvisoryInfo;`
   - `cn?: AdvisoryInfo;`
   - `in?: AdvisoryInfo;`

2. In `src/pipeline/types.ts`, add the same 6 keys to the `ScoredCountry.advisories` block, after the `// Tier 2b` section, with a `// Tier 3a` comment.

3. In `src/pipeline/config/sources.json`, add an `advisories_tier3a` entry after the `advisories_tier2b` entry, following the same structure:
```json
"advisories_tier3a": {
  "name": "Tier 3a Government Travel Advisories (Complex)",
  "url": "https://www.viaggiaresicuri.it",
  "dataUrl": "https://www.viaggiaresicuri.it",
  "description": "Official travel advisory levels from Italy (Viaggiare Sicuri), Spain (Exteriores), South Korea (MOFA), Taiwan (BOCA), China (MFA), and India (MEA)",
  "updateFrequency": "as issued"
}
```

Also update the `advisories.ts` JSDoc comment for AdvisoryInfoMap to include the new keys.
  </action>
  <verify>
    <automated>cd /Users/riccardo/Developer/VibeCoding/Isitsafetotravel && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "it?: AdvisoryInfo" src/pipeline/fetchers/advisories.ts returns 1
    - grep -c "in?: AdvisoryInfo" src/pipeline/fetchers/advisories.ts returns 1
    - grep -c "it?: AdvisoryInfo" src/pipeline/types.ts returns 1
    - grep -c "in?: AdvisoryInfo" src/pipeline/types.ts returns 1
    - grep -c "advisories_tier3a" src/pipeline/config/sources.json returns at least 1
  </acceptance_criteria>
  <done>AdvisoryInfoMap and ScoredCountry.advisories both have it/es/kr/tw/cn/in keys. sources.json has advisories_tier3a entry. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Add 6 normalization functions for tier3a sources</name>
  <files>src/pipeline/normalize/advisory-levels.ts</files>
  <read_first>src/pipeline/normalize/advisory-levels.ts</read_first>
  <action>
Add 6 new normalization functions at the end of `advisory-levels.ts`, after the tier 2b section, with a `// --- Tier 3a normalization functions ---` comment:

1. `normalizeItLevel(text: string): UnifiedLevel` — Italian text matching:
   - "non recarsi" / "sconsigliato" -> 4
   - "sconsigliati i viaggi" / "evitare" -> 3
   - "cautela" / "attenzione" / "particolare prudenza" -> 2
   - Default -> 1

2. `normalizeEsLevel(text: string): UnifiedLevel` — Spanish text matching:
   - "se desaconseja todo viaje" / "no viajar" -> 4
   - "se desaconseja" / "evitar" -> 3
   - "precaucion" / "prudencia" -> 2
   - Default -> 1

3. `normalizeKrLevel(level: number): UnifiedLevel` — Korea 4-level numeric (1-4 maps directly):
   - Clamp to 1-4 range using Math.min/Math.max (same pattern as normalizeJpLevel)

4. `normalizeTwLevel(text: string): UnifiedLevel` — Taiwan color/text matching:
   - "紅色" / "red" -> 4
   - "橙色" / "orange" -> 3
   - "黃色" / "yellow" -> 2
   - "灰色" / "gray" / default -> 1

5. `normalizeCnLevel(text: string): UnifiedLevel` — Chinese text matching:
   - "暂勿前往" (do not travel) -> 4
   - "谨慎前往" (proceed with caution) -> 3
   - "注意安全" (exercise caution) -> 2
   - Default -> 1

6. `normalizeInLevel(text: string): UnifiedLevel` — Indian/English text matching:
   - "do not travel" / "leave immediately" -> 4
   - "avoid" / "defer" / "reconsider" -> 3
   - "caution" / "exercise" -> 2
   - Default -> 1

Each function should have a JSDoc comment with source name and level system description. Use `.toLowerCase()` on text input for case-insensitive matching.
  </action>
  <verify>
    <automated>cd /Users/riccardo/Developer/VibeCoding/Isitsafetotravel && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "normalizeItLevel" src/pipeline/normalize/advisory-levels.ts returns at least 1
    - grep -c "normalizeEsLevel" src/pipeline/normalize/advisory-levels.ts returns at least 1
    - grep -c "normalizeKrLevel" src/pipeline/normalize/advisory-levels.ts returns at least 1
    - grep -c "normalizeTwLevel" src/pipeline/normalize/advisory-levels.ts returns at least 1
    - grep -c "normalizeCnLevel" src/pipeline/normalize/advisory-levels.ts returns at least 1
    - grep -c "normalizeInLevel" src/pipeline/normalize/advisory-levels.ts returns at least 1
    - grep "暂勿前往" src/pipeline/normalize/advisory-levels.ts returns a match
  </acceptance_criteria>
  <done>Six normalization functions exported from advisory-levels.ts covering Italian, Spanish, Korean (numeric), Taiwanese (color+Chinese), Chinese, and Indian (English) text patterns. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- All 6 new keys exist in both AdvisoryInfoMap and ScoredCountry.advisories
- All 6 normalization functions are exported from advisory-levels.ts
- sources.json has advisories_tier3a entry
</verification>

<success_criteria>
TypeScript compiles cleanly with all new type keys and normalization functions. No runtime code changes yet -- this is the type/config foundation for Plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/32-tier-3-complex-sources-batch-1/32-01-SUMMARY.md`
</output>
