---
phase: 33-tier-3-complex-sources-batch-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline/fetchers/advisories.ts
  - src/pipeline/types.ts
  - src/pipeline/normalize/advisory-levels.ts
  - src/pipeline/config/sources.json
autonomous: true
requirements: [CPLX-07, CPLX-08, CPLX-09, CPLX-10, CPLX-11, CPLX-12, CPLX-13]

must_haves:
  truths:
    - "AdvisoryInfoMap type includes ch, se, no, pl, cz, hu, pt optional keys"
    - "ScoredCountry.advisories type includes ch, se, no, pl, cz, hu, pt optional keys"
    - "7 normalization functions exist for Swiss/Swedish/Norwegian/Polish/Czech/Hungarian/Portuguese text"
    - "sources.json has advisories_tier3b entry"
  artifacts:
    - path: "src/pipeline/fetchers/advisories.ts"
      provides: "AdvisoryInfoMap with tier3b keys"
      contains: "ch?: AdvisoryInfo"
    - path: "src/pipeline/types.ts"
      provides: "ScoredCountry.advisories with tier3b keys"
      contains: "ch?: AdvisoryInfo"
    - path: "src/pipeline/normalize/advisory-levels.ts"
      provides: "7 normalize functions for tier3b sources"
      exports: ["normalizeChLevel", "normalizeSeLevel", "normalizeNoLevel", "normalizePlLevel", "normalizeCzLevel", "normalizeHuLevel", "normalizePtLevel"]
    - path: "src/pipeline/config/sources.json"
      provides: "advisories_tier3b source config"
      contains: "advisories_tier3b"
  key_links:
    - from: "src/pipeline/normalize/advisory-levels.ts"
      to: "advisories-tier3b.ts (plan 02)"
      via: "normalize function imports"
      pattern: "normalizeChLevel|normalizeSeLevel|normalizeNoLevel"
---

<objective>
Extend type definitions, normalization functions, and source configuration to support 7 new Tier 3b advisory sources: Switzerland (EDA), Sweden (UD), Norway (UD), Poland (MSZ), Czech Republic (MZV), Hungary (KKM), and Portugal (MNE).

Purpose: Prepare the type system and normalization layer so Plan 02 can build fetchers against stable contracts.
Output: Extended types, 7 normalization functions, updated sources.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-tier-3-complex-sources-batch-2/33-RESEARCH.md

@src/pipeline/fetchers/advisories.ts
@src/pipeline/types.ts
@src/pipeline/normalize/advisory-levels.ts
@src/pipeline/config/sources.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend types and source config for 7 Tier 3b sources</name>
  <files>src/pipeline/fetchers/advisories.ts, src/pipeline/types.ts, src/pipeline/config/sources.json</files>
  <read_first>src/pipeline/fetchers/advisories.ts, src/pipeline/types.ts, src/pipeline/config/sources.json</read_first>
  <action>
1. In `src/pipeline/fetchers/advisories.ts`, add Tier 3b keys to the `AdvisoryInfoMap` type after the existing Tier 3a block (after `in?: AdvisoryInfo;`). Add a comment `// Tier 3b` and then:
```typescript
// Tier 3b
ch?: AdvisoryInfo;
se?: AdvisoryInfo;
no?: AdvisoryInfo;
pl?: AdvisoryInfo;
cz?: AdvisoryInfo;
hu?: AdvisoryInfo;
pt?: AdvisoryInfo;
```

2. In `src/pipeline/types.ts`, add the same 7 keys to `ScoredCountry.advisories` after the Tier 3a block (after `in?: AdvisoryInfo;`). Add a comment `// Tier 3b` and then the same 7 optional properties.

3. In `src/pipeline/config/sources.json`, add a new entry after `advisories_tier3a`:
```json
"advisories_tier3b": {
  "name": "Tier 3b Government Travel Advisories (Complex)",
  "url": "https://www.eda.admin.ch",
  "dataUrl": "https://www.eda.admin.ch",
  "description": "Official travel advisory levels from Switzerland (EDA), Sweden (UD), Norway (UD), Poland (MSZ), Czech Republic (MZV), Hungary (KKM), and Portugal (MNE)",
  "updateFrequency": "as issued"
}
```
  </action>
  <verify>
    <automated>grep -c "ch?: AdvisoryInfo" src/pipeline/fetchers/advisories.ts src/pipeline/types.ts && grep "advisories_tier3b" src/pipeline/config/sources.json</automated>
  </verify>
  <acceptance_criteria>
    - src/pipeline/fetchers/advisories.ts contains `ch?: AdvisoryInfo;` and `pt?: AdvisoryInfo;` after a `// Tier 3b` comment
    - src/pipeline/types.ts contains `ch?: AdvisoryInfo;` and `pt?: AdvisoryInfo;` after a `// Tier 3b` comment
    - src/pipeline/config/sources.json contains `"advisories_tier3b"` key with `"name": "Tier 3b Government Travel Advisories (Complex)"`
    - All 7 keys (ch, se, no, pl, cz, hu, pt) appear in both type definitions
  </acceptance_criteria>
  <done>AdvisoryInfoMap and ScoredCountry.advisories have 7 new optional tier3b keys; sources.json has advisories_tier3b entry</done>
</task>

<task type="auto">
  <name>Task 2: Add 7 normalization functions for Tier 3b languages</name>
  <files>src/pipeline/normalize/advisory-levels.ts</files>
  <read_first>src/pipeline/normalize/advisory-levels.ts</read_first>
  <action>
Add 7 new exported normalization functions at the end of `src/pipeline/normalize/advisory-levels.ts`, after the existing `normalizeInLevel` function. Add a section comment `// --- Tier 3b normalization functions ---`.

Each function takes a `text: string` parameter and returns `UnifiedLevel`. Include BOTH diacritical and ASCII-folded variants for resilience (per research pitfall #1).

1. **`normalizeChLevel`** (Switzerland - German/English):
   - Level 4: includes 'von reisen wird abgeraten' OR 'grundsätzlich abgeraten' OR 'grundsaetzlich abgeraten' OR 'do not travel'
   - Level 3: includes 'von nicht dringenden reisen' OR 'nicht dringenden reisen wird abgeraten' OR 'avoid non-essential'
   - Level 2: includes 'erhöhte vorsicht' OR 'erhoehte vorsicht' OR 'increased caution'
   - Level 1: default

2. **`normalizeSeLevel`** (Sweden - Swedish):
   - Level 4: includes 'avråder alla resor' OR 'avraader alla resor' OR 'alla resor avrads'
   - Level 3: includes 'avråder resor' OR 'avraader resor' OR 'ud avråder' OR 'ud avraader'
   - Level 2: includes 'iaktta stor försiktighet' OR 'iaktta stor forsiktighet' OR 'skärpt uppmaning' OR 'skaerpt uppmaning'
   - Level 1: default

3. **`normalizeNoLevel`** (Norway - Norwegian):
   - Level 4: includes 'fraråder alle reiser' OR 'fraraader alle reiser' OR 'ikke reis'
   - Level 3: includes 'fraråder reiser' OR 'fraraader reiser' OR 'ikke-nødvendige reiser' OR 'ikke-noedvendige reiser'
   - Level 2: includes 'utvise forsiktighet' OR 'økt aktsomhet' OR 'oekt aktsomhet'
   - Level 1: default

4. **`normalizePlLevel`** (Poland - Polish):
   - Level 4: includes 'nie planuj podróży' OR 'nie planuj podrozy' OR 'zakaz wjazdu'
   - Level 3: includes 'odradza się podróżowanie' OR 'odradza sie podrozowanie' OR 'odradza podróże' OR 'odradza podroze'
   - Level 2: includes 'zachowaj szczególną ostrożność' OR 'zachowaj szczegolna ostroznosc' OR 'zachowaj ostrożność' OR 'zachowaj ostroznosc'
   - Level 1: default

5. **`normalizeCzLevel`** (Czech Republic - Czech):
   - Level 4: includes 'nedoporučujeme cestovat' OR 'nedoporucujeme cestovat' OR 'necestujte'
   - Level 3: includes 'zvažit nezbytnost cesty' OR 'zvazit nezbytnost cesty' OR 'doporučujeme se vyhnout' OR 'doporucujeme se vyhnout'
   - Level 2: includes 'zvýšená opatrnost' OR 'zvysena opatrnost' OR 'dbejte zvýšené opatrnosti' OR 'dbejte zvysene opatrnosti'
   - Level 1: default

6. **`normalizeHuLevel`** (Hungary - Hungarian):
   - Level 4: includes 'ne utazzon' OR 'utazás nem javasolt' OR 'utazas nem javasolt'
   - Level 3: includes 'fokozott előrelátás' OR 'fokozott elore-latas' OR 'kiemelt figyelemmel'
   - Level 2: includes 'fokozott óvatosság' OR 'fokozott ovatossag' OR 'utazás előtt tájékozódjon' OR 'utazas elott tajekodjon'
   - Level 1: default

7. **`normalizePtLevel`** (Portugal - Portuguese):
   - Level 4: includes 'desaconselhada' OR 'não viaje' OR 'nao viaje' OR 'evite todas as viagens'
   - Level 3: includes 'condicionada' OR 'evite viagens não essenciais' OR 'evite viagens nao essenciais'
   - Level 2: includes 'recomenda precaução' OR 'recomenda precaucao' OR 'cuidados especiais'
   - Level 1: default

IMPORTANT: For all functions, use `.toLowerCase()` on the input text before matching. For Chinese/Korean-style direct character matching (like `normalizeCnLevel`), this is NOT needed, but all 7 tier3b sources are Latin-script languages that SHOULD use `.toLowerCase()`.

Check that Level 4 patterns are checked BEFORE Level 3 patterns (e.g., "avråder alla resor" must be checked before "avråder resor" to avoid false match).
  </action>
  <verify>
    <automated>grep -c "export function normalize" src/pipeline/normalize/advisory-levels.ts && npx tsc --noEmit src/pipeline/normalize/advisory-levels.ts 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - advisory-levels.ts contains `export function normalizeChLevel(text: string): UnifiedLevel`
    - advisory-levels.ts contains `export function normalizeSeLevel(text: string): UnifiedLevel`
    - advisory-levels.ts contains `export function normalizeNoLevel(text: string): UnifiedLevel`
    - advisory-levels.ts contains `export function normalizePlLevel(text: string): UnifiedLevel`
    - advisory-levels.ts contains `export function normalizeCzLevel(text: string): UnifiedLevel`
    - advisory-levels.ts contains `export function normalizeHuLevel(text: string): UnifiedLevel`
    - advisory-levels.ts contains `export function normalizePtLevel(text: string): UnifiedLevel`
    - Each function contains `.toLowerCase()` call
    - Each function checks Level 4 before Level 3 (longer/more-specific patterns first)
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>7 normalization functions exported for CH/SE/NO/PL/CZ/HU/PT with diacritical and ASCII-folded variants; TypeScript compiles clean</done>
</task>

</tasks>

<verification>
- `grep -c "ch?: AdvisoryInfo" src/pipeline/fetchers/advisories.ts` returns 1
- `grep -c "pt?: AdvisoryInfo" src/pipeline/types.ts` returns at least 1 (pt for Portugal, plus existing pt for Philippines tier would not conflict since PH uses `ph`)
- `grep "advisories_tier3b" src/pipeline/config/sources.json` shows the entry
- `grep -c "export function normalize" src/pipeline/normalize/advisory-levels.ts` returns at least 20 (13 existing + 7 new)
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
- All 4 files modified with tier3b extensions
- 7 new normalization functions compile and export correctly
- sources.json has advisories_tier3b entry
- No TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/33-tier-3-complex-sources-batch-2/33-01-SUMMARY.md`
</output>
