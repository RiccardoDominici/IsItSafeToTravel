---
phase: 10-country-comparison
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/pages/en/compare.astro
  - src/pages/it/confronta.astro
autonomous: true
requirements: [COMP-03, COMP-04]

must_haves:
  truths:
    - "User sees grouped horizontal bars comparing pillar sub-scores across all selected countries"
    - "User sees an overlay trend chart with historical score lines for all selected countries"
    - "Each country line in the trend chart has a distinct color and dash pattern for colorblind accessibility"
    - "Trend chart has a legend mapping country names to their line colors"
  artifacts:
    - path: "src/pages/en/compare.astro"
      provides: "Complete comparison page with charts"
      contains: "renderPillarBars"
    - path: "src/pages/it/confronta.astro"
      provides: "Italian version with charts"
      contains: "renderPillarBars"
  key_links:
    - from: "renderPillarBars()"
      to: "selectedCountries[].pillars"
      via: "reads pillar data from selected countries"
      pattern: "pillars"
    - from: "renderTrendChart()"
      to: "allHistory[iso3]"
      via: "reads history data keyed by ISO3"
      pattern: "allHistory"
    - from: "compare.astro <script>"
      to: "d3-scale, d3-shape, d3-time-format"
      via: "import for chart rendering"
      pattern: "import.*d3"
---

<objective>
Add pillar comparison bars and overlay trend chart to the comparison page.

Purpose: Users can visually compare category-level scores and historical trends across selected countries. This completes the comparison feature.
Output: Fully interactive comparison page with grouped horizontal bars and multi-line overlay trend chart.
</objective>

<execution_context>
@/Users/riccardo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/riccardo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-country-comparison/10-CONTEXT.md
@.planning/phases/10-country-comparison/10-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — the client-side state and data available in the script -->

State available in compare.astro script (created by Plan 01):
```typescript
let selectedCountries: CountryItem[]; // current selections
const allCountries: CountryItem[];    // all 248 countries with scores + pillars
const allHistory: Record<string, HistoryPoint[]>; // keyed by ISO3
const t: Record<string, string>;      // translation strings
const PALETTE: string[];              // ['#c47a5a', '#4a9e8f', '#d4a03c', '#5b6abf', '#7a9e6a']

interface CountryItem {
  iso3: string; name: string; score: number; scoreDisplay: number;
  pillars: Array<{ name: string; score: number }>; color: string;
}
interface HistoryPoint { date: string; score: number; }
```

DOM containers (created by Plan 01):
- `#pillar-bars` — empty div for grouped horizontal bars
- `#trend-overlay` — empty div for overlay trend chart

Pillar names: 'conflict', 'crime', 'health', 'governance', 'environment'
Translation keys: t.pillars_title, t.trend_title, t.pillar_conflict, t.pillar_crime, t.pillar_health, t.pillar_governance, t.pillar_environment

Existing chart pattern (from TrendChart.astro):
- Uses D3 sub-package imports: scaleLinear, scaleTime, line, timeFormat
- Chart dimensions: width=800, height=240, margin { top: 20, right: 20, bottom: 40, left: 50 }
- Tailwind classes: text-sand-*, font-heading, dark:text-sand-*
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add grouped horizontal pillar bars to comparison page</name>
  <files>src/pages/en/compare.astro, src/pages/it/confronta.astro</files>
  <read_first>src/pages/en/compare.astro, src/components/country/PillarBreakdown.astro, src/pages/it/confronta.astro</read_first>
  <action>
In the `<script>` block of `src/pages/en/compare.astro`, add a `renderPillarBars()` function and call it from `render()`.

**Pillar name mapping** (use the translation strings already embedded in `data-t`):
```typescript
const pillarLabels: Record<string, string> = {
  conflict: t.pillar_conflict,
  crime: t.pillar_crime,
  health: t.pillar_health,
  governance: t.pillar_governance,
  environment: t.pillar_environment,
};
```

**`renderPillarBars()` implementation:**
- Target: `#pillar-bars`
- If selectedCountries.length === 0, clear and return
- Add heading: `<h2 class="text-xl md:text-2xl font-heading font-bold text-sand-800 dark:text-sand-100 mb-4">${t.pillars_title}</h2>`
- For each of the 5 pillars (conflict, crime, health, governance, environment):
  - Render a group with the pillar label on the left
  - For each selected country, render a horizontal bar:
    - Bar width proportional to `pillar.score` (0-1 scale, mapped to percentage of container)
    - Bar color = PALETTE[index of country in selectedCountries]
    - Country name label to the right of the bar
    - Score value at the end: `(pillar.score * 10).toFixed(1)/10`

**HTML structure for each pillar group:**
```html
<div class="mb-5">
  <h3 class="text-sm font-medium text-sand-600 dark:text-sand-400 mb-2">{pillarLabel}</h3>
  <div class="space-y-1.5">
    <!-- For each selected country: -->
    <div class="flex items-center gap-2">
      <span class="w-20 shrink-0 text-xs text-sand-500 dark:text-sand-400 text-right truncate">{countryName}</span>
      <div class="flex-1 h-5 bg-sand-200 dark:bg-sand-700 rounded">
        <div class="h-5 rounded transition-all duration-300" style="width: {score*100}%; background-color: {PALETTE[i]}; min-width: 2px;"></div>
      </div>
      <span class="w-12 shrink-0 text-xs font-medium text-sand-700 dark:text-sand-300 text-right">{displayScore}/10</span>
    </div>
  </div>
</div>
```

Wire `renderPillarBars()` into the existing `render()` function so it is called after `renderScoreCards()`.

Apply the same changes to `src/pages/it/confronta.astro`.
  </action>
  <verify>
    <automated>cd /Users/riccardo/Developer/VibeCoding/Isitsafetotravel && grep -c "renderPillarBars" src/pages/en/compare.astro && grep -c "renderPillarBars" src/pages/it/confronta.astro && grep "pillarLabels" src/pages/en/compare.astro | head -2</automated>
  </verify>
  <acceptance_criteria>
    - grep "renderPillarBars" src/pages/en/compare.astro returns 2+ matches (definition + call from render)
    - grep "renderPillarBars" src/pages/it/confronta.astro returns 2+ matches
    - grep "pillar-bars" src/pages/en/compare.astro returns matches
    - grep "PALETTE\[" src/pages/en/compare.astro returns matches showing palette colors used for bars
  </acceptance_criteria>
  <done>Grouped horizontal bars render for each pillar, showing all selected countries side by side with palette colors and score labels</done>
</task>

<task type="auto">
  <name>Task 2: Add overlay trend chart with D3 and colorblind-accessible line patterns</name>
  <files>src/pages/en/compare.astro, src/pages/it/confronta.astro</files>
  <read_first>src/pages/en/compare.astro, src/components/country/TrendChart.astro, src/pages/it/confronta.astro</read_first>
  <action>
In the `<script>` block of `src/pages/en/compare.astro`, add D3 imports and a `renderTrendChart()` function. Call it from `render()`.

**Add D3 imports at top of script:**
```typescript
import { scaleLinear, scaleTime, line as d3Line, timeFormat } from 'd3';
```

**Dash patterns for colorblind accessibility:**
```typescript
const DASH_PATTERNS = ['', '8 4', '4 4', '12 4 4 4', '16 4']; // solid, dashed, dotted, dash-dot, long-dash
```

**`renderTrendChart()` implementation:**
- Target: `#trend-overlay`
- If selectedCountries.length === 0, clear and return
- Check if any selected country has history data; if none have data, show accumulating message and return

**Chart dimensions** (match TrendChart.astro pattern):
```typescript
const width = 800;
const height = 300; // slightly taller for multi-line
const margin = { top: 20, right: 120, bottom: 40, left: 50 }; // extra right margin for legend
const innerWidth = width - margin.left - margin.right;
const innerHeight = height - margin.top - margin.bottom;
```

**Build scales:**
- Collect all dates from all selected countries' history arrays. Create Date objects.
- X scale: scaleTime() with domain [earliest date, latest date], range [0, innerWidth]
- Y scale: scaleLinear() with domain [1, 10], range [innerHeight, 0]

**Build SVG string** (innerHTML approach, not D3 DOM manipulation):
```typescript
const lineGen = d3Line<HistoryPoint>()
  .x(d => x(new Date(d.date)))
  .y(d => y(d.score));
```

SVG structure:
```html
<svg viewBox="0 0 ${width} ${height}" class="w-full text-sand-700 dark:text-sand-300" role="img" aria-label="${t.trend_title}">
  <g transform="translate(${margin.left}, ${margin.top})">
    <!-- Y gridlines at scores 2, 4, 6, 8, 10 -->
    <!-- Y axis labels -->
    <!-- X axis labels (5-6 evenly spaced dates) -->

    <!-- For each selected country, a path: -->
    <path d="${pathData}" fill="none" stroke="${PALETTE[i]}" stroke-width="2.5"
          stroke-dasharray="${DASH_PATTERNS[i]}" stroke-linejoin="round" stroke-linecap="round" />
  </g>

  <!-- Legend (positioned in the right margin area) -->
  <g transform="translate(${margin.left + innerWidth + 15}, ${margin.top})">
    <!-- For each country: -->
    <g transform="translate(0, ${i * 24})">
      <line x1="0" y1="0" x2="20" y2="0" stroke="${PALETTE[i]}" stroke-width="2.5" stroke-dasharray="${DASH_PATTERNS[i]}" />
      <text x="26" y="4" class="text-xs fill-sand-600 dark:fill-sand-400" font-size="11">${country.name}</text>
    </g>
  </g>
</svg>
```

**X axis ticks:**
- Compute 5-6 evenly spaced dates from the combined date range
- Format with `timeFormat('%b %d')`

**Y axis ticks:** scores 2, 4, 6, 8, 10 (same as TrendChart.astro)

**Interactive tooltip** (same nearest-point approach as TrendChart.astro):
- Add a tooltip div: `<div id="trend-compare-tooltip" class="absolute hidden pointer-events-none bg-sand-100 dark:bg-sand-800 border border-terracotta-300 dark:border-terracotta-600 rounded px-2 py-1 text-sm shadow-md z-10 whitespace-nowrap"></div>`
- On mousemove over the SVG: find the nearest date across all lines, show tooltip with all countries' scores at that date: `"Mar 15: Italy 7.2, France 6.8"`
- On mouseleave: hide tooltip
- On touchstart: same nearest-date logic
- Build a unified data structure for tooltip: for each date, collect scores from all countries. Find nearest date to mouse X position.

**Heading:**
```html
<h2 class="text-xl md:text-2xl font-heading font-bold text-sand-800 dark:text-sand-100 mb-4">${t.trend_title}</h2>
```

Wire `renderTrendChart()` into `render()` after `renderPillarBars()`.

Apply the same changes to `src/pages/it/confronta.astro`.
  </action>
  <verify>
    <automated>cd /Users/riccardo/Developer/VibeCoding/Isitsafetotravel && grep -c "renderTrendChart" src/pages/en/compare.astro && grep "d3" src/pages/en/compare.astro | head -3 && grep "DASH_PATTERNS" src/pages/en/compare.astro | head -2 && npx astro build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep "renderTrendChart" src/pages/en/compare.astro returns 2+ matches (definition + call)
    - grep "import.*d3" src/pages/en/compare.astro returns match for D3 imports
    - grep "DASH_PATTERNS" src/pages/en/compare.astro returns match (colorblind accessibility)
    - grep "stroke-dasharray" src/pages/en/compare.astro returns match (dash patterns applied to SVG paths)
    - grep "trend-compare-tooltip" src/pages/en/compare.astro returns match (interactive tooltip)
    - Same patterns present in src/pages/it/confronta.astro
    - npx astro build completes without errors
  </acceptance_criteria>
  <done>Overlay trend chart renders multi-line SVG with D3 scales, each country has a unique color + dash pattern, legend shows country-to-line mapping, tooltip shows all scores at nearest date, colorblind accessible. Build passes.</done>
</task>

</tasks>

<verification>
1. `npx astro build` succeeds without errors
2. Open /en/compare?c=ITA,FRA,DEU — see score cards, pillar bars with 3 bars per pillar, overlay chart with 3 lines
3. Each line in overlay chart has distinct color AND dash pattern
4. Legend shows country names with matching line styles
5. Hovering on chart shows tooltip with date and all countries' scores
6. Pillar bars show country names and scores aligned correctly
</verification>

<success_criteria>
- Grouped horizontal bars render for all 5 pillars when countries are selected
- Overlay trend chart shows historical lines with distinct colors and dash patterns
- Legend correctly maps country names to line styles
- Tooltip shows multi-country scores at nearest date
- Build passes cleanly
- Both English and Italian pages include all chart features
</success_criteria>

<output>
After completion, create `.planning/phases/10-country-comparison/10-02-SUMMARY.md`
</output>
