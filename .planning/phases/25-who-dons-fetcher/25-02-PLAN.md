---
phase: 25-who-dons-fetcher
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - src/pipeline/fetchers/index.ts
  - src/pipeline/scoring/normalize.ts
  - src/pipeline/config/weights.json
autonomous: true
requirements: [SRC-04]

must_haves:
  truths:
    - "WHO DONs fetcher runs as part of fetchAllSources"
    - "who_active_outbreaks values are normalized correctly (inverse: more outbreaks = less safe)"
    - "Health pillar includes who_active_outbreaks with explicit indicatorWeights"
    - "Pipeline compiles and runs end-to-end with 9 data sources"
  artifacts:
    - path: "src/pipeline/fetchers/index.ts"
      provides: "9-source fetchAllSources"
      contains: "fetchWhoDons"
    - path: "src/pipeline/scoring/normalize.ts"
      provides: "who_active_outbreaks normalization range"
      contains: "who_active_outbreaks"
    - path: "src/pipeline/config/weights.json"
      provides: "Health pillar with WHO DONs indicator and indicatorWeights"
      contains: "who_active_outbreaks"
  key_links:
    - from: "src/pipeline/fetchers/index.ts"
      to: "src/pipeline/fetchers/who-dons.ts"
      via: "import fetchWhoDons"
      pattern: "fetchWhoDons"
    - from: "src/pipeline/config/weights.json"
      to: "src/pipeline/scoring/normalize.ts"
      via: "indicator name matching"
      pattern: "who_active_outbreaks"
---

<objective>
Wire the WHO DONs fetcher into the pipeline: register in fetchAllSources, add normalization range, and update health pillar weights with indicatorWeights to control the new indicator's influence.

Purpose: Complete the integration so WHO outbreak data flows into health pillar scoring.
Output: Pipeline runs with 9 sources, health pillar uses 4 indicators with explicit weights.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-who-dons-fetcher/25-CONTEXT.md
@.planning/phases/25-who-dons-fetcher/25-01-SUMMARY.md

<interfaces>
<!-- Current state of files to be modified -->

From src/pipeline/fetchers/index.ts (current 8 sources):
```typescript
import { fetchGdelt } from './gdelt.js';
// ... 8 fetchers in Promise.allSettled
const sourceNames = ['worldbank', 'gpi', 'inform', 'acled', 'advisories', 'reliefweb', 'gdacs', 'gdelt'];
```

From src/pipeline/scoring/normalize.ts (current ranges):
```typescript
const INDICATOR_RANGES: Record<string, { min: number; max: number; inverse: boolean }> = {
  // ... existing ranges, no who_active_outbreaks yet
  gdelt_instability: { min: 0, max: 1.0, inverse: true },
};
```

From src/pipeline/config/weights.json (current health pillar):
```json
{
  "name": "health",
  "weight": 0.20,
  "indicators": ["wb_child_mortality", "inform_health", "inform_epidemic"]
}
// NOTE: health pillar has NO indicatorWeights yet (uses equal averaging)
```

From src/pipeline/config/source-tiers.json (who-dons already configured):
```json
"who-dons": { "tier": "signal", "maxAgeDays": 90, "decayHalfLifeDays": 30 }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register WHO DONs fetcher and add normalization range</name>
  <files>src/pipeline/fetchers/index.ts, src/pipeline/scoring/normalize.ts</files>
  <read_first>
    - src/pipeline/fetchers/index.ts (current imports, exports, fetchAllSources)
    - src/pipeline/scoring/normalize.ts (current INDICATOR_RANGES)
    - src/pipeline/fetchers/who-dons.ts (confirm export name from Plan 01)
  </read_first>
  <action>
    **In src/pipeline/fetchers/index.ts:**
    - Add import: `import { fetchWhoDons } from './who-dons.js';`
    - Add export: `export { fetchWhoDons } from './who-dons.js';`
    - Add `fetchWhoDons(date)` to the Promise.allSettled array (9th entry)
    - Add 'who-dons' to sourceNames array (9th entry)
    - Update JSDoc comment to say "9 sources" and add WHO DONs description line:
      `* - WHO DONs (disease outbreak news) — free, no auth`

    **In src/pipeline/scoring/normalize.ts:**
    - Add to INDICATOR_RANGES:
      ```
      // WHO Disease Outbreak News - active outbreaks (lower = safer)
      who_active_outbreaks: { min: 0, max: 5, inverse: true },
      ```
    - Range 0-5 because most countries have 0 outbreaks, a country with 5+ simultaneous active outbreaks is an extreme situation. Inverse because more outbreaks = less safe.
  </action>
  <verify>
    <automated>cd /Users/riccardo/Developer/VibeCoding/Isitsafetotravel && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - index.ts imports and exports fetchWhoDons
    - index.ts includes fetchWhoDons(date) in Promise.allSettled array
    - index.ts sourceNames includes 'who-dons'
    - normalize.ts has who_active_outbreaks range with inverse: true
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>WHO DONs fetcher registered as 9th source in pipeline, normalization range configured for who_active_outbreaks</done>
</task>

<task type="auto">
  <name>Task 2: Add WHO DONs to health pillar with indicatorWeights</name>
  <files>src/pipeline/config/weights.json</files>
  <read_first>
    - src/pipeline/config/weights.json (current health pillar, other pillars with indicatorWeights for reference)
  </read_first>
  <action>
    **In src/pipeline/config/weights.json:**
    - Bump version to "5.3.0"
    - Add "who_active_outbreaks" to health pillar indicators array
    - Add indicatorWeights to health pillar (it currently uses equal averaging with no explicit weights):
      ```json
      "indicatorWeights": {
        "wb_child_mortality": 0.30,
        "inform_health": 0.25,
        "inform_epidemic": 0.25,
        "who_active_outbreaks": 0.20
      }
      ```
    - Rationale: baseline indicators (wb_child_mortality at 30%) get slightly more weight than signal indicators. INFORM health and epidemic (25% each) are annual baseline indices. WHO DONs (20%) is a realtime signal source -- meaningful but should not dominate the pillar. Weights sum to 1.0.
    - Do NOT modify any other pillar.
  </action>
  <verify>
    <automated>cd /Users/riccardo/Developer/VibeCoding/Isitsafetotravel && node -e "const w=require('./src/pipeline/config/weights.json'); const h=w.pillars.find(p=>p.name==='health'); console.log('version:', w.version); console.log('indicators:', h.indicators); console.log('weights:', h.indicatorWeights); const sum=Object.values(h.indicatorWeights).reduce((a,b)=>a+b,0); console.log('sum:', sum.toFixed(2)); console.log(sum >= 0.99 && sum <= 1.01 ? 'PASS' : 'FAIL: weights do not sum to 1.0')"</automated>
  </verify>
  <acceptance_criteria>
    - weights.json version is "5.3.0"
    - Health pillar indicators array includes "who_active_outbreaks"
    - Health pillar has indicatorWeights with all 4 indicators
    - indicatorWeights sum to 1.0
    - No other pillars are modified
  </acceptance_criteria>
  <done>Health pillar includes who_active_outbreaks at 20% weight, weights v5.3.0, all 4 indicator weights sum to 1.0</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `grep -c "fetchWhoDons" src/pipeline/fetchers/index.ts` returns at least 2 (import + call)
- `grep -c "who_active_outbreaks" src/pipeline/scoring/normalize.ts` returns at least 1
- `grep -c "who_active_outbreaks" src/pipeline/config/weights.json` returns at least 2 (in indicators + indicatorWeights)
- Health pillar indicatorWeights sum to 1.0
- weights.json version is 5.3.0
</verification>

<success_criteria>
Pipeline compiles with 9 registered data sources. WHO DONs outbreak counts flow through normalization (0-5 range, inverse) into the health pillar at 20% indicator weight. Weights version bumped to 5.3.0 with explicit indicatorWeights for the health pillar.
</success_criteria>

<output>
After completion, create `.planning/phases/25-who-dons-fetcher/25-02-SUMMARY.md`
</output>
