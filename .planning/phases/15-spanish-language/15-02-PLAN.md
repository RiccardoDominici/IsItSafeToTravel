---
phase: 15-spanish-language
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - src/pages/es/index.astro
  - src/pages/es/pais/[slug].astro
  - src/pages/es/comparar.astro
  - src/pages/es/seguridad-global.astro
  - src/pages/es/metodologia/index.astro
  - src/pages/es/terminos-legales/index.astro
autonomous: false
requirements: [LANG-01, LANG-02, LANG-03]

must_haves:
  truths:
    - "User can browse all pages under /es/ with fully translated UI text"
    - "All 248 country names display correctly in Spanish on map tooltips, search, and detail pages"
    - "Language switcher includes Spanish and switches to the Spanish version of the current page"
    - "Spanish pages have correct hreflang tags and localized meta descriptions"
  artifacts:
    - path: "src/pages/es/index.astro"
      provides: "Spanish homepage with map and global score banner"
      contains: "lang: Lang = 'es'"
    - path: "src/pages/es/pais/[slug].astro"
      provides: "Spanish country detail pages for all 248 countries"
      contains: "lang: Lang = 'es'"
    - path: "src/pages/es/comparar.astro"
      provides: "Spanish comparison page"
      contains: "lang: Lang = 'es'"
    - path: "src/pages/es/seguridad-global.astro"
      provides: "Spanish global safety page"
      contains: "lang: Lang = 'es'"
    - path: "src/pages/es/metodologia/index.astro"
      provides: "Spanish methodology page"
      contains: "lang: Lang = 'es'"
    - path: "src/pages/es/terminos-legales/index.astro"
      provides: "Spanish legal disclaimer page"
      contains: "lang: Lang = 'es'"
  key_links:
    - from: "src/pages/es/*.astro"
      to: "src/i18n/ui.ts"
      via: "useTranslations('es') for all UI text"
      pattern: "useTranslations.*es"
    - from: "src/pages/es/pais/[slug].astro"
      to: "src/pipeline/config/countries.ts"
      via: "country.name.es for display names"
      pattern: "name\\[lang\\]|name\\.es"
    - from: "src/pages/es/comparar.astro"
      to: "src/i18n/ui.ts"
      via: "Client-side locale detection must handle 'es'"
      pattern: "locale|lang"
---

<objective>
Create all Spanish page files under /es/ by cloning the Italian page structure with Spanish locale.

Purpose: Make the full site browsable in Spanish — completing the LANG-01, LANG-02, LANG-03 requirements.
Output: 6 Astro page files under src/pages/es/ plus a visual verification checkpoint.
</objective>

<execution_context>
@/Users/riccardo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/riccardo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-spanish-language/15-CONTEXT.md
@.planning/phases/15-spanish-language/15-01-SUMMARY.md

<interfaces>
<!-- Page file pattern from Italian locale -->

Every ES page follows the same pattern as IT pages:
1. Frontmatter sets `const lang: Lang = 'es'` (instead of 'it')
2. Uses `const t = useTranslations(lang)` for all text
3. Uses `country.name[lang]` for country names
4. Imports shared components (Base, SafetyMap, GlobalScoreBanner, etc.)
5. Client-side scripts that detect locale must handle 'es' (compare page)

Route slug mapping (from routes.es in ui.ts):
- /es/ = homepage
- /es/pais/[slug] = country detail
- /es/comparar = compare
- /es/seguridad-global = global safety
- /es/metodologia/ = methodology
- /es/terminos-legales/ = legal

IT pages to clone (change only lang constant and route references):
- src/pages/it/index.astro -> src/pages/es/index.astro
- src/pages/it/paese/[slug].astro -> src/pages/es/pais/[slug].astro
- src/pages/it/confronta.astro -> src/pages/es/comparar.astro
- src/pages/it/sicurezza-globale.astro -> src/pages/es/seguridad-global.astro
- src/pages/it/metodologia/index.astro -> src/pages/es/metodologia/index.astro
- src/pages/it/note-legali/index.astro -> src/pages/es/terminos-legales/index.astro
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all Spanish page files</name>
  <files>src/pages/es/index.astro, src/pages/es/pais/[slug].astro, src/pages/es/comparar.astro, src/pages/es/seguridad-global.astro, src/pages/es/metodologia/index.astro, src/pages/es/terminos-legales/index.astro</files>
  <read_first>
    - src/pages/it/index.astro (full file)
    - src/pages/it/paese/[slug].astro (full file)
    - src/pages/it/confronta.astro (full file)
    - src/pages/it/sicurezza-globale.astro (full file)
    - src/pages/it/metodologia/index.astro (full file)
    - src/pages/it/note-legali/index.astro (full file)
  </read_first>
  <action>
For each IT page, create the ES equivalent by:

1. Copy the IT file content exactly
2. Change `const lang: Lang = 'it'` to `const lang: Lang = 'es'`
3. Update any hardcoded IT route references to ES equivalents:
   - `/${lang}/paese/` -> will resolve correctly since lang='es' and routes handle it
   - Check for any hardcoded `/it/` paths and replace with `/${lang}/` pattern
4. In the compare page (`comparar.astro`), find the client-side locale detection. The IT page likely has a ternary like `lang === 'it' ? 'it-IT' : 'en-US'` for date formatting. This should already be fixed to use a locale map in Plan 01, but verify and use the same map pattern.
5. Ensure `getStaticPaths` in `[slug].astro` returns paths for all 248 countries (same logic as IT version).

Create directory structure:
- `src/pages/es/` (root)
- `src/pages/es/pais/` (country detail)
- `src/pages/es/metodologia/` (methodology)
- `src/pages/es/terminos-legales/` (legal)

IMPORTANT: The pages should use `country.name[lang]` (not `country.name.es`) so they work generically with the lang variable. Check that all name references use the bracket notation pattern.
  </action>
  <verify>
    <automated>cd /Users/riccardo/Developer/VibeCoding/Isitsafetotravel && ls src/pages/es/index.astro src/pages/es/pais/\[slug\].astro src/pages/es/comparar.astro src/pages/es/seguridad-global.astro src/pages/es/metodologia/index.astro src/pages/es/terminos-legales/index.astro && grep -l "lang: Lang = 'es'" src/pages/es/*.astro src/pages/es/**/*.astro | wc -l && npx astro check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All 6 ES page files exist in correct directory structure
    - Every ES page has `const lang: Lang = 'es'`
    - No hardcoded '/it/' paths remain in ES pages
    - `npx astro check` passes with no type errors
    - No remaining 2-way locale ternaries in ES pages
  </acceptance_criteria>
  <done>All 6 Spanish page files created, typed correctly, building without errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Spanish pages render correctly</name>
  <files>none — verification only</files>
  <action>
Start the dev server if not running (`npx astro dev`), then present the verification checklist to the user. This is a human visual verification of the complete Spanish locale.
  </action>
  <what-built>Complete Spanish locale: all pages under /es/ with translated UI, 248 country names, language switcher, and SEO metadata.</what-built>
  <how-to-verify>
    1. Run `npx astro dev` (or check if already running)
    2. Visit http://localhost:4321/es/ — verify Spanish homepage with map, translated hero text, Spanish global score banner
    3. Click any country on the map — verify /es/pais/[slug] loads with Spanish country name, translated labels
    4. Visit http://localhost:4321/es/comparar — search for a country, verify Spanish names in search results
    5. Visit http://localhost:4321/es/seguridad-global — verify translated headings and chart
    6. Visit http://localhost:4321/es/metodologia/ — verify translated methodology text and pillar explanations
    7. Visit http://localhost:4321/es/terminos-legales/ — verify translated legal text
    8. Click language switcher — verify it shows English, Italiano, Espanol and switches correctly
    9. View page source on any /es/ page — verify hreflang tags include es, en, it links
  </how-to-verify>
  <verify>User confirms all Spanish pages render correctly with translated content</verify>
  <done>User approves Spanish locale rendering</done>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- `npx astro build` completes successfully (all pages generate)
- `find dist/client/es -name "*.html" | wc -l` shows ~252+ files (248 countries + index + compare + global-safety + methodology + legal)
- `grep -r 'hreflang="es"' dist/client/en/index.html` confirms cross-linking
- Language switcher on /en/ page includes Spanish link to /es/
</verification>

<success_criteria>
- All /es/ pages render with Spanish UI text
- Country names display in Spanish throughout (map, search, detail pages)
- Language switcher includes Spanish and navigates correctly
- hreflang tags on all pages reference all 3 locales
- Site builds successfully with no type or rendering errors
</success_criteria>

<output>
After completion, create `.planning/phases/15-spanish-language/15-02-SUMMARY.md`
</output>
