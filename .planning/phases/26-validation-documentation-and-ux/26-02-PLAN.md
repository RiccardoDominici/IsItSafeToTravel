---
phase: 26-validation-documentation-and-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/i18n/ui.ts
  - src/pages/en/methodology/index.astro
  - src/pages/it/metodologia/index.astro
  - src/pages/es/metodologia/index.astro
  - src/pages/fr/methodologie/index.astro
  - src/pages/pt/metodologia/index.astro
  - README.md
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03, REPO-01, REPO-02]

must_haves:
  truths:
    - "User reads methodology page and finds explanations of baseline+signal formula, every data source, update frequency, and role"
    - "Methodology page available in all 5 languages (EN, IT, ES, FR, PT) with consistent content"
    - "Pillar explanations reflect new realtime indicators (GDELT, ReliefWeb, GDACS, WHO DONs)"
    - "README documents the new data architecture, all 9 sources, tiered scoring, and decay parameters"
  artifacts:
    - path: "src/i18n/ui.ts"
      provides: "Updated translation strings for methodology in all 5 languages"
      contains: "methodology.tiered"
    - path: "src/pages/en/methodology/index.astro"
      provides: "Updated methodology page with tiered formula explanation"
      contains: "source-tiers"
    - path: "README.md"
      provides: "Updated README with v3.0 data architecture"
      contains: "baseline.*signal"
  key_links:
    - from: "src/pages/en/methodology/index.astro"
      to: "src/pipeline/config/weights.json"
      via: "imports weightsConfig for pillar/indicator table generation"
      pattern: "weightsConfig"
    - from: "src/i18n/ui.ts"
      to: "src/pages/*/methodology/index.astro"
      via: "translation keys consumed by methodology pages"
      pattern: "methodology\\."
---

<objective>
Update the methodology page content in all 5 languages to document the v3.0 baseline+signal tiered formula, all 9 data sources with update frequencies, and updated pillar explanations. Update README and add pipeline documentation for the new architecture.

Purpose: Transparency -- users and developers understand how scores are computed with the new formula.
Output: Updated methodology pages in 5 languages, updated README.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pages/en/methodology/index.astro
@src/i18n/ui.ts
@src/pipeline/config/weights.json
@src/pipeline/config/source-tiers.json
@README.md
</context>

<interfaces>
From src/pipeline/config/weights.json (v5.3.0):
- 5 pillars: conflict (30%), crime (25%), health (20%), governance (15%), environment (10%)
- Conflict indicators: wb_political_stability, gpi_overall, gpi_safety_security, gpi_militarisation, acled_fatalities, acled_events, gdelt_instability
- Health indicators: wb_child_mortality, inform_health, inform_epidemic, who_active_outbreaks
- Environment indicators: wb_air_pollution, inform_natural, inform_climate, reliefweb_active_disasters, gdacs_disaster_alerts

From src/pipeline/config/source-tiers.json:
- Baseline sources (70%): worldbank, gpi, inform (annual, 365-day decay)
- Signal sources (max 30%): acled (weekly, 14d decay), advisories (varies, 7d decay), gdelt (daily, 3d decay), reliefweb (daily, 14d decay), gdacs (daily, 7d decay), who-dons (weekly, 30d decay)
- maxSignalInfluence: 0.30, maxDailyScoreChange: 0.3

Translation file: src/i18n/ui.ts has ~1200 lines, with en/it/es/fr/pt language blocks.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update methodology page content and translations in all 5 languages</name>
  <files>src/i18n/ui.ts, src/pages/en/methodology/index.astro</files>
  <read_first>
    - src/i18n/ui.ts (full file -- need to find all methodology.* keys in all 5 language blocks)
    - src/pages/en/methodology/index.astro (current page structure)
    - src/pages/it/metodologia/index.astro (verify Italian page exists and its structure)
    - src/pipeline/config/source-tiers.json
    - src/pipeline/config/weights.json
  </read_first>
  <action>
**Part A: Update translation strings (src/i18n/ui.ts)**

For ALL 5 languages (en, it, es, fr, pt), update and add these methodology translation keys:

1. Update `methodology.overview_text` to explain the tiered baseline+signal architecture: annual indices provide ~70% baseline stability, realtime sources contribute up to 30% based on data freshness with exponential decay.

2. Update `methodology.formula_text` to describe: "Raw indicators are normalized 0-1 (higher=safer). Within each pillar, indicators have explicit sub-weights. The formula blends baseline sources (~70%) with realtime signal sources (up to ~30%, scaled by data freshness). Freshness decay ensures stale data contributes less. Final composite = weighted pillar sum mapped to 1-10."

3. Add new source entries for the 4 new data sources (per DOC-02):
   - `methodology.source.reliefweb` / `_desc` / `_url`: ReliefWeb (OCHA), active humanitarian disasters, https://reliefweb.int
   - `methodology.source.gdacs` / `_desc` / `_url`: GDACS, natural disaster alerts (earthquake/flood/cyclone/volcano), https://www.gdacs.org
   - `methodology.source.gdelt` / `_desc` / `_url`: GDELT Project, media-derived instability signal, https://www.gdeltproject.org
   - `methodology.source.who_dons` / `_desc` / `_url`: WHO Disease Outbreak News, active disease outbreaks, https://www.who.int/emergencies/disease-outbreak-news

4. Add frequency keys: `methodology.frequency.daily` = "Daily" (and equivalents in other languages), `methodology.frequency.realtime` = "Near real-time".

5. Add new indicator translation keys for the new indicators:
   - `methodology.indicator.gdelt_instability`: "GDELT Media Instability Index"
   - `methodology.indicator.reliefweb_active_disasters`: "ReliefWeb Active Humanitarian Disasters"
   - `methodology.indicator.gdacs_disaster_alerts`: "GDACS Natural Disaster Alerts"
   - `methodology.indicator.who_active_outbreaks`: "WHO Active Disease Outbreaks"

6. Add tiered formula section keys:
   - `methodology.tiered_title`: "Baseline + Signal Architecture" (translated in all langs)
   - `methodology.tiered_text`: Explain that baseline sources (World Bank, INFORM, GPI) are updated annually and provide stable foundation. Signal sources (ACLED, GDELT, ReliefWeb, GDACS, WHO DONs, advisories) update daily/weekly and capture emerging crises. Signal influence is capped at 30% and decays with staleness.
   - `methodology.decay_title`: "Data Freshness Decay"
   - `methodology.decay_text`: Explain exponential decay -- each source has a configured half-life. If data is 1 half-life old, it contributes 50% of its weight; 2 half-lives = 25%, etc. Beyond max-age, stale data is dropped entirely.

7. Update pillar description and sources keys for all 5 pillars to mention the new realtime indicators (per DOC-03):
   - Conflict: add mention of GDELT instability signal
   - Health: add mention of WHO DONs outbreak tracking
   - Environment: add mention of ReliefWeb disasters and GDACS alerts

**Part B: Update methodology page template (src/pages/en/methodology/index.astro)**

1. Add the 4 new data sources to the `dataSources` array with correct frequency keys:
   ```
   { key: 'reliefweb', frequency: t('methodology.frequency.daily') },
   { key: 'gdacs', frequency: t('methodology.frequency.daily') },
   { key: 'gdelt', frequency: t('methodology.frequency.daily') },
   { key: 'who_dons', frequency: t('methodology.frequency.weekly') },
   ```

2. Add a new "Baseline + Signal Architecture" section between the existing formula section and the weights table. Use the `methodology.tiered_title` and `methodology.tiered_text` keys.

3. Add a "Data Freshness Decay" section after the tiered architecture section. Use `methodology.decay_title` and `methodology.decay_text`.

4. Update the formula display box to show the tiered formula:
   ```
   Score = (Baseline x 0.70 + Signal x 0.30) where each = (Conflict x 0.30 + Crime x 0.25 + Health x 0.20 + Governance x 0.15 + Environment x 0.10) x 9 + 1
   ```

5. Remove or update the "GPI Pending" section -- GPI is now configured and active in the formula. If GPI data is still unavailable upstream, keep a note but update the text to reflect the current state.

6. Ensure the page still imports weightsConfig and dynamically generates the pillar/indicator tables from it (no hardcoded data).

All other language methodology pages (IT, ES, FR, PT) should follow the same template structure -- check that they also import weightsConfig and use the same dataSources array. If they are copies of the EN template, update them identically.
  </action>
  <verify>
    <automated>npx astro build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
- All 5 methodology pages build without errors
- Each methodology page shows 9 data sources in the table (not just 5)
- Tiered architecture and decay sections are visible
- All new translation keys exist in all 5 languages
  </acceptance_criteria>
  <done>Methodology page in 5 languages documents baseline+signal formula, all 9 sources with frequencies, updated pillar explanations with realtime indicators, freshness decay explanation</done>
</task>

<task type="auto">
  <name>Task 2: Update README and pipeline documentation</name>
  <files>README.md</files>
  <read_first>
    - README.md (current content)
    - src/pipeline/config/source-tiers.json
    - src/pipeline/config/weights.json
  </read_first>
  <action>
Update README.md to document the v3.0 data architecture (per REPO-01, REPO-02):

1. **Data Sources table**: Add 4 new rows:
   - ReliefWeb | Active humanitarian disasters | UN OCHA
   - GDACS | Natural disaster alerts (earthquakes, floods, cyclones, volcanoes) | EU JRC/UN
   - GDELT Project | Media-derived instability signal | GDELT
   - WHO Disease Outbreak News | Active disease outbreaks | World Health Organization

2. **Scoring Methodology section**: Rewrite to explain the tiered architecture:
   - Replace the old weight table with current v5.3.0 weights: Conflict 30%, Crime 25%, Health 20%, Governance 15%, Environment 10%
   - Add subsection "Baseline + Signal Tiering": explain that 9 sources are split into baseline (World Bank, INFORM, GPI at ~70%) and signal (ACLED, advisories, GDELT, ReliefWeb, GDACS, WHO DONs at up to 30%)
   - Add subsection "Freshness Decay": explain exponential decay per source with configurable half-lives (see source-tiers.json)
   - Add subsection "Per-Indicator Sub-Weights": explain that indicators within a pillar are not equally averaged but have explicit sub-weights (see weights.json indicatorWeights)

3. **Project Structure**: Update the `pipeline/` tree description:
   ```
   pipeline/
     fetchers/      One module per data source (9 fetchers)
     scoring/       Tiered baseline+signal engine with freshness decay
     config/        weights.json, source-tiers.json, countries, normalization
   ```

4. **Adding a New Source**: Add a brief section explaining how to add a new data source:
   - Create fetcher in `src/pipeline/fetchers/`
   - Register in `src/pipeline/fetchers/index.ts`
   - Add entry to `source-tiers.json` with tier, maxAgeDays, decayHalfLifeDays
   - Add indicator(s) to relevant pillar in `weights.json`
   - Add normalization ranges in the scoring engine

5. **Multilingual**: Update "English and Italian" to "English, Italian, Spanish, French, and Portuguese".

6. **Features**: Update to mention 9 data sources, tiered scoring, and 14+ years of historical data.
  </action>
  <verify>
    <automated>node -e "const r=require('fs').readFileSync('README.md','utf8'); const checks=['ReliefWeb','GDACS','GDELT','WHO','baseline','signal','source-tiers','decay']; const missing=checks.filter(c=>!r.includes(c)); if(missing.length){console.error('Missing:',missing);process.exit(1)}else{console.log('All v3 topics present in README')}"</automated>
  </verify>
  <acceptance_criteria>
- README mentions all 9 data sources
- README explains baseline+signal tiering with decay
- README explains how to add a new source
- README reflects current v5.3.0 weights
  </acceptance_criteria>
  <done>README documents the complete v3.0 data architecture, source tiers, decay parameters, and how to extend</done>
</task>

</tasks>

<verification>
```bash
npx astro build 2>&1 | tail -5
# All pages build cleanly

node -e "const r=require('fs').readFileSync('README.md','utf8'); console.log('README has baseline+signal:', r.includes('baseline') && r.includes('signal'))"
# true
```
</verification>

<success_criteria>
- Methodology page in all 5 languages explains tiered formula, all 9 sources, decay, and updated pillar indicators
- README documents the new architecture and how to add sources
- Build completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/26-validation-documentation-and-ux/26-02-SUMMARY.md`
</output>
