---
phase: 01-project-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - astro.config.mjs
  - tailwind.config.mjs
  - src/styles/global.css
  - src/layouts/Base.astro
  - src/components/LanguageSwitcher.astro
  - src/components/Header.astro
  - src/components/Footer.astro
  - src/pages/index.astro
  - src/pages/en/index.astro
  - src/pages/it/index.astro
  - src/i18n/ui.ts
  - src/i18n/utils.ts
  - public/fonts/
autonomous: true
requirements:
  - TECH-03
must_haves:
  truths:
    - "Visiting /en/ renders an English placeholder page"
    - "Visiting /it/ renders an Italian placeholder page"
    - "Root URL / detects browser language and redirects to /en/ or /it/"
    - "Each page has hreflang tags linking to its counterpart in the other language"
    - "Language switcher navigates between EN and IT versions of the same page"
    - "Dark mode toggles based on system preference and manual switch"
  artifacts:
    - path: "src/i18n/ui.ts"
      provides: "Translation dictionaries for EN and IT"
      contains: "export const ui"
    - path: "src/i18n/utils.ts"
      provides: "i18n helper functions (getLangFromUrl, useTranslations)"
      exports: ["getLangFromUrl", "useTranslations"]
    - path: "src/layouts/Base.astro"
      provides: "HTML shell with hreflang, meta, dark mode, Tailwind"
      contains: "hreflang"
    - path: "src/pages/en/index.astro"
      provides: "English placeholder homepage"
      min_lines: 10
    - path: "src/pages/it/index.astro"
      provides: "Italian placeholder homepage"
      min_lines: 10
    - path: "src/styles/global.css"
      provides: "Tailwind directives and design tokens"
      contains: "@tailwind"
  key_links:
    - from: "src/pages/en/index.astro"
      to: "src/layouts/Base.astro"
      via: "layout import"
      pattern: "layout.*Base"
    - from: "src/layouts/Base.astro"
      to: "src/i18n/utils.ts"
      via: "i18n helper import"
      pattern: "import.*i18n"
    - from: "src/pages/index.astro"
      to: "/en/ or /it/"
      via: "redirect based on Accept-Language"
      pattern: "redirect"
---

<objective>
Scaffold the Astro project with Tailwind CSS design system, i18n routing for English and Italian, placeholder pages, and hreflang tags.

Purpose: Establishes the site foundation that all subsequent phases build on. The i18n URL structure must be correct from day one because changing it after launch breaks all links and SEO equity (per PITFALLS.md).

Output: A buildable Astro site with /en/ and /it/ locale routes, warm travel-magazine design tokens, dark mode support, and language switching.
</objective>

<execution_context>
@/Users/riccardo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/riccardo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-foundation/01-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Astro project with Tailwind CSS design system</name>
  <files>
    package.json, tsconfig.json, astro.config.mjs, tailwind.config.mjs,
    src/styles/global.css, public/fonts/
  </files>
  <read_first>
    .planning/phases/01-project-foundation/01-CONTEXT.md
    .planning/research/ARCHITECTURE.md (Astro Site Structure section)
  </read_first>
  <action>
    1. Initialize Astro project in the repo root:
       ```
       npm create astro@latest . -- --template minimal --typescript strict --install --no-git
       ```

    2. Install dependencies:
       ```
       npm install @astrojs/tailwind @astrojs/sitemap
       npm install -D tailwindcss @tailwindcss/vite
       ```

    3. Configure `astro.config.mjs`:
       - Add Tailwind integration via @astrojs/tailwind
       - Add sitemap integration via @astrojs/sitemap
       - Set `site: 'https://isitsafetotravel.com'`
       - Set `i18n` config with defaultLocale 'en', locales ['en', 'it'], routing type 'manual' (we handle routing via page files for translated slugs)

    4. Create `src/styles/global.css` with Tailwind directives and custom design tokens:
       ```css
       @import "tailwindcss";

       @theme {
         /* Warm & inviting palette - travel magazine feel */
         --color-sand-50: oklch(0.97 0.01 85);
         --color-sand-100: oklch(0.94 0.02 85);
         --color-sand-200: oklch(0.88 0.03 85);
         --color-sand-300: oklch(0.80 0.04 80);
         --color-sand-400: oklch(0.70 0.05 75);
         --color-sand-500: oklch(0.58 0.05 70);
         --color-sand-600: oklch(0.48 0.05 65);
         --color-sand-700: oklch(0.38 0.04 60);
         --color-sand-800: oklch(0.28 0.03 55);
         --color-sand-900: oklch(0.18 0.02 50);

         /* Earthy greens for safe indicators */
         --color-sage-50: oklch(0.97 0.02 145);
         --color-sage-100: oklch(0.93 0.04 145);
         --color-sage-200: oklch(0.87 0.06 145);
         --color-sage-300: oklch(0.78 0.08 145);
         --color-sage-400: oklch(0.68 0.10 145);
         --color-sage-500: oklch(0.58 0.10 145);
         --color-sage-600: oklch(0.48 0.09 145);

         /* Colorblind-safe map gradient: blue -> yellow -> red */
         --color-safe: oklch(0.65 0.15 250);       /* Blue - safe */
         --color-moderate: oklch(0.85 0.15 90);     /* Yellow - moderate */
         --color-danger: oklch(0.55 0.20 25);       /* Red - danger */

         /* Warm accent */
         --color-terracotta-400: oklch(0.65 0.14 45);
         --color-terracotta-500: oklch(0.55 0.15 40);
         --color-terracotta-600: oklch(0.45 0.14 35);

         /* Typography - humanist sans for warmth */
         --font-sans: 'Inter', 'system-ui', sans-serif;
         --font-heading: 'DM Sans', 'system-ui', sans-serif;

         /* Spacing scale */
         --spacing-header: 4rem;
         --spacing-section: 3rem;
         --spacing-card: 1.5rem;
       }

       /* Dark mode via class strategy (supports system + manual toggle) */
       @media (prefers-color-scheme: dark) {
         :root:not(.light) {
           color-scheme: dark;
         }
       }
       .dark {
         color-scheme: dark;
       }
       ```

    5. Add Google Fonts links for Inter and DM Sans in a note/comment for the Base layout to include (or use @fontsource packages):
       ```
       npm install @fontsource-variable/inter @fontsource-variable/dm-sans
       ```

    6. Verify the project builds:
       ```
       npm run build
       ```
  </action>
  <verify>
    <automated>cd /Users/riccardo/Developer/VibeCoding/Isitsafetotravel && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "astro" in dependencies
    - package.json contains "@astrojs/tailwind" in dependencies
    - astro.config.mjs contains "i18n" and "defaultLocale"
    - astro.config.mjs contains "site:" with "isitsafetotravel.com"
    - src/styles/global.css contains "@import" and "--color-sand-" and "--color-safe" and "--color-danger"
    - src/styles/global.css contains "--font-heading" with "DM Sans"
    - src/styles/global.css contains "prefers-color-scheme: dark"
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>Astro project scaffolded with Tailwind CSS 4, warm design tokens (sand, sage, terracotta palettes), colorblind-safe map gradient (blue/yellow/red in OKLCH), dark mode support, and Inter + DM Sans fonts. Project builds successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Implement i18n routing with placeholder pages, hreflang, and language switcher</name>
  <files>
    src/i18n/ui.ts, src/i18n/utils.ts,
    src/layouts/Base.astro,
    src/components/Header.astro, src/components/Footer.astro, src/components/LanguageSwitcher.astro,
    src/components/DarkModeToggle.astro,
    src/pages/index.astro, src/pages/en/index.astro, src/pages/it/index.astro
  </files>
  <read_first>
    astro.config.mjs (from Task 1)
    .planning/phases/01-project-foundation/01-CONTEXT.md (i18n decisions)
    .planning/research/PITFALLS.md (Pitfall 7: i18n SEO)
    .planning/research/ARCHITECTURE.md (Astro Site Structure section)
  </read_first>
  <action>
    1. Create `src/i18n/ui.ts` with translation dictionaries:
       ```typescript
       export const languages = {
         en: 'English',
         it: 'Italiano',
       } as const;

       export const defaultLang = 'en' as const;
       export type Lang = keyof typeof languages;

       export const ui = {
         en: {
           'site.title': 'Is It Safe to Travel?',
           'site.description': 'Check safety scores for 200+ travel destinations worldwide, backed by transparent data from trusted public sources.',
           'nav.home': 'Home',
           'nav.about': 'About',
           'nav.methodology': 'Methodology',
           'hero.title': 'Is your destination safe?',
           'hero.subtitle': 'Real-time safety scores for every country, powered by open data.',
           'placeholder.coming_soon': 'Interactive safety map coming soon.',
           'placeholder.description': 'We are building a transparent, data-driven travel safety platform. Check back soon for safety scores covering 200+ countries.',
           'footer.disclaimer': 'This information is aggregated from public sources for informational purposes only. Always consult your government\'s official travel advisory before traveling.',
           'footer.sources': 'Sources',
           'footer.methodology': 'Methodology',
           'footer.legal': 'Legal',
           'language.switch': 'Switch language',
           'darkmode.toggle': 'Toggle dark mode',
         },
         it: {
           'site.title': 'Si Puo Viaggiare in Sicurezza?',
           'site.description': 'Consulta i punteggi di sicurezza per oltre 200 destinazioni di viaggio nel mondo, basati su dati trasparenti da fonti pubbliche affidabili.',
           'nav.home': 'Home',
           'nav.about': 'Chi Siamo',
           'nav.methodology': 'Metodologia',
           'hero.title': 'La tua destinazione e sicura?',
           'hero.subtitle': 'Punteggi di sicurezza aggiornati per ogni paese, basati su dati aperti.',
           'placeholder.coming_soon': 'Mappa interattiva di sicurezza in arrivo.',
           'placeholder.description': 'Stiamo costruendo una piattaforma di sicurezza dei viaggi trasparente e basata sui dati. Torna presto per i punteggi di sicurezza di oltre 200 paesi.',
           'footer.disclaimer': 'Queste informazioni sono aggregate da fonti pubbliche a solo scopo informativo. Consulta sempre l\'avviso di viaggio ufficiale del tuo governo prima di viaggiare.',
           'footer.sources': 'Fonti',
           'footer.methodology': 'Metodologia',
           'footer.legal': 'Note Legali',
           'language.switch': 'Cambia lingua',
           'darkmode.toggle': 'Cambia tema',
         },
       } as const;

       // Translated route slugs for SEO
       export const routes = {
         en: {
           country: 'country',
           about: 'about',
           methodology: 'methodology',
         },
         it: {
           country: 'paese',
           about: 'chi-siamo',
           methodology: 'metodologia',
         },
       } as const;
       ```

    2. Create `src/i18n/utils.ts` with helper functions:
       ```typescript
       import { ui, defaultLang, type Lang, languages, routes } from './ui';

       export function getLangFromUrl(url: URL): Lang {
         const [, lang] = url.pathname.split('/');
         if (lang in languages) return lang as Lang;
         return defaultLang;
       }

       export function useTranslations(lang: Lang) {
         return function t(key: keyof typeof ui[typeof defaultLang]): string {
           return ui[lang][key] || ui[defaultLang][key];
         };
       }

       export function getRouteFromUrl(url: URL): string {
         const pathname = url.pathname;
         const parts = pathname.split('/').filter(Boolean);
         // Remove the language prefix
         if (parts[0] && parts[0] in languages) {
           parts.shift();
         }
         return '/' + parts.join('/');
       }

       /** Build the equivalent URL for a different locale */
       export function getLocalizedPath(path: string, targetLang: Lang): string {
         const parts = path.split('/').filter(Boolean);
         const currentLang = parts[0] && parts[0] in languages ? parts.shift()! as Lang : defaultLang;

         // Translate known route segments
         let translatedParts = parts.map(part => {
           // Find if this part is a known route slug in the current language
           const currentRoutes = routes[currentLang];
           const targetRoutes = routes[targetLang];
           for (const [key, value] of Object.entries(currentRoutes)) {
             if (value === part) {
               return targetRoutes[key as keyof typeof targetRoutes];
             }
           }
           return part;
         });

         return `/${targetLang}/${translatedParts.join('/')}`;
       }

       export function getAlternateLinks(currentPath: string): Array<{ lang: Lang; href: string }> {
         return (Object.keys(languages) as Lang[]).map(lang => ({
           lang,
           href: getLocalizedPath(currentPath, lang),
         }));
       }

       export { type Lang, languages, defaultLang } from './ui';
       ```

    3. Create `src/layouts/Base.astro`:
       - Accept `lang` prop (type Lang) and optional `title`, `description` overrides
       - Include `<!DOCTYPE html>` with `<html lang={lang} class="">` (class for dark mode toggling)
       - In `<head>`:
         - `<meta charset="utf-8">`
         - `<meta name="viewport" content="width=device-width, initial-scale=1">`
         - `<title>{title} | {t('site.title')}</title>`
         - `<meta name="description" content={description || t('site.description')}>`
         - hreflang `<link>` tags for each locale using `getAlternateLinks()` plus `x-default` pointing to `/en/` equivalent
         - Canonical URL for current page
         - Import global.css
         - Import @fontsource-variable/inter and @fontsource-variable/dm-sans
         - Inline dark mode detection script: reads localStorage for theme preference, falls back to prefers-color-scheme, sets `dark` class on `<html>`
       - In `<body>`: include Header, `<main><slot /></main>`, Footer
       - Body classes: `bg-sand-50 text-sand-900 dark:bg-sand-900 dark:text-sand-50 font-sans`

    4. Create `src/components/Header.astro`:
       - Props: lang (Lang)
       - Contains site name/logo area (text for now: "IsItSafeToTravel" styled with font-heading)
       - Nav links: Home, About, Methodology (using translated route slugs)
       - Includes LanguageSwitcher and DarkModeToggle
       - Responsive: hamburger menu on mobile (details/summary HTML-only pattern, no JS needed)
       - Tailwind classes: `bg-sand-50/80 dark:bg-sand-900/80 backdrop-blur-sm border-b border-sand-200 dark:border-sand-700 sticky top-0 z-50`

    5. Create `src/components/Footer.astro`:
       - Props: lang (Lang)
       - Disclaimer text from translations (visible without scrolling on short pages)
       - Links: Sources, Methodology, Legal
       - Copyright line
       - Tailwind classes: `bg-sand-100 dark:bg-sand-800 border-t border-sand-200 dark:border-sand-700 mt-auto`

    6. Create `src/components/LanguageSwitcher.astro`:
       - Props: lang (Lang), currentPath (string)
       - Renders a link for each language using `getLocalizedPath()`
       - Current language shown as active (bold/underlined)
       - Non-current languages are clickable links
       - Place in header (per Claude's discretion)

    7. Create `src/components/DarkModeToggle.astro`:
       - Button with sun/moon icons (inline SVG, no dependencies)
       - Inline `<script>` that toggles `dark` class on `<html>` and saves to localStorage
       - Accessible: aria-label from translations

    8. Create `src/pages/index.astro` (root redirect):
       - No layout, just a server-side redirect
       - Read `Accept-Language` header from `Astro.request`
       - If it contains 'it', redirect to `/it/`
       - Otherwise redirect to `/en/`
       - Use 302 (temporary) redirect so browsers can re-evaluate language preference

    9. Create `src/pages/en/index.astro`:
       - Uses Base layout with lang="en"
       - Hero section with translated title and subtitle
       - Placeholder box for the future map: a styled div with "Interactive safety map coming soon" message
       - Brief description paragraph about the platform
       - Warm, inviting design using sand/sage/terracotta tokens

    10. Create `src/pages/it/index.astro`:
        - Same structure as en/index.astro but with lang="it"
        - All text from Italian translations
        - Identical layout and styling

    11. Verify build and hreflang output:
        ```
        npm run build
        ```
        Check that dist/en/index.html contains hreflang link tags for both en and it.
  </action>
  <verify>
    <automated>cd /Users/riccardo/Developer/VibeCoding/Isitsafetotravel && npm run build 2>&1 | tail -5 && grep -l "hreflang" dist/en/index.html 2>/dev/null && grep -c "hreflang" dist/en/index.html</automated>
  </verify>
  <acceptance_criteria>
    - src/i18n/ui.ts contains "export const ui" with "en" and "it" keys
    - src/i18n/ui.ts contains "export const routes" with "country"/"paese" translated slugs
    - src/i18n/utils.ts contains "export function getLangFromUrl"
    - src/i18n/utils.ts contains "export function useTranslations"
    - src/i18n/utils.ts contains "export function getLocalizedPath"
    - src/i18n/utils.ts contains "export function getAlternateLinks"
    - src/layouts/Base.astro contains "hreflang" and "x-default"
    - src/layouts/Base.astro contains "prefers-color-scheme" or "localStorage" (dark mode script)
    - src/layouts/Base.astro contains "@fontsource"
    - src/components/LanguageSwitcher.astro contains "getLocalizedPath"
    - src/components/DarkModeToggle.astro contains "localStorage"
    - src/components/Header.astro contains "LanguageSwitcher"
    - src/components/Footer.astro contains "footer.disclaimer"
    - src/pages/index.astro contains "redirect" and "Accept-Language"
    - src/pages/en/index.astro contains "lang" and "en"
    - src/pages/it/index.astro contains "lang" and "it"
    - dist/en/index.html contains at least 2 occurrences of "hreflang" (after build)
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>
    i18n routing works with /en/ and /it/ paths. Root URL redirects based on Accept-Language header. Every page has hreflang tags pointing to EN and IT versions plus x-default. Language switcher in header navigates between locale versions of the same page. Dark mode toggle with system preference detection and manual override. Placeholder homepages render with warm travel-magazine design. Legal disclaimer visible in footer.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. dist/en/index.html exists and contains English content
3. dist/it/index.html exists and contains Italian content
4. Both pages contain hreflang link tags for en, it, and x-default
5. Design tokens (sand, sage, terracotta, safe/moderate/danger) are defined in global.css
6. Dark mode classes are present in the HTML output
</verification>

<success_criteria>
- Astro project builds successfully with zero errors
- /en/ page renders English placeholder with hero, map placeholder, and disclaimer
- /it/ page renders Italian placeholder with all text translated
- hreflang tags present on every page linking EN <-> IT with x-default
- Root / redirects to /en/ or /it/ based on Accept-Language header
- Language switcher navigates between EN and IT versions of current page
- Dark mode toggle works (class-based with localStorage persistence)
- Design system uses warm palette tokens (sand, sage, terracotta) and OKLCH colorblind-safe map gradient
- Inter and DM Sans fonts are configured
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-01-SUMMARY.md`
</output>
