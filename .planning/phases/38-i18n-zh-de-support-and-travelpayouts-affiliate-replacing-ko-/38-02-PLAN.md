---
phase: 38
plan: 02
name: Wire zh and de pages, sitemap, hreflang, and country-name fallback
type: execute
wave: 2
depends_on:
  - "38-01"
files_modified:
  - src/pages/zh/index.astro
  - src/pages/zh/about/index.astro
  - src/pages/zh/methodology/index.astro
  - src/pages/zh/sources/index.astro
  - src/pages/zh/legal/index.astro
  - src/pages/zh/feedback/index.astro
  - src/pages/zh/compare.astro
  - src/pages/zh/global-safety.astro
  - src/pages/zh/country/[slug].astro
  - src/pages/zh/safest-countries/index.astro
  - src/pages/zh/most-dangerous-countries/index.astro
  - src/pages/zh/safest-for-solo-travelers/index.astro
  - src/pages/zh/safest-for-families/index.astro
  - src/pages/zh/countries-to-avoid/index.astro
  - src/pages/zh/improving-safety/index.astro
  - src/pages/zh/declining-safety/index.astro
  - src/pages/zh/embed-badge/index.astro
  - src/pages/zh/regions/europe/index.astro
  - src/pages/zh/regions/asia/index.astro
  - src/pages/zh/regions/africa/index.astro
  - src/pages/zh/regions/americas/index.astro
  - src/pages/zh/regions/oceania/index.astro
  - src/pages/zh/regions/middle-east/index.astro
  - src/pages/de/index.astro
  - src/pages/de/ueber-uns/index.astro
  - src/pages/de/methodik/index.astro
  - src/pages/de/quellen/index.astro
  - src/pages/de/impressum/index.astro
  - src/pages/de/feedback/index.astro
  - src/pages/de/vergleichen.astro
  - src/pages/de/globale-sicherheit.astro
  - src/pages/de/land/[slug].astro
  - src/pages/de/sicherste-laender/index.astro
  - src/pages/de/gefaehrlichste-laender/index.astro
  - src/pages/de/sicher-fuer-alleinreisende/index.astro
  - src/pages/de/sicher-fuer-familien/index.astro
  - src/pages/de/laender-zu-meiden/index.astro
  - src/pages/de/sicherheit-verbessert-sich/index.astro
  - src/pages/de/sicherheit-verschlechtert-sich/index.astro
  - src/pages/de/einbettbares-abzeichen/index.astro
  - src/pages/de/regionen/europa/index.astro
  - src/pages/de/regionen/asien/index.astro
  - src/pages/de/regionen/afrika/index.astro
  - src/pages/de/regionen/amerika/index.astro
  - src/pages/de/regionen/ozeanien/index.astro
  - src/pages/de/regionen/naher-osten/index.astro
  - src/pages/index.astro
  - src/lib/scores.ts
  - astro.config.mjs
  - scripts/validate-seo.ts
  - scripts/generate-llms-full.ts
  - functions/index.ts
autonomous: true
requirements:
  - I18N-ZH
  - I18N-DE
must_haves:
  truths:
    - "Visiting /zh/ in a browser renders a localized homepage with <html lang=\"zh\"> and Chinese UI strings"
    - "Visiting /de/ in a browser renders a localized homepage with <html lang=\"de\"> and German UI strings"
    - "All existing per-locale pages (about, country, methodology, sources, legal, compare, global-safety, hub pages, regions, badge, feedback) have working zh and de equivalents under their translated route slugs"
    - "Hreflang alternate links in every page <head> include both zh and de"
    - "Sitemap (dist/client/sitemap-*.xml) includes /zh/ and /de/ URLs after build"
    - "Country names display correctly in zh and de pages, falling back to English when no localized name is available in snapshot data"
    - "`bun run build` (or `npm run build`) completes successfully with no missing-key warnings and no broken-link errors"
  artifacts:
    - path: "src/pages/zh/index.astro"
      provides: "Chinese homepage entry point"
    - path: "src/pages/de/index.astro"
      provides: "German homepage entry point"
    - path: "src/pages/zh/country/[slug].astro"
      provides: "Chinese country detail dynamic route"
    - path: "src/pages/de/land/[slug].astro"
      provides: "German country detail dynamic route"
    - path: "src/lib/scores.ts"
      provides: "getLocalizedCountryName helper with en fallback"
      contains: "getLocalizedCountryName"
    - path: "astro.config.mjs"
      provides: "Sitemap config with zh + de locales and chunks"
      contains: "zh: 'zh'"
  key_links:
    - from: "src/pages/zh/* and src/pages/de/*"
      to: "src/i18n/ui.ts"
      via: "useTranslations(lang) with lang='zh'|'de'"
      pattern: "useTranslations\\('zh'\\)|useTranslations\\('de'\\)|const lang: Lang = '(zh|de)'"
    - from: "astro.config.mjs sitemap"
      to: "/zh/ and /de/ URLs"
      via: "i18n.locales config + chunks function"
      pattern: "zh:.*'zh'|de:.*'de'"
    - from: "src/pages/index.astro fallback redirect"
      to: "zh and de homepages"
      via: "supported map and hreflang alternates"
      pattern: "zh: 1|de: 1|hreflang=\"zh\"|hreflang=\"de\""
---

<objective>
Build the per-locale page tree and wire infrastructure (Astro config, sitemap, hreflang, root redirect, validation scripts, country-name fallback) so that the new `zh` and `de` languages introduced in plan 01 actually render real localized pages at `/zh/*` and `/de/*`. After this plan, the production build succeeds with both new locales fully integrated.

Purpose: Plan 01 added the translation source-of-truth. This plan delivers the user-facing pages that consume it.

Output: Two new per-locale page trees (~24 pages each, mirroring the en tree), updated Astro/sitemap/redirect config, a `getLocalizedCountryName` helper for graceful fallback when snapshot data lacks zh/de names, and a green build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/38-i18n-zh-de-support-and-travelpayouts-affiliate-replacing-ko-/38-CONTEXT.md
@.planning/phases/38-i18n-zh-de-support-and-travelpayouts-affiliate-replacing-ko-/38-01-SUMMARY.md
@/Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/i18n/ui.ts
@/Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/i18n/utils.ts
@/Users/riccardo/Developer/VibeCoding/Isitsafetotravel/astro.config.mjs
@/Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/lib/scores.ts
@/Users/riccardo/Developer/VibeCoding/Isitsafetotravel/scripts/validate-seo.ts
@/Users/riccardo/Developer/VibeCoding/Isitsafetotravel/scripts/generate-llms-full.ts
@/Users/riccardo/Developer/VibeCoding/Isitsafetotravel/functions/index.ts
@/Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/pages/index.astro
@/Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/pages/en/index.astro
@/Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/pages/en/about/index.astro
@/Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/pages/it/index.astro
@/Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/pages/it/paese/[slug].astro
@/Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/pages/it/regioni/europa/index.astro

<interfaces>
<!-- Existing per-locale page pattern (read it/index.astro and en/about/index.astro to confirm). -->
<!-- Every page is a thin wrapper that:
     1. exports prerender = true
     2. imports Base layout, useTranslations, routes
     3. declares `const lang: Lang = '<locale>'`
     4. computes JSON-LD with localized strings
     5. renders <Base lang={lang}> with localized headings
     The differences between locales are: lang literal, JSON-LD strings, file location matching the route slug. -->

<!-- Country snapshot shape (data/scores/latest.json):
     country.name = { en, it, es, fr, pt }   // NO zh/de keys yet
     Solution: introduce getLocalizedCountryName(country, lang) helper that does
       country.name[lang] ?? country.name.en
     and replace bare `country.name[lang]` accesses across components. -->

<!-- routes.zh and routes.de slugs (from plan 01):
     zh slugs intentionally remain English-aligned for SEO clarity
     de slugs are German ASCII transliterations:
       country=land, about=ueber-uns, methodology=methodik, legal=impressum,
       global-safety=globale-sicherheit, compare=vergleichen, donate=spenden,
       sources=quellen, safest-countries=sicherste-laender,
       most-dangerous-countries=gefaehrlichste-laender, regions=regionen,
       safest-for-solo-travelers=sicher-fuer-alleinreisende,
       safest-for-families=sicher-fuer-familien,
       countries-to-avoid=laender-zu-meiden,
       improving-safety=sicherheit-verbessert-sich,
       declining-safety=sicherheit-verschlechtert-sich,
       embed-badge=einbettbares-abzeichen,
       europe=europa, asia=asien, africa=afrika, americas=amerika,
       oceania=ozeanien, middle-east=naher-osten -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add getLocalizedCountryName helper, update astro.config.mjs, scripts, and root redirect</name>
  <files>
    src/lib/scores.ts,
    astro.config.mjs,
    scripts/validate-seo.ts,
    scripts/generate-llms-full.ts,
    functions/index.ts,
    src/pages/index.astro,
    src/components/Search.astro,
    src/components/hub/CountryRankRow.astro,
    src/components/country/AnswerFirstParagraph.astro,
    src/components/country/StatsSummary.astro,
    src/components/country/CountrySummary.astro,
    src/components/country/ComparisonTable.astro,
    src/components/country/ScoreHero.astro,
    src/components/country/RelatedCountries.astro,
    src/components/country/NeighborComparison.astro,
    src/pages/it/confronta.astro,
    src/pages/it/index.astro,
    src/pages/en/index.astro,
    src/pages/es/index.astro,
    src/pages/fr/index.astro,
    src/pages/pt/index.astro
  </files>
  <read_first>
    - /Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/lib/scores.ts
    - /Users/riccardo/Developer/VibeCoding/Isitsafetotravel/astro.config.mjs
    - /Users/riccardo/Developer/VibeCoding/Isitsafetotravel/scripts/validate-seo.ts
    - /Users/riccardo/Developer/VibeCoding/Isitsafetotravel/scripts/generate-llms-full.ts
    - /Users/riccardo/Developer/VibeCoding/Isitsafetotravel/functions/index.ts
    - /Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/pages/index.astro
    - /Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/pipeline/types.ts (read to confirm ScoredCountry.name shape)
  </read_first>
  <behavior>
    - `getLocalizedCountryName(country, lang)` returns localized name if present, else `country.name.en`
    - astro.config.mjs sitemap config and i18n.locales include zh and de
    - scripts/validate-seo.ts and scripts/generate-llms-full.ts iterate over 7 languages including zh and de
    - functions/index.ts SUPPORTED_LANGS includes zh and de
    - src/pages/index.astro hreflang and JS supported map include zh and de
    - All `country.name[lang]` accesses replaced with `getLocalizedCountryName(country, lang)` to avoid runtime undefined
  </behavior>
  <action>
    Make the following edits in sequence. Do NOT rebuild between edits — verification happens at the end of task 3.

    **Edit 1.1 — Add getLocalizedCountryName helper to src/lib/scores.ts.** Append at the end of the file (after the last function):

    ```ts
    import type { Lang } from '../i18n/ui';

    /**
     * Get a country's display name in the requested language, falling back to English
     * if the snapshot data was generated before that locale was added to the pipeline.
     * Snapshot files (data/scores/*.json) currently contain en/it/es/fr/pt names only;
     * zh and de fall back to en until the pipeline is regenerated to emit those keys.
     */
    export function getLocalizedCountryName(
      country: { name: Record<string, string> },
      lang: Lang,
    ): string {
      return country.name[lang] ?? country.name.en ?? '';
    }
    ```

    **Edit 1.2 — Update astro.config.mjs.** In the `sitemap({ ... })` integration call:
    - Extend `i18n.locales` map to include `zh: 'zh'` and `de: 'de'`
    - Extend `chunks` map to include `zh: (item) => item.url.includes('/zh/') ? item : undefined,` and `de: (item) => item.url.includes('/de/') ? item : undefined,`
    - Extend top-level `i18n.locales` array (line ~128) from `['en', 'it', 'es', 'fr', 'pt']` to `['en', 'it', 'es', 'fr', 'pt', 'zh', 'de']`
    - Extend `countryRouteSlugs` array (line ~66) from `['country', 'paese', 'pais', 'pays']` to `['country', 'paese', 'pais', 'pays', 'land']` (zh uses 'country' which is already covered; de uses 'land')

    **Edit 1.3 — Update scripts/validate-seo.ts.** Change line 14 from `const LANGUAGES = ["en", "it", "es", "fr", "pt"] as const;` to `const LANGUAGES = ["en", "it", "es", "fr", "pt", "zh", "de"] as const;`. Extend `COUNTRY_SEGMENT` map (lines 17–23) to add `zh: "country", de: "land",`. Update the comment on line 115 from "Check all 5 languages are present" to "Check all 7 languages are present".

    **Edit 1.4 — Update scripts/generate-llms-full.ts.** Change line 14 from `const LANGUAGES = ["en", "it", "es", "fr", "pt"] as const;` to `const LANGUAGES = ["en", "it", "es", "fr", "pt", "zh", "de"] as const;`. Extend `COUNTRY_SEGMENT` map (around line 17) to add `zh: "country", de: "land",`. Update the `ScoredCountry.name` type definition (line 31) from `name: { en: string; it: string; es: string; fr: string; pt: string };` to `name: { en: string; it: string; es: string; fr: string; pt: string; zh?: string; de?: string };` so optional access compiles.

    **Edit 1.5 — Update functions/index.ts.** Change line 5 from `const SUPPORTED_LANGS = ['it', 'es', 'fr', 'pt', 'en'] as const;` to `const SUPPORTED_LANGS = ['it', 'es', 'fr', 'pt', 'en', 'zh', 'de'] as const;`. The Accept-Language parser already handles arbitrary 2-letter codes, so adding to the supported set is sufficient (zh-CN → zh, de-DE → de).

    **Edit 1.6 — Update src/pages/index.astro.** This is the root fallback redirect page. Add hreflang alternates and update the JS `supported` map:
    - Add `<link rel="alternate" hreflang="zh" href="https://isitsafetotravel.org/zh/" />` and `<link rel="alternate" hreflang="de" href="https://isitsafetotravel.org/de/" />` after the existing `pt` line
    - Change the JS line `var supported = { it: 1, es: 1, fr: 1, pt: 1 };` to `var supported = { it: 1, es: 1, fr: 1, pt: 1, zh: 1, de: 1 };`
    - Add `| <a href="/zh/">中文</a> | <a href="/de/">Deutsch</a>` to the body `<p>` fallback link list

    **Edit 1.7 — Replace `country.name[lang]` accesses with `getLocalizedCountryName(country, lang)`.** Across the following files (already enumerated in `<files>`), replace every `country.name[lang]`, `c.name[lang]`, etc. with the helper call:

    Files to edit:
    - src/components/Search.astro (line 23: `name: c.name[lang]` → `name: getLocalizedCountryName(c, lang)`)
    - src/components/hub/CountryRankRow.astro (line 39)
    - src/components/country/AnswerFirstParagraph.astro (line 46)
    - src/components/country/StatsSummary.astro (line 65)
    - src/components/country/CountrySummary.astro (line 37)
    - src/components/country/ComparisonTable.astro (lines 35, 66)
    - src/components/country/ScoreHero.astro (line 66)
    - src/components/country/RelatedCountries.astro (lines 72, 95)
    - src/components/country/NeighborComparison.astro (lines 30, 44, 57)
    - src/pages/it/confronta.astro (line 22)
    - src/pages/it/index.astro (lines 14, 68)
    - src/pages/en/index.astro, src/pages/es/index.astro, src/pages/fr/index.astro, src/pages/pt/index.astro (any `name[lang]` accesses)

    For each file, add the import at the top of the frontmatter:
    ```ts
    import { getLocalizedCountryName } from '../../lib/scores';   // adjust depth
    ```
    and then replace the access. Pattern: `c.name[lang]` becomes `getLocalizedCountryName(c, lang)`. For `localeCompare` calls like `a.name[lang].localeCompare(b.name[lang])`, become `getLocalizedCountryName(a, lang).localeCompare(getLocalizedCountryName(b, lang))`.

    Use `grep -rln "name\[lang\]" src/` to find every remaining occurrence and replace exhaustively.

    NOTE on src/pipeline/config/countries.ts line 280 (`getCountryName`): widen the `lang` parameter type from `'en' | 'it' | 'es' | 'fr' | 'pt'` to include `'zh' | 'de'`, and adjust the return statement to `return entry?.name[lang as 'en'] ?? entry?.name.en ?? iso3;` so the existing en-only data still serves as fallback. This file is in the pipeline (not used at request time) but TypeScript compilation must still pass.
  </action>
  <verify>
    <automated>cd /Users/riccardo/Developer/VibeCoding/Isitsafetotravel && grep -q "getLocalizedCountryName" src/lib/scores.ts && grep -q "zh: 'zh'" astro.config.mjs && grep -q "de: 'de'" astro.config.mjs && grep -q "'zh'" scripts/validate-seo.ts && grep -q "'de'" scripts/validate-seo.ts && grep -q "zh: 1" src/pages/index.astro && grep -q "de: 1" src/pages/index.astro && grep -q "'zh'" functions/index.ts && grep -q "'de'" functions/index.ts && [ "$(grep -rln 'name\[lang\]' src/ | grep -v 'pipeline/config/countries.ts' | wc -l)" -eq 0 ] && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/scores.ts` exports `getLocalizedCountryName`
    - `astro.config.mjs` sitemap i18n.locales includes zh and de; chunks map includes zh and de; top-level `i18n.locales` array has 7 entries
    - `scripts/validate-seo.ts` LANGUAGES has 7 entries; COUNTRY_SEGMENT has zh and de mappings
    - `scripts/generate-llms-full.ts` LANGUAGES has 7 entries; ScoredCountry.name type allows optional zh/de
    - `functions/index.ts` SUPPORTED_LANGS contains zh and de
    - `src/pages/index.astro` has hreflang alternates for zh and de; JS supported map includes them
    - No remaining `name[lang]` direct accesses in `src/` outside pipeline/config/countries.ts (which uses safe cast)
    - File compiles: `npx astro check 2>&1 | grep -E "^[0-9]+ error"` shows 0 errors OR no new errors compared to baseline
  </acceptance_criteria>
  <done>Infrastructure ready for zh and de page rendering; helper functions and config changes in place.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create src/pages/zh/ page tree (24 pages)</name>
  <files>
    src/pages/zh/index.astro,
    src/pages/zh/about/index.astro,
    src/pages/zh/methodology/index.astro,
    src/pages/zh/sources/index.astro,
    src/pages/zh/legal/index.astro,
    src/pages/zh/feedback/index.astro,
    src/pages/zh/compare.astro,
    src/pages/zh/global-safety.astro,
    src/pages/zh/country/[slug].astro,
    src/pages/zh/safest-countries/index.astro,
    src/pages/zh/most-dangerous-countries/index.astro,
    src/pages/zh/safest-for-solo-travelers/index.astro,
    src/pages/zh/safest-for-families/index.astro,
    src/pages/zh/countries-to-avoid/index.astro,
    src/pages/zh/improving-safety/index.astro,
    src/pages/zh/declining-safety/index.astro,
    src/pages/zh/embed-badge/index.astro,
    src/pages/zh/regions/europe/index.astro,
    src/pages/zh/regions/asia/index.astro,
    src/pages/zh/regions/africa/index.astro,
    src/pages/zh/regions/americas/index.astro,
    src/pages/zh/regions/oceania/index.astro,
    src/pages/zh/regions/middle-east/index.astro
  </files>
  <read_first>
    - /Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/pages/en/index.astro
    - /Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/pages/en/about/index.astro
    - /Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/pages/en/methodology/index.astro
    - /Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/pages/en/sources/index.astro
    - /Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/pages/en/legal/index.astro
    - /Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/pages/en/feedback/index.astro
    - /Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/pages/en/compare.astro
    - /Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/pages/en/global-safety.astro
    - /Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/pages/en/country/[slug].astro
    - /Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/pages/en/safest-countries/index.astro
    - /Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/pages/en/most-dangerous-countries/index.astro
    - /Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/pages/en/safest-for-solo-travelers/index.astro
    - /Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/pages/en/safest-for-families/index.astro
    - /Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/pages/en/countries-to-avoid/index.astro
    - /Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/pages/en/improving-safety/index.astro
    - /Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/pages/en/declining-safety/index.astro
    - /Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/pages/en/embed-badge/index.astro
    - /Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/pages/en/regions/europe/index.astro
    - /Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/pages/en/regions/asia/index.astro
    - /Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/pages/en/regions/africa/index.astro
    - /Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/pages/en/regions/americas/index.astro
    - /Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/pages/en/regions/oceania/index.astro
    - /Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/pages/en/regions/middle-east/index.astro
  </read_first>
  <behavior>
    - Each new zh page is structurally identical to its en counterpart but with `const lang: Lang = 'zh'` and uses translated route slugs from `routes[lang]`
    - All pages prerender (export `prerender = true`)
    - JSON-LD blocks reference `t('...')` so they automatically pick up zh translations
    - All `name[lang]` accesses use the new `getLocalizedCountryName` helper added in task 1
  </behavior>
  <action>
    For each path listed in `<files>`, create the file by copying the structure of the corresponding en page, then make THREE substitutions:

    1. Change `const lang: Lang = 'en';` → `const lang: Lang = 'zh';`
    2. Where the file path differs (e.g., en uses `compare.astro` at /en/compare.astro, zh uses /zh/compare.astro at the same name — the route slug from routes.zh maps `compare → 'compare'`), keep the file at the literal path under `src/pages/zh/`. Note: zh route slugs are intentionally English-aligned (per plan 01 task 3), so the directory names match the en tree exactly. Country dynamic route is `src/pages/zh/country/[slug].astro` (not `paese`).
    3. Replace any `country.name[lang]` access with `getLocalizedCountryName(country, lang)` and import the helper from `../../lib/scores` (depth-adjust the relative path).

    Use one MultiEdit-like batch where possible: read en/index.astro, write zh/index.astro with the lang change. Repeat for all 23 other pages.

    For `src/pages/zh/country/[slug].astro`: copy `src/pages/en/country/[slug].astro` verbatim, change `const lang: Lang = 'en';` → `const lang: Lang = 'zh';`, replace `name[lang]` accesses with helper, and ensure the `getStaticPaths` function still iterates over all ISO3 codes (no language-specific filtering).

    For region pages, the directory name under regions/ stays English (europe, asia, africa, americas, oceania, middle-east) since routes.zh maps them to English-aligned slugs.

    Do NOT modify any en pages. Do NOT modify shared components — task 1 already updated them.

    If any en page imports a relative module (e.g., `../../layouts/Base.astro`), the depth from zh/ is identical (also 2 levels deep), so paths remain `../../layouts/...` without adjustment. Verify by inspection of one en page before bulk-creating.
  </action>
  <verify>
    <automated>cd /Users/riccardo/Developer/VibeCoding/Isitsafetotravel && [ -f src/pages/zh/index.astro ] && [ -f src/pages/zh/about/index.astro ] && [ -f src/pages/zh/country/[slug].astro ] && [ -f src/pages/zh/regions/europe/index.astro ] && [ -f src/pages/zh/embed-badge/index.astro ] && [ "$(find src/pages/zh -name '*.astro' | wc -l | tr -d ' ')" -ge 23 ] && grep -l "const lang: Lang = 'zh'" src/pages/zh/index.astro src/pages/zh/about/index.astro src/pages/zh/methodology/index.astro && [ "$(grep -rln "name\[lang\]" src/pages/zh/ | wc -l)" -eq 0 ] && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - All 23+ zh page files exist under src/pages/zh/ matching the en tree structure
    - Every zh page sets `const lang: Lang = 'zh';`
    - No `name[lang]` direct accesses inside src/pages/zh/ (all use helper)
    - All pages export `prerender = true`
    - File count under src/pages/zh/ matches src/pages/en/ exactly: `find src/pages/zh -name '*.astro' | wc -l` equals `find src/pages/en -name '*.astro' | wc -l`
  </acceptance_criteria>
  <done>Chinese page tree complete; structurally identical to en tree, ready for build.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create src/pages/de/ page tree (24 pages) and run final build</name>
  <files>
    src/pages/de/index.astro,
    src/pages/de/ueber-uns/index.astro,
    src/pages/de/methodik/index.astro,
    src/pages/de/quellen/index.astro,
    src/pages/de/impressum/index.astro,
    src/pages/de/feedback/index.astro,
    src/pages/de/vergleichen.astro,
    src/pages/de/globale-sicherheit.astro,
    src/pages/de/land/[slug].astro,
    src/pages/de/sicherste-laender/index.astro,
    src/pages/de/gefaehrlichste-laender/index.astro,
    src/pages/de/sicher-fuer-alleinreisende/index.astro,
    src/pages/de/sicher-fuer-familien/index.astro,
    src/pages/de/laender-zu-meiden/index.astro,
    src/pages/de/sicherheit-verbessert-sich/index.astro,
    src/pages/de/sicherheit-verschlechtert-sich/index.astro,
    src/pages/de/einbettbares-abzeichen/index.astro,
    src/pages/de/regionen/europa/index.astro,
    src/pages/de/regionen/asien/index.astro,
    src/pages/de/regionen/afrika/index.astro,
    src/pages/de/regionen/amerika/index.astro,
    src/pages/de/regionen/ozeanien/index.astro,
    src/pages/de/regionen/naher-osten/index.astro
  </files>
  <read_first>
    - /Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/pages/it/index.astro (closest existing pattern reference for non-default locale)
    - /Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/pages/it/paese/[slug].astro
    - /Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/pages/it/regioni/europa/index.astro
    - /Users/riccardo/Developer/VibeCoding/Isitsafetotravel/src/pages/en/index.astro and remaining en pages from task 2's list (re-use the structure)
    - /Users/riccardo/Developer/VibeCoding/Isitsafetotravel/package.json (to confirm `npm run build` script)
  </read_first>
  <behavior>
    - Each new de page is structurally identical to its en counterpart but with `const lang: Lang = 'de'` and the directory/file name uses the German route slug from routes.de
    - Build succeeds with no TypeScript errors, no missing-key warnings, and no broken-link errors
    - dist/client/de/ and dist/client/zh/ exist after build
    - dist/client/sitemap-*.xml includes /zh/ and /de/ URLs
  </behavior>
  <action>
    **Step A — Create the de page tree.** For each path listed in `<files>`, create the file by copying the structure of the corresponding en page (mapping below), then make these substitutions:

    1. Change `const lang: Lang = 'en';` → `const lang: Lang = 'de';`
    2. Replace `country.name[lang]` accesses with `getLocalizedCountryName(country, lang)` (import from `../../lib/scores`).
    3. Place the file at the German-slug path matching `routes.de` (from plan 01).

    File mapping (en source → de destination):
    - src/pages/en/index.astro → src/pages/de/index.astro
    - src/pages/en/about/index.astro → src/pages/de/ueber-uns/index.astro
    - src/pages/en/methodology/index.astro → src/pages/de/methodik/index.astro
    - src/pages/en/sources/index.astro → src/pages/de/quellen/index.astro
    - src/pages/en/legal/index.astro → src/pages/de/impressum/index.astro
    - src/pages/en/feedback/index.astro → src/pages/de/feedback/index.astro
    - src/pages/en/compare.astro → src/pages/de/vergleichen.astro
    - src/pages/en/global-safety.astro → src/pages/de/globale-sicherheit.astro
    - src/pages/en/country/[slug].astro → src/pages/de/land/[slug].astro
    - src/pages/en/safest-countries/index.astro → src/pages/de/sicherste-laender/index.astro
    - src/pages/en/most-dangerous-countries/index.astro → src/pages/de/gefaehrlichste-laender/index.astro
    - src/pages/en/safest-for-solo-travelers/index.astro → src/pages/de/sicher-fuer-alleinreisende/index.astro
    - src/pages/en/safest-for-families/index.astro → src/pages/de/sicher-fuer-familien/index.astro
    - src/pages/en/countries-to-avoid/index.astro → src/pages/de/laender-zu-meiden/index.astro
    - src/pages/en/improving-safety/index.astro → src/pages/de/sicherheit-verbessert-sich/index.astro
    - src/pages/en/declining-safety/index.astro → src/pages/de/sicherheit-verschlechtert-sich/index.astro
    - src/pages/en/embed-badge/index.astro → src/pages/de/einbettbares-abzeichen/index.astro
    - src/pages/en/regions/europe/index.astro → src/pages/de/regionen/europa/index.astro
    - src/pages/en/regions/asia/index.astro → src/pages/de/regionen/asien/index.astro
    - src/pages/en/regions/africa/index.astro → src/pages/de/regionen/afrika/index.astro
    - src/pages/en/regions/americas/index.astro → src/pages/de/regionen/amerika/index.astro
    - src/pages/en/regions/oceania/index.astro → src/pages/de/regionen/ozeanien/index.astro
    - src/pages/en/regions/middle-east/index.astro → src/pages/de/regionen/naher-osten/index.astro

    Relative import paths from de/ pages: same depth as en/ (2 levels), so `../../layouts/Base.astro` etc. work unchanged.

    For region pages (3 levels deep), use `../../../`.

    **Step B — Run build and fix any failures.** Execute:
    ```bash
    cd /Users/riccardo/Developer/VibeCoding/Isitsafetotravel
    npm run build 2>&1 | tee /tmp/build-38-02.log
    ```

    Address common failure modes:
    - **TypeScript errors** in country.name access: ensure all components/pages import and use `getLocalizedCountryName`
    - **Missing key warnings**: if `t('foo' as any)` calls reference keys not present in zh/de blocks, the runtime fallback to en works but the cast indicates the key was added later — no action needed unless validate-seo fails
    - **Broken hreflang in validate-seo.ts**: the script enumerates LANGUAGES; if it fails for zh/de homepages, confirm the page files exist
    - **Sitemap chunk missing**: confirm astro.config.mjs `chunks` map has zh and de entries
    - **Cloudflare adapter route conflicts**: none expected since /zh/ and /de/ are new prefixes

    If `npm run build` fails, read the error, fix the root cause (do not suppress), re-run until green.

    **Step C — Spot-check the rendered output:**
    ```bash
    grep -o '<html[^>]*lang="zh"' dist/client/zh/index.html | head -1
    grep -o '<html[^>]*lang="de"' dist/client/de/index.html | head -1
    grep -c 'hreflang="zh"' dist/client/en/index.html  # should be ≥ 1
    grep -c 'hreflang="de"' dist/client/en/index.html  # should be ≥ 1
    ls dist/client/sitemap-*.xml | xargs grep -l "/zh/" | head -1
    ls dist/client/sitemap-*.xml | xargs grep -l "/de/" | head -1
    ```
  </action>
  <verify>
    <automated>cd /Users/riccardo/Developer/VibeCoding/Isitsafetotravel && [ -f src/pages/de/index.astro ] && [ -f src/pages/de/ueber-uns/index.astro ] && [ -f src/pages/de/land/[slug].astro ] && [ -f src/pages/de/regionen/europa/index.astro ] && [ "$(find src/pages/de -name '*.astro' | wc -l | tr -d ' ')" -ge 23 ] && grep -q "const lang: Lang = 'de'" src/pages/de/index.astro && [ "$(grep -rln "name\[lang\]" src/pages/de/ | wc -l)" -eq 0 ] && npm run build 2>&1 | tail -20 | grep -qi "complete\|built\|success" && [ -f dist/client/zh/index.html ] && [ -f dist/client/de/index.html ] && grep -q 'lang="zh"' dist/client/zh/index.html && grep -q 'lang="de"' dist/client/de/index.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - All 23+ de page files exist at the German-slug paths listed in `<files>`
    - Every de page sets `const lang: Lang = 'de';`
    - No `name[lang]` direct accesses inside src/pages/de/
    - `npm run build` exits 0
    - `dist/client/zh/index.html` exists and `<html lang="zh">` is present
    - `dist/client/de/index.html` exists and `<html lang="de">` is present
    - `dist/client/en/index.html` contains `hreflang="zh"` and `hreflang="de"` alternate links
    - Sitemap includes /zh/ and /de/ URLs (`dist/client/sitemap-zh.xml` and `dist/client/sitemap-de.xml` exist OR the main sitemap-index references them)
    - validate-seo.ts post-build script passes (it runs as part of `npm run build`)
  </acceptance_criteria>
  <done>Both new locales render in production build; sitemap and hreflang properly include them; build is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crawler/SEO → static HTML | Hreflang, sitemap, and per-locale URLs feed search-engine crawlers |
| browser → /zh/* and /de/* | Anonymous read-only static page requests served via Cloudflare Pages |
| pipeline data → page rendering | Snapshot JSON files (`data/scores/*.json`) lack zh/de country names; fallback helper prevents undefined renders |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-03 | I (Information Disclosure) | astro.config.mjs sitemap | accept | Sitemap is public by design; no sensitive paths leak |
| T-38-04 | D (Denial of Service) | new /zh/ and /de/ pages adding ~2× page count | accept | Cloudflare Pages tier handles 10k+ pages; current site already serves 248 country pages × 5 locales |
| T-38-05 | T (Tampering) | getLocalizedCountryName fallback | mitigate | Fallback to `country.name.en ?? ''` ensures no undefined leakage to UI; type signature constrains input |
| T-38-06 | I (Information Disclosure) | functions/index.ts Accept-Language redirect | accept | Existing pattern; no new attack surface introduced by adding zh/de to SUPPORTED_LANGS |
</threat_model>

<verification>
Run after all 3 tasks complete:

```bash
cd /Users/riccardo/Developer/VibeCoding/Isitsafetotravel

# 1. Helper exists and is exported
grep -q "export function getLocalizedCountryName" src/lib/scores.ts

# 2. Config files updated
grep -q "zh: 'zh'" astro.config.mjs
grep -q "de: 'de'" astro.config.mjs
grep -q "'zh'" scripts/validate-seo.ts
grep -q "'de'" scripts/validate-seo.ts
grep -q "'zh'" functions/index.ts
grep -q "'de'" functions/index.ts

# 3. Page tree exists for both locales
test "$(find src/pages/zh -name '*.astro' | wc -l | tr -d ' ')" -ge 23
test "$(find src/pages/de -name '*.astro' | wc -l | tr -d ' ')" -ge 23

# 4. No direct name[lang] accesses in zh/de pages
test "$(grep -rln 'name\[lang\]' src/pages/zh src/pages/de | wc -l)" -eq 0

# 5. Build succeeds
npm run build

# 6. Output rendered correctly
grep -q 'lang="zh"' dist/client/zh/index.html
grep -q 'lang="de"' dist/client/de/index.html
grep -q 'hreflang="zh"' dist/client/en/index.html
grep -q 'hreflang="de"' dist/client/en/index.html

# 7. Sitemap includes new locales
ls dist/client/sitemap-*.xml
grep -l '/zh/' dist/client/sitemap-*.xml
grep -l '/de/' dist/client/sitemap-*.xml
```
</verification>

<success_criteria>
1. `getLocalizedCountryName(country, lang)` helper exists and is used in every component/page that previously did `country.name[lang]`
2. `astro.config.mjs` sitemap and i18n config include zh and de
3. `scripts/validate-seo.ts`, `scripts/generate-llms-full.ts`, `functions/index.ts`, and `src/pages/index.astro` all enumerate 7 locales
4. `src/pages/zh/` and `src/pages/de/` contain ≥ 23 .astro files each, structurally mirroring `src/pages/en/`
5. `npm run build` exits 0 with no TypeScript errors and no validate-seo failures
6. Built output (`dist/client/zh/index.html` and `dist/client/de/index.html`) renders with correct `<html lang>` and includes hreflang alternates for all 7 locales
7. Sitemap includes /zh/ and /de/ URLs
</success_criteria>

<output>
After completion, create `.planning/phases/38-i18n-zh-de-support-and-travelpayouts-affiliate-replacing-ko-/38-02-SUMMARY.md` documenting:
- Number of pages created in zh/ and de/
- Build duration and any warnings observed
- List of components touched for the name[lang] → helper migration
- Confirmation that validate-seo.ts passes for all 7 locales
- Sample localized URLs to spot-check (e.g., /zh/methodology/, /de/methodik/, /de/land/ITA/)
</output>
