---
phase: 34-scoring-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline/config/weights.json
  - src/pipeline/config/source-tiers.json
  - src/pipeline/scoring/normalize.ts
autonomous: true
requirements: [NORM-02, NORM-03]

must_haves:
  truths:
    - "weights.json contains all 37 advisory indicators in the conflict pillar with diminishing sub-weights summing to 0.36"
    - "source-tiers.json contains all 29 new advisory sources as signal tier with 7-day half-life and 30-day max-age"
    - "normalize.ts INDICATOR_RANGES has entries for all 37 advisory indicators with min:1, max:4, inverse:true"
  artifacts:
    - path: "src/pipeline/config/weights.json"
      provides: "v8.0.0 weights with 41 conflict indicators"
      contains: "advisory_level_pt"
    - path: "src/pipeline/config/source-tiers.json"
      provides: "Signal tier config for all 37 advisory sources"
      contains: "advisories_pt"
    - path: "src/pipeline/scoring/normalize.ts"
      provides: "Normalization ranges for all 37 advisory indicators"
      contains: "advisory_level_pt"
  key_links:
    - from: "src/pipeline/config/weights.json"
      to: "src/pipeline/scoring/engine.ts"
      via: "WeightsConfig loaded at runtime"
      pattern: "advisory_level_pt.*0\\.003"
    - from: "src/pipeline/config/source-tiers.json"
      to: "src/pipeline/scoring/engine.ts"
      via: "SourcesConfig loaded at runtime"
      pattern: "advisories_pt.*signal"
---

<objective>
Add all 29 missing advisory sources to the scoring configuration files: weights.json (indicator weights), source-tiers.json (signal tier classification), and normalize.ts (normalization ranges).

Purpose: Without these config entries, the 29 advisory fetchers produce data that gets silently dropped during scoring -- normalize.ts skips unknown indicators, and source-tiers.json doesn't classify them as signal tier.
Output: Updated weights.json v8.0.0, source-tiers.json, and normalize.ts with all 37 advisory sources registered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-scoring-integration/34-RESEARCH.md

@src/pipeline/config/weights.json
@src/pipeline/config/source-tiers.json
@src/pipeline/scoring/normalize.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update weights.json and source-tiers.json with all 37 advisory sources</name>
  <files>src/pipeline/config/weights.json, src/pipeline/config/source-tiers.json</files>
  <read_first>src/pipeline/config/weights.json, src/pipeline/config/source-tiers.json</read_first>
  <action>
**weights.json changes:**

1. Bump version from "7.0.0" to "8.0.0"
2. Replace the conflict pillar's `indicators` array with all 41 indicators (4 baseline + 37 advisory):
   ```
   "indicators": ["wb_political_stability", "gpi_overall", "gpi_safety_security", "gpi_militarisation",
     "advisory_level_us", "advisory_level_uk", "advisory_level_ca", "advisory_level_au",
     "advisory_level_de", "advisory_level_nl", "advisory_level_jp", "advisory_level_sk",
     "advisory_level_fr", "advisory_level_nz", "advisory_level_ie", "advisory_level_fi",
     "advisory_level_hk", "advisory_level_br", "advisory_level_at", "advisory_level_ph",
     "advisory_level_be", "advisory_level_dk", "advisory_level_sg", "advisory_level_ro",
     "advisory_level_rs", "advisory_level_ee", "advisory_level_hr", "advisory_level_ar",
     "advisory_level_it", "advisory_level_es", "advisory_level_kr", "advisory_level_tw",
     "advisory_level_cn", "advisory_level_in",
     "advisory_level_ch", "advisory_level_se", "advisory_level_no", "advisory_level_pl",
     "advisory_level_cz", "advisory_level_hu", "advisory_level_pt"]
   ```

3. Replace `indicatorWeights` with these exact values (baseline 0.64 + advisory 0.36 = 1.00):
   - Baseline (unchanged): wb_political_stability: 0.17, gpi_overall: 0.17, gpi_safety_security: 0.16, gpi_militarisation: 0.14
   - Original 4 (highest trust): advisory_level_us: 0.026, advisory_level_uk: 0.025, advisory_level_ca: 0.025, advisory_level_au: 0.025
   - Tier 1 API (4 sources, 0.018 each): advisory_level_de: 0.018, advisory_level_nl: 0.018, advisory_level_jp: 0.018, advisory_level_sk: 0.018
   - Tier 2a HTML (8 sources, 0.010 each): advisory_level_fr: 0.010, advisory_level_nz: 0.010, advisory_level_ie: 0.010, advisory_level_fi: 0.010, advisory_level_hk: 0.010, advisory_level_br: 0.010, advisory_level_at: 0.010, advisory_level_ph: 0.010
   - Tier 2b HTML (8 sources, 0.007 each): advisory_level_be: 0.007, advisory_level_dk: 0.007, advisory_level_sg: 0.007, advisory_level_ro: 0.007, advisory_level_rs: 0.007, advisory_level_ee: 0.007, advisory_level_hr: 0.007, advisory_level_ar: 0.007
   - Tier 3a complex (6 sources, 0.005 each): advisory_level_it: 0.005, advisory_level_es: 0.005, advisory_level_kr: 0.005, advisory_level_tw: 0.005, advisory_level_cn: 0.005, advisory_level_in: 0.005
   - Tier 3b complex (7 sources, 0.003 each): advisory_level_ch: 0.003, advisory_level_se: 0.003, advisory_level_no: 0.003, advisory_level_pl: 0.003, advisory_level_cz: 0.003, advisory_level_hu: 0.003, advisory_level_pt: 0.003

4. All other pillars (crime, health, governance, environment) remain UNCHANGED.

**source-tiers.json changes:**

Add 29 new entries to the `sources` object, one per missing advisory source. Each entry follows the same pattern as existing advisory sources:
```json
"advisories_fr": { "tier": "signal", "maxAgeDays": 30, "decayHalfLifeDays": 7 },
"advisories_nz": { "tier": "signal", "maxAgeDays": 30, "decayHalfLifeDays": 7 },
"advisories_ie": { "tier": "signal", "maxAgeDays": 30, "decayHalfLifeDays": 7 },
"advisories_fi": { "tier": "signal", "maxAgeDays": 30, "decayHalfLifeDays": 7 },
"advisories_hk": { "tier": "signal", "maxAgeDays": 30, "decayHalfLifeDays": 7 },
"advisories_br": { "tier": "signal", "maxAgeDays": 30, "decayHalfLifeDays": 7 },
"advisories_at": { "tier": "signal", "maxAgeDays": 30, "decayHalfLifeDays": 7 },
"advisories_ph": { "tier": "signal", "maxAgeDays": 30, "decayHalfLifeDays": 7 },
"advisories_be": { "tier": "signal", "maxAgeDays": 30, "decayHalfLifeDays": 7 },
"advisories_dk": { "tier": "signal", "maxAgeDays": 30, "decayHalfLifeDays": 7 },
"advisories_sg": { "tier": "signal", "maxAgeDays": 30, "decayHalfLifeDays": 7 },
"advisories_ro": { "tier": "signal", "maxAgeDays": 30, "decayHalfLifeDays": 7 },
"advisories_rs": { "tier": "signal", "maxAgeDays": 30, "decayHalfLifeDays": 7 },
"advisories_ee": { "tier": "signal", "maxAgeDays": 30, "decayHalfLifeDays": 7 },
"advisories_hr": { "tier": "signal", "maxAgeDays": 30, "decayHalfLifeDays": 7 },
"advisories_ar": { "tier": "signal", "maxAgeDays": 30, "decayHalfLifeDays": 7 },
"advisories_it": { "tier": "signal", "maxAgeDays": 30, "decayHalfLifeDays": 7 },
"advisories_es": { "tier": "signal", "maxAgeDays": 30, "decayHalfLifeDays": 7 },
"advisories_kr": { "tier": "signal", "maxAgeDays": 30, "decayHalfLifeDays": 7 },
"advisories_tw": { "tier": "signal", "maxAgeDays": 30, "decayHalfLifeDays": 7 },
"advisories_cn": { "tier": "signal", "maxAgeDays": 30, "decayHalfLifeDays": 7 },
"advisories_in": { "tier": "signal", "maxAgeDays": 30, "decayHalfLifeDays": 7 },
"advisories_ch": { "tier": "signal", "maxAgeDays": 30, "decayHalfLifeDays": 7 },
"advisories_se": { "tier": "signal", "maxAgeDays": 30, "decayHalfLifeDays": 7 },
"advisories_no": { "tier": "signal", "maxAgeDays": 30, "decayHalfLifeDays": 7 },
"advisories_pl": { "tier": "signal", "maxAgeDays": 30, "decayHalfLifeDays": 7 },
"advisories_cz": { "tier": "signal", "maxAgeDays": 30, "decayHalfLifeDays": 7 },
"advisories_hu": { "tier": "signal", "maxAgeDays": 30, "decayHalfLifeDays": 7 },
"advisories_pt": { "tier": "signal", "maxAgeDays": 30, "decayHalfLifeDays": 7 }
```

Keep all existing entries (worldbank, gpi, inform, advisories, advisories_us through advisories_sk, reliefweb, gdacs) unchanged.
  </action>
  <verify>
    <automated>node -e "const w=require('./src/pipeline/config/weights.json'); const c=w.pillars.find(p=>p.name==='conflict'); const iw=c.indicatorWeights; const sum=Object.values(iw).reduce((a,b)=>a+b,0); const count=c.indicators.length; console.log('Indicators:',count,'Weight sum:',sum.toFixed(6)); if(count!==41||Math.abs(sum-1.0)>0.001) process.exit(1); const st=require('./src/pipeline/config/source-tiers.json'); const skeys=Object.keys(st.sources).filter(k=>k.startsWith('advisories_')); console.log('Source-tiers advisory entries:',skeys.length); if(skeys.length!==37) process.exit(1); console.log('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - weights.json version is "8.0.0"
    - conflict pillar has exactly 41 indicators (4 baseline + 37 advisory)
    - indicatorWeights sum to 1.0 (baseline 0.64 + advisory 0.36)
    - source-tiers.json has 37 advisories_XX entries (8 existing + 29 new), all signal tier
  </acceptance_criteria>
  <done>weights.json v8.0.0 has all 37 advisory indicators with diminishing sub-weights. source-tiers.json has all 37 advisory sources as signal tier.</done>
</task>

<task type="auto">
  <name>Task 2: Add 29 advisory indicators to normalize.ts INDICATOR_RANGES</name>
  <files>src/pipeline/scoring/normalize.ts</files>
  <read_first>src/pipeline/scoring/normalize.ts</read_first>
  <action>
Add 29 new entries to the `INDICATOR_RANGES` const object in normalize.ts, after the existing 8 advisory entries (advisory_level_us through advisory_level_sk). All use identical config: `{ min: 1, max: 4, inverse: true }`.

Add these entries grouped by tier with comments:

```typescript
  // Tier 2a advisory sources (HTML scraping)
  advisory_level_fr: { min: 1, max: 4, inverse: true },
  advisory_level_nz: { min: 1, max: 4, inverse: true },
  advisory_level_ie: { min: 1, max: 4, inverse: true },
  advisory_level_fi: { min: 1, max: 4, inverse: true },
  advisory_level_hk: { min: 1, max: 4, inverse: true },
  advisory_level_br: { min: 1, max: 4, inverse: true },
  advisory_level_at: { min: 1, max: 4, inverse: true },
  advisory_level_ph: { min: 1, max: 4, inverse: true },

  // Tier 2b advisory sources (HTML scraping)
  advisory_level_be: { min: 1, max: 4, inverse: true },
  advisory_level_dk: { min: 1, max: 4, inverse: true },
  advisory_level_sg: { min: 1, max: 4, inverse: true },
  advisory_level_ro: { min: 1, max: 4, inverse: true },
  advisory_level_rs: { min: 1, max: 4, inverse: true },
  advisory_level_ee: { min: 1, max: 4, inverse: true },
  advisory_level_hr: { min: 1, max: 4, inverse: true },
  advisory_level_ar: { min: 1, max: 4, inverse: true },

  // Tier 3a complex advisory sources
  advisory_level_it: { min: 1, max: 4, inverse: true },
  advisory_level_es: { min: 1, max: 4, inverse: true },
  advisory_level_kr: { min: 1, max: 4, inverse: true },
  advisory_level_tw: { min: 1, max: 4, inverse: true },
  advisory_level_cn: { min: 1, max: 4, inverse: true },
  advisory_level_in: { min: 1, max: 4, inverse: true },

  // Tier 3b complex advisory sources
  advisory_level_ch: { min: 1, max: 4, inverse: true },
  advisory_level_se: { min: 1, max: 4, inverse: true },
  advisory_level_no: { min: 1, max: 4, inverse: true },
  advisory_level_pl: { min: 1, max: 4, inverse: true },
  advisory_level_cz: { min: 1, max: 4, inverse: true },
  advisory_level_hu: { min: 1, max: 4, inverse: true },
  advisory_level_pt: { min: 1, max: 4, inverse: true },
```

Do NOT modify the existing entries or the normalize/normalizeInverse/normalizeIndicators functions.
  </action>
  <verify>
    <automated>grep -c "advisory_level_" src/pipeline/scoring/normalize.ts | xargs -I{} bash -c 'if [ {} -eq 37 ]; then echo "PASS: 37 advisory entries"; else echo "FAIL: expected 37, got {}"; exit 1; fi'</automated>
  </verify>
  <acceptance_criteria>
    - normalize.ts INDICATOR_RANGES contains exactly 37 advisory_level_XX entries
    - All 29 new entries have min:1, max:4, inverse:true
    - Existing entries are unchanged
    - File compiles without TypeScript errors
  </acceptance_criteria>
  <done>All 37 advisory indicators have normalization ranges so none are silently skipped during scoring.</done>
</task>

</tasks>

<verification>
- weights.json conflict pillar has 41 indicators with sub-weights summing to 1.0
- source-tiers.json has 37 advisory source entries, all signal tier
- normalize.ts has 37 advisory_level_XX entries in INDICATOR_RANGES
- No existing entries in any file were modified (only additions)
</verification>

<success_criteria>
All 37 advisory sources are registered in the three config/normalization files so advisory data flows through normalization and tiered scoring. The 29 previously-dropped advisory indicators will now be normalized and weighted in the conflict pillar.
</success_criteria>

<output>
After completion, create `.planning/phases/34-scoring-integration/34-01-SUMMARY.md`
</output>
