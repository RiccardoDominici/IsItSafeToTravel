---
phase: 28-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - src/i18n/ui.ts
  - src/pages/en/methodology/index.astro
  - src/pages/it/metodologia/index.astro
  - src/pages/es/metodologia/index.astro
  - src/pages/fr/methodologie/index.astro
  - src/pages/pt/metodologia/index.astro
  - public/llms.txt
  - public/llms-full.txt
  - scripts/generate-llms-full.ts
  - README.md
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03

must_haves:
  truths:
    - "No mention of GDELT or WHO DONs in any user-facing page or documentation"
    - "Pipeline runs end-to-end and produces valid scores for all 248 countries"
    - "No country score deviates more than 0.3 points from pre-removal scores"
    - "Methodology page accurately lists only the remaining active data sources"
  artifacts:
    - path: "src/i18n/ui.ts"
      provides: "Updated i18n strings without GDELT/WHO DON references in all 5 languages"
    - path: "README.md"
      provides: "Updated documentation without GDELT/WHO DON references"
    - path: "public/llms.txt"
      provides: "Updated LLM-facing content without GDELT/WHO DON references"
  key_links:
    - from: "src/pages/en/methodology/index.astro"
      to: "src/i18n/ui.ts"
      via: "t() translation function"
      pattern: "methodology\\.source\\."
---

<objective>
Update all documentation, i18n strings, and LLM-facing content to remove GDELT and WHO DONs references, then verify pipeline produces stable scores.

Purpose: Ensure user-facing content accurately reflects the remaining data sources and confirm score stability after source removal.
Output: All docs updated, pipeline verified with stable scores.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-cleanup/28-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update i18n strings and methodology pages to remove GDELT and WHO DONs</name>
  <files>
    src/i18n/ui.ts
    src/pages/en/methodology/index.astro
    src/pages/it/metodologia/index.astro
    src/pages/es/metodologia/index.astro
    src/pages/fr/methodologie/index.astro
    src/pages/pt/metodologia/index.astro
    public/llms.txt
    public/llms-full.txt
    scripts/generate-llms-full.ts
    README.md
  </files>
  <read_first>
    src/i18n/ui.ts
    src/pages/en/methodology/index.astro
    public/llms.txt
    public/llms-full.txt
    scripts/generate-llms-full.ts
    README.md
  </read_first>
  <action>
    **1. Update `src/i18n/ui.ts` — ALL 5 language blocks (en, it, es, fr, pt):**

    For each language block:
    - Remove the `methodology.source.gdelt` key
    - Remove the `methodology.source.gdelt_url` key
    - Remove the `methodology.source.gdelt_desc` key
    - Remove the `methodology.source.who_dons` key
    - Remove the `methodology.source.who_dons_url` key
    - Remove the `methodology.source.who_dons_desc` key
    - Remove the `methodology.indicator.gdelt_instability` key
    - Update `methodology.tiered_text` to remove "GDELT" and "WHO DONs" from the signal sources list. Keep ReliefWeb, GDACS, and government advisories.
    - Update `methodology.pillar.conflict.description` to remove "GDELT media instability signal" references. Keep advisory and GPI references.
    - Update `methodology.pillar.conflict.sources` to remove "GDELT Media Instability Index" / equivalent. Keep other conflict sources.
    - Update `about.project_text` to remove "GDELT" and "WHO Disease Outbreak News" from the source list. Update source count from "9+" to "7+" (or appropriate count).

    **2. Update methodology pages (all 5 languages):**
    - In each `index.astro`: remove `{ key: 'gdelt', frequency: t('methodology.frequency.daily') }` from the sources array
    - Remove `{ key: 'who_dons', frequency: t('methodology.frequency.weekly') }` from the sources array

    **3. Update `public/llms.txt`:**
    - Remove "GDELT" from the sources line. Change to: "Sources: Global Peace Index (GPI), INFORM Risk Index, ReliefWeb, GDACS, US/UK/CA/AU government travel advisories"

    **4. Update `public/llms-full.txt`:**
    - Remove "GDELT" from the Source line in the header. Update to mention ReliefWeb and GDACS instead.

    **5. Update `scripts/generate-llms-full.ts`:**
    - Remove "GDELT" from the source description template string. Update to mention ReliefWeb and GDACS.
    - Update the sources line at the bottom to match llms.txt changes.

    **6. Update `README.md`:**
    - Remove "GDELT Project" row from the data sources table
    - Remove "WHO DONs" references from the signal sources list
    - Remove GDELT from the conflict pillar description
    - Remove "GDELT" and "WHO DONs" from the freshness decay table
    - Update the indicator weights description to remove gdelt_instability
    - Update source count references
  </action>
  <verify>
    <automated>cd /Users/riccardo/Developer/VibeCoding/Isitsafetotravel && grep -ri "gdelt\|who.dons\|who_active" src/i18n/ui.ts src/pages/*/metod*/index.astro src/pages/en/methodology/index.astro public/llms.txt public/llms-full.txt scripts/generate-llms-full.ts README.md 2>/dev/null | grep -v "planning" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ri "gdelt" src/i18n/ui.ts` returns empty
    - `grep -ri "who.dons\|who_active" src/i18n/ui.ts` returns empty
    - `grep -ri "gdelt" src/pages/en/methodology/index.astro` returns empty
    - `grep -ri "gdelt" public/llms.txt public/llms-full.txt` returns empty
    - `grep -ri "gdelt" README.md` returns empty (excluding planning references)
    - The methodology source array in each language's index.astro has no gdelt or who_dons entries
  </acceptance_criteria>
  <done>
    All 5 language blocks in ui.ts updated. All 5 methodology pages updated. llms.txt, llms-full.txt, generate script, and README updated. Zero GDELT/WHO DONs references remain in user-facing content.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run pipeline and verify score stability</name>
  <files></files>
  <read_first>
    src/pipeline/config/weights.json
    src/pipeline/config/source-tiers.json
  </read_first>
  <action>
    **1. Capture pre-removal scores as baseline:**
    - Read `data/scores/latest.json` and extract scores for a representative sample of countries (at least 20 countries across all regions and risk levels)

    **2. Run the full pipeline:**
    - Execute `npm run pipeline` (or equivalent pipeline command)
    - Verify it completes without errors

    **3. Compare scores:**
    - Read the newly generated scores file
    - Compare each country's score against the pre-removal baseline
    - Verify no country deviates by more than 0.3 points
    - Log any countries that changed and by how much

    **4. Build verification:**
    - Run `npm run build` to ensure the Astro site builds cleanly with updated content
    - Verify no build warnings about missing translation keys

    **5. Final grep sweep:**
    - Run a comprehensive grep across the ENTIRE src/ directory (not just pipeline) for any remaining gdelt/who-dons references
    - Fix any stragglers found
  </action>
  <verify>
    <automated>cd /Users/riccardo/Developer/VibeCoding/Isitsafetotravel && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Pipeline completes successfully producing scores for 248 countries
    - `npm run build` succeeds with no errors
    - No country score deviates by more than 0.3 points from pre-removal scores
    - `grep -ri "gdelt\|who.dons\|who_active\|fips.to.iso3" src/ --include="*.ts" --include="*.astro" | grep -v node_modules | grep -v ".planning"` returns empty
  </acceptance_criteria>
  <done>
    Pipeline runs cleanly, all 248 countries scored, no score deviates by more than 0.3 points, site builds successfully, zero remaining references to removed sources anywhere in src/.
  </done>
</task>

</tasks>

<verification>
1. `grep -ri "gdelt\|who.dons\|who_active" src/ --include="*.ts" --include="*.astro" | grep -v node_modules` — Zero matches
2. `npm run build` — Clean build, no errors
3. Score comparison — No country deviates more than 0.3 points
</verification>

<success_criteria>
- All user-facing documentation accurately reflects remaining sources (no GDELT/WHO DONs)
- Pipeline produces valid scores for all 248 countries
- No country score deviates more than 0.3 points from pre-removal baseline
- Site builds and deploys cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/28-cleanup/28-02-SUMMARY.md`
</output>
