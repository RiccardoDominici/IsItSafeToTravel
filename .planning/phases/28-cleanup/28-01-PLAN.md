---
phase: 28-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline/fetchers/gdelt.ts
  - src/pipeline/fetchers/who-dons.ts
  - src/pipeline/fetchers/index.ts
  - src/pipeline/config/weights.json
  - src/pipeline/config/source-tiers.json
  - src/pipeline/config/fips-to-iso3.ts
  - src/pipeline/scoring/normalize.ts
  - src/pipeline/scoring/engine.ts
  - src/pipeline/scoring/__tests__/engine.test.ts
  - src/pipeline/__tests__/crisis-validation.test.ts
  - src/pipeline/__tests__/data01-fetchers.test.ts
  - src/pipeline/__tests__/data02-score-range.test.ts
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03

must_haves:
  truths:
    - "Pipeline compiles and runs without any import or reference to gdelt.ts or who-dons.ts"
    - "weights.json has no gdelt_instability or who_active_outbreaks indicators"
    - "source-tiers.json has no gdelt or who-dons entries"
    - "All existing tests pass after indicator/weight redistribution"
  artifacts:
    - path: "src/pipeline/config/weights.json"
      provides: "Updated weights without GDELT/WHO DON indicators"
      contains: "version"
    - path: "src/pipeline/config/source-tiers.json"
      provides: "Updated source tiers without GDELT/WHO DON entries"
    - path: "src/pipeline/fetchers/index.ts"
      provides: "Fetcher barrel export without GDELT/WHO DON"
  key_links:
    - from: "src/pipeline/fetchers/index.ts"
      to: "fetchAllSources"
      via: "Promise.allSettled array"
      pattern: "fetchAllSources"
    - from: "src/pipeline/scoring/engine.ts"
      to: "INDICATOR_SOURCE_MAP"
      via: "indicator-to-source mapping"
      pattern: "INDICATOR_SOURCE_MAP"
---

<objective>
Remove WHO DONs and GDELT source code from the pipeline, update scoring configuration, and fix all tests.

Purpose: Clean the codebase of broken data sources before adding 30+ new advisory sources in subsequent phases.
Output: Pipeline code compiles and all tests pass with no references to WHO DONs or GDELT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove GDELT and WHO DONs fetchers and update pipeline config</name>
  <files>
    src/pipeline/fetchers/gdelt.ts
    src/pipeline/fetchers/who-dons.ts
    src/pipeline/fetchers/index.ts
    src/pipeline/config/weights.json
    src/pipeline/config/source-tiers.json
    src/pipeline/config/fips-to-iso3.ts
    src/pipeline/scoring/normalize.ts
    src/pipeline/scoring/engine.ts
  </files>
  <read_first>
    src/pipeline/fetchers/index.ts
    src/pipeline/fetchers/gdelt.ts
    src/pipeline/fetchers/who-dons.ts
    src/pipeline/config/weights.json
    src/pipeline/config/source-tiers.json
    src/pipeline/scoring/normalize.ts
    src/pipeline/scoring/engine.ts
    src/pipeline/config/fips-to-iso3.ts
  </read_first>
  <action>
    **1. Delete fetcher files:**
    - Delete `src/pipeline/fetchers/gdelt.ts` entirely
    - Delete `src/pipeline/fetchers/who-dons.ts` entirely
    - Delete `src/pipeline/config/fips-to-iso3.ts` entirely (only used by GDELT fetcher)

    **2. Update `src/pipeline/fetchers/index.ts`:**
    - Remove `import { fetchGdelt } from './gdelt.js';` and `import { fetchWhoDons } from './who-dons.js';`
    - Remove `export { fetchGdelt } from './gdelt.js';` and `export { fetchWhoDons } from './who-dons.js';`
    - Remove `fetchGdelt(date)` and `fetchWhoDons(date)` from the `Promise.allSettled` array in `fetchAllSources`
    - Remove `'gdelt'` and `'who-dons'` from the `sourceNames` array
    - Update the JSDoc comment to remove GDELT and WHO DONs from the sources list

    **3. Update `src/pipeline/config/weights.json`:**
    - Bump version to `"7.0.0"`
    - In the `conflict` pillar:
      - Remove `"gdelt_instability"` from `indicators` array
      - Remove `"gdelt_instability": 0.18` from `indicatorWeights`
      - Redistribute the 0.18 weight among remaining conflict indicators proportionally:
        - `wb_political_stability`: 0.14 -> 0.17
        - `gpi_overall`: 0.14 -> 0.17
        - `gpi_safety_security`: 0.13 -> 0.16
        - `gpi_militarisation`: 0.11 -> 0.14
        - `advisory_level_us`: 0.09 -> 0.11
        - `advisory_level_uk`: 0.08 -> 0.10
        - `advisory_level_ca`: 0.07 -> 0.08
        - `advisory_level_au`: 0.06 -> 0.07
        (Total: 1.00)
    - In the `health` pillar:
      - Remove `"who_active_outbreaks"` from `indicators` array
      - Remove `"who_active_outbreaks": 0.20` from `indicatorWeights`
      - Redistribute the 0.20 weight among remaining health indicators:
        - `wb_child_mortality`: 0.30 -> 0.38
        - `inform_health`: 0.25 -> 0.31
        - `inform_epidemic`: 0.25 -> 0.31
        (Total: 1.00)

    **4. Update `src/pipeline/config/source-tiers.json`:**
    - Remove the `"gdelt"` entry from `sources`
    - Remove the `"who-dons"` entry from `sources`

    **5. Update `src/pipeline/scoring/normalize.ts`:**
    - Remove `gdelt_instability` entry from `INDICATOR_RANGES`
    - Remove `who_active_outbreaks` entry from `INDICATOR_RANGES`

    **6. Update `src/pipeline/scoring/engine.ts`:**
    - Remove `gdelt_instability: 'gdelt'` from `INDICATOR_SOURCE_MAP`
    - Remove `who_active_outbreaks: 'who-dons'` from `INDICATOR_SOURCE_MAP`
    - Remove the `gdelt` entry from `SOURCE_CATALOG`
    - Remove the `'who-dons'` entry from `SOURCE_CATALOG`
    - Update JSDoc comment on `computeCountryScore` from "v6.0" to "v7.0"
  </action>
  <verify>
    <automated>cd /Users/riccardo/Developer/VibeCoding/Isitsafetotravel && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "gdelt" src/pipeline/ --include="*.ts" | grep -v __tests__ | grep -v ".d.ts"` returns empty
    - `grep -r "who-dons\|who_dons\|who_active" src/pipeline/ --include="*.ts" | grep -v __tests__ | grep -v ".d.ts"` returns empty
    - `grep -r "fips-to-iso3\|fips_to_iso3\|FIPS_TO_ISO3" src/pipeline/ --include="*.ts" | grep -v __tests__` returns empty
    - `cat src/pipeline/config/weights.json | grep -c "gdelt_instability"` returns 0
    - `cat src/pipeline/config/weights.json | grep -c "who_active_outbreaks"` returns 0
    - `cat src/pipeline/config/source-tiers.json | grep -c "gdelt"` returns 0
    - `cat src/pipeline/config/source-tiers.json | grep -c "who-dons"` returns 0
    - `cat src/pipeline/config/weights.json | grep "version"` shows "7.0.0"
    - TypeScript compilation succeeds with no errors
  </acceptance_criteria>
  <done>
    GDELT and WHO DONs fetcher files deleted, all pipeline config/code references removed, weights redistributed with version 7.0.0, TypeScript compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update all tests to remove GDELT and WHO DONs references</name>
  <files>
    src/pipeline/scoring/__tests__/engine.test.ts
    src/pipeline/__tests__/crisis-validation.test.ts
    src/pipeline/__tests__/data01-fetchers.test.ts
    src/pipeline/__tests__/data02-score-range.test.ts
  </files>
  <read_first>
    src/pipeline/scoring/__tests__/engine.test.ts
    src/pipeline/__tests__/crisis-validation.test.ts
    src/pipeline/__tests__/data01-fetchers.test.ts
    src/pipeline/__tests__/data02-score-range.test.ts
  </read_first>
  <action>
    **1. Update `src/pipeline/scoring/__tests__/engine.test.ts`:**
    - Remove all `gdelt_instability` indicators from test data arrays
    - Remove `gdelt` from sourcesConfig test objects
    - Remove `who-dons` from sourcesConfig test objects if present
    - Update conflict pillar test configs to remove `gdelt_instability` from indicators arrays
    - Update `indicatorWeights` in test configs to match new weights (without gdelt_instability)
    - Adjust expected score calculations to reflect only remaining indicators
    - In the tiered scoring test: remove gdelt signal data, keep only advisories/reliefweb/gdacs as signals
    - In the stale data test: use a different signal source (e.g., reliefweb or advisories) instead of gdelt

    **2. Update `src/pipeline/__tests__/crisis-validation.test.ts`:**
    - In the conflict pillar config: remove `gdelt_instability` from indicators and indicatorWeights
    - In the health pillar config: remove `who_active_outbreaks` from indicators and indicatorWeights
    - Remove `gdelt` and `who-dons` from the sourcesConfig
    - In the Sudan test case: remove the `gdelt_instability` indicator from test data. The crisis should still be detectable via the remaining conflict indicators (low political stability, high GPI scores, advisory levels)
    - In the DRC/health test case: remove the `who_active_outbreaks` indicator from test data. The crisis should still be detectable via remaining health indicators (high child mortality, high INFORM health/epidemic scores)
    - Update `indicatorWeights` to match new weights from weights.json v7.0.0

    **3. Update `src/pipeline/__tests__/data01-fetchers.test.ts`:**
    - Remove the assertion `assert.equal(typeof mod.fetchGdelt, 'function', 'fetchGdelt should be exported');`
    - Remove any assertion about fetchWhoDons if present

    **4. Update `src/pipeline/__tests__/data02-score-range.test.ts`:**
    - Remove `gdelt_instability` from the conflict pillar indicators array
    - Remove `makeIndicator('gdelt_instability', ...)` calls from test data
    - Adjust pillar weights if test uses custom weights
  </action>
  <verify>
    <automated>cd /Users/riccardo/Developer/VibeCoding/Isitsafetotravel && npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "gdelt" src/pipeline/__tests__/ src/pipeline/scoring/__tests__/` returns empty
    - `grep -r "who.dons\|who_active" src/pipeline/__tests__/ src/pipeline/scoring/__tests__/` returns empty
    - `npm test` passes with 0 failures
  </acceptance_criteria>
  <done>
    All test files updated to remove GDELT and WHO DONs references. All tests pass.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — TypeScript compiles with no errors
2. `npm test` — All tests pass
3. `grep -r "gdelt\|who.dons\|who_active\|fips.to.iso3" src/pipeline/ --include="*.ts"` — No references remain in pipeline code
</verification>

<success_criteria>
- Zero references to GDELT or WHO DONs in any pipeline source file (src/pipeline/)
- All tests pass
- weights.json v7.0.0 with redistributed indicator weights that sum to 1.00 per pillar
- source-tiers.json has no gdelt or who-dons entries
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/28-cleanup/28-01-SUMMARY.md`
</output>
