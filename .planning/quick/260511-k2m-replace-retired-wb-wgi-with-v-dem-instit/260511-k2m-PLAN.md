---
phase: quick-260511-k2m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline/fetchers/vdem.ts
  - src/pipeline/fetchers/worldbank.ts
  - src/pipeline/fetchers/index.ts
  - src/pipeline/scoring/normalize.ts
  - src/pipeline/scoring/engine.ts
  - src/pipeline/config/weights.json
  - src/pipeline/config/source-tiers.json
  - src/pipeline/scoring/__tests__/engine.test.ts
  - src/pipeline/scoring/__tests__/vdem.test.ts
  - scripts/backfill-vdem-rescore.ts
  - src/pages/en/sources/index.astro
  - src/pages/it/fonti/index.astro
  - src/pages/pt/fontes/index.astro
  - src/pages/es/fuentes/index.astro
  - src/pages/fr/sources/index.astro
  - src/pages/de/quellen/index.astro
  - src/pages/zh/sources/index.astro
  - src/i18n/ui.ts
  - .gitignore
autonomous: true
requirements: [QUICK-260511-K2M]

must_haves:
  truths:
    - "V-Dem v16 data is fetched for every snapshot year 2012-2025 from OWID CSV endpoints with no auth"
    - "Indicator `wb_political_stability` is fully removed from weights.json, normalize.ts, engine.ts INDICATOR_SOURCE_MAP"
    - "Indicators `wb_rule_of_law`, `wb_gov_effectiveness`, `wb_corruption_control` are fully renamed to `vdem_rule_of_law`, `vdem_gov_effectiveness`, `vdem_corruption_control` across config, scoring code, tests, i18n, UI, and all 611 historical scored snapshots"
    - "Conflict pillar `indicatorWeights` sum to exactly 1.0 after political_stability removal and redistribution"
    - "weights.json `version` bumps to `8.1.0`"
    - "All 611 snapshots in data/scores/*.json are re-scored with V-Dem data and contain `vdem_*` indicator names (no surviving `wb_political_stability` / `wb_rule_of_law` / `wb_gov_effectiveness` / `wb_corruption_control` references)"
    - "history-index.json is regenerated from the rescored snapshots"
    - "Algeria + US + JP + NO + BR + IN have non-null crime and governance pillars with coverage >= 30% in public/scores.json (current snapshot)"
    - "Methodology + sources pages and ui.ts strings reference V-Dem v16 (CC-BY-SA) instead of WGI in all 7 locales"
    - "Source count copy enumerates exactly 7 distinct sources by name (WB, V-Dem, INFORM, GPI, ReliefWeb, GDACS, Travel Advisories) consistently across `methodology.tiered_text`, `about.project_text`, and `about.sources_text` in all 7 locales — no surviving `7+` or `9+` loose-count phrasings, no surviving `8 sources` claim"
    - "V-Dem CC-BY-SA attribution (Coppedge et al. + V-Dem Institute + DOI 10.23696/vdemds26) appears in user-visible methodology copy and in at least one of the 7 sources/index.astro pages"
    - "npm run test:pipeline and npm run build both exit green"
    - "Backfill script is idempotent: running it twice produces byte-identical scored snapshots (skips per-snapshot rescore when `weightsVersion` in data/scores/{D}.json already equals `8.1.0`)"
    - "Raw V-Dem CSV downloads are .gitignored; only parsed JSON enters the repo"
    - "MIN_PILLAR_COVERAGE remains 0.30 (unchanged) at src/pipeline/scoring/engine.ts:49"
  artifacts:
    - path: "src/pipeline/fetchers/vdem.ts"
      provides: "fetchVdem(date) — OWID CSV fetcher for 3 V-Dem indicators, writes data/raw/{date}/vdem-parsed.json"
      exports: ["fetchVdem"]
    - path: "src/pipeline/config/weights.json"
      provides: "version 8.1.0 with vdem_* names, no wb_political_stability, conflict weights sum to 1.0"
      contains: "vdem_rule_of_law, vdem_gov_effectiveness, vdem_corruption_control"
    - path: "src/pipeline/scoring/normalize.ts"
      provides: "INDICATOR_RANGES for vdem_rule_of_law (0-1), vdem_gov_effectiveness (-3.5..3.5), vdem_corruption_control (0-1 inverse:true)"
    - path: "scripts/backfill-vdem-rescore.ts"
      provides: "Year-grouped rescore script that fetches V-Dem once per year (2012-2025) and rescores all 611 snapshots without refetching other sources; skips snapshots already at weightsVersion=8.1.0"
    - path: "data/raw/2026-05-11/vdem-parsed.json"
      provides: "Fetched 2025-year V-Dem indicators in RawSourceData envelope"
    - path: "data/scores/history-index.json"
      provides: "Regenerated history index after rescore"
  key_links:
    - from: "src/pipeline/fetchers/vdem.ts"
      to: "ourworldindata.org/grapher/*.csv"
      via: "fetch() over 3 indicator slugs"
      pattern: "fetch.*ourworldindata\\.org/grapher/(rule-of-law-index|political-corruption-index|rigorous-and-impartial-public-administration-score)"
    - from: "src/pipeline/scoring/normalize.ts"
      to: "src/pipeline/config/weights.json"
      via: "indicator name match (vdem_*)"
      pattern: "vdem_rule_of_law.*vdem_gov_effectiveness.*vdem_corruption_control"
    - from: "src/pipeline/scoring/engine.ts INDICATOR_SOURCE_MAP"
      to: "src/pipeline/config/source-tiers.json"
      via: "source name 'vdem' present in both"
      pattern: "vdem.*baseline"
    - from: "scripts/backfill-vdem-rescore.ts"
      to: "src/pipeline/run.ts runPipeline()"
      via: "year-grouped iteration over data/scores/*.json"
      pattern: "runPipeline\\("
    - from: "src/i18n/ui.ts methodology.source.wb"
      to: "src/pages/{lang}/{sources}/index.astro"
      via: "shared SOURCES list / i18n key"
      pattern: "V-Dem|vdem"
---

<objective>
Replace the four retired World Bank Worldwide Governance Indicators (PV.EST / RL.EST / GE.EST / CC.EST) with three V-Dem Institute v16 indicators served via Our World In Data CSV endpoints. Drop political_stability (no clean V-Dem 1:1) and redistribute its 0.17 internal weight inside the Conflict pillar to GPI + advisories. Fully rename the three remaining governance indicators (`wb_*` -> `vdem_*`) across pipeline code, weights, tests, all 611 historical scored snapshots, methodology / sources UI pages and the i18n strings for all 7 locales.

Purpose: WB WGI stopped returning current data after 2023; Algeria + several other countries are losing crime+governance pillars entirely (coverage < 30% gate). V-Dem v16 (released 2026-03-17, CC-BY-SA, covers through 2025) is the cleanest open replacement with stable OWID mirror URLs and zero-auth CSV format.

Output: New `vdem` fetcher, weights.json v8.1.0, full rescore of 2012-03-19 -> 2026-05-11 (611 snapshots), regenerated history index, refreshed methodology + source pages in en/it/pt/es/fr/de/zh, green tests and build.
</objective>

<source_count_rule>
**Single counting rule (applies to all locales and all copy strings).**

Empirical reality after this task: the `SOURCE_CATALOG` in `engine.ts` will contain **7 distinct providers**: `worldbank`, `vdem`, `gpi`, `inform`, `reliefweb`, `gdacs`, `advisories`. There is no ACLED in the codebase (verified via `grep -rn "acled\|ACLED" src/` returns zero hits — do not invent one).

Therefore the locked source-count claim everywhere in user-visible copy is **"7 sources"**, and the enumeration must list exactly these 7 names: **World Bank, V-Dem, INFORM, GPI, ReliefWeb, GDACS, government travel advisories**.

This OVERRIDES the earlier "bump 7 → 8" framing that appeared in pre-revision drafts. Reasoning: pre-revision the count was "7+" (loose) covering 6 catalog entries; post-revision the count is exactly 7 covering 7 catalog entries — so the number on the page stays at "7" but it now reflects a tightened/enumerated rule rather than a loose "+". This eliminates the prior internal inconsistency where `methodology.tiered_text` enumerated only 3 baseline + 3 signal = 6 named entries while `about.project_text` claimed "7+" / `about.sources_text` claimed "9+".

Affected strings that must enumerate exactly the 7 sources by name (or, where the locale traditionally uses a brief mention, must at minimum name V-Dem alongside the existing names):
- `methodology.tiered_text` — add "V-Dem" to the baseline group: "(World Bank, V-Dem, INFORM, GPI)".
- `about.project_text` — replace any "7+" / "more than 7" / "9+" framing with "7" and ensure the named list contains V-Dem.
- `about.sources_text` — replace "9+" / "7+" with "7" and align with the methodology phrasing.
</source_count_rule>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260511-k2m-replace-retired-wb-wgi-with-v-dem-instit/260511-k2m-RESEARCH.md
@src/pipeline/fetchers/worldbank.ts
@src/pipeline/config/weights.json
@src/pipeline/scoring/normalize.ts
@scripts/backfill-historical.ts

<interfaces>
Key contracts the executor must respect (extracted from current HEAD):

From `src/pipeline/types.ts` (used by every fetcher):
```typescript
// RawIndicator { countryIso3: string; indicatorName: string; value: number; year: number; source: string; dataDate?: string }
// RawSourceData { source: string; fetchedAt: string; indicators: RawIndicator[] }
// FetchResult { source: string; success: boolean; countriesFound: number; error?: string; fetchedAt: string }
```

From `src/pipeline/fetchers/worldbank.ts` (template the new fetcher mirrors — same envelope, same partial-failure cache fallback, no papaparse):
- `fetchWorldBank(date: string): Promise<FetchResult>` — at lines 87-190
- Per-indicator helper pattern: group by country, keep latest year <= targetYear (worldbank.ts:53-71)
- Cache fallback: `findLatestCached('worldbank-parsed.json')` then `readJson<RawSourceData>` (worldbank.ts:121-133, 165-180)

From `src/pipeline/scoring/normalize.ts:28` — INDICATOR_RANGES dictionary signature:
```typescript
Record<string, { min: number; max: number; inverse: boolean }>
```
`inverse: true` means lower raw = safer (uses `normalizeInverse = 1 - normalize(value, min, max)` at normalize.ts:16-18). For V-Dem corruption (`v2x_corr`: 0 = clean, 1 = corrupt), use `{ min: 0, max: 1, inverse: true }` — do NOT invert in the fetcher (would double-invert).

From `src/pipeline/scoring/engine.ts:49` — DO NOT CHANGE:
```typescript
export const MIN_PILLAR_COVERAGE = 0.30;
```

From `src/pipeline/config/source-tiers.json:5` — pattern for the new entry:
```json
"worldbank": { "tier": "baseline", "maxAgeDays": 730, "decayHalfLifeDays": 365 }
```
</interfaces>

<verified_urls>
HTTP 200 confirmed live in RESEARCH.md (2026-05-11). Append `?v=1&csvType=full&useColumnShortNames=false`:
- `https://ourworldindata.org/grapher/rule-of-law-index.csv` -> indicator `vdem_rule_of_law`, range 0..1, inverse=false
- `https://ourworldindata.org/grapher/political-corruption-index.csv` -> indicator `vdem_corruption_control`, range 0..1, inverse=true
- `https://ourworldindata.org/grapher/rigorous-and-impartial-public-administration-score.csv` -> indicator `vdem_gov_effectiveness`, range -3.5..3.5, inverse=false
</verified_urls>

<conflict_pillar_redistribution>
Canonical post-redistribution weights for `weights.json` -> `conflict.indicatorWeights` (sum MUST equal 1.0 after rounding; absorb any rounding residue into `advisory_level_us` so the JSON parser-side sum is exactly 1.0):

GPI (absorbs 0.12 of the 0.17 freed):
- `gpi_overall`: 0.22  (was 0.17, +0.05)
- `gpi_safety_security`: 0.20  (was 0.16, +0.04)
- `gpi_militarisation`: 0.17  (was 0.14, +0.03)

Advisories (absorb 0.05; scale factor 1.1389x = 0.41 / 0.36):
- us 0.026 -> 0.0296, uk 0.025 -> 0.0285, ca 0.025 -> 0.0285, au 0.025 -> 0.0285
- de/nl/jp/sk 0.018 -> 0.0205 (each)
- fr/nz/ie/fi/hk/br/at/ph 0.010 -> 0.0114 (each)
- be/dk/sg/ro/rs/ee/hr/ar 0.007 -> 0.0080 (each)
- it/es/kr/tw/cn/in 0.005 -> 0.0057 (each)
- ch/se/no/pl/cz/hu/pt 0.003 -> 0.0034 (each)

Naive rounded sum = 0.59 (GPI) + 0.4103 (adv) = 1.0003. Trim the +0.0003 from `advisory_level_us` (final us = 0.0293) so the persisted sum is exactly 1.0000. The Task-2 verify command will assert this with `jq`.

The current `crime.indicators` keeps only `vdem_rule_of_law` (no indicatorWeights block needed — single indicator).
The current `governance.indicators` becomes `["vdem_gov_effectiveness", "vdem_corruption_control", "inform_governance"]` (no indicatorWeights -> equal-weights fallback, unchanged behavior).
</conflict_pillar_redistribution>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: V-Dem fetcher + normalize.ts ranges + tier registration</name>
  <files>src/pipeline/fetchers/vdem.ts, src/pipeline/fetchers/index.ts, src/pipeline/scoring/normalize.ts, src/pipeline/config/source-tiers.json, src/pipeline/scoring/__tests__/vdem.test.ts, .gitignore</files>
  <behavior>
    - vdem.test.ts: given a fake OWID CSV string with header `Entity,Code,Year,IndicatorValue,World region` and rows including (a) a normal country row like `Australia,AUS,2025,3.324`, (b) an OWID aggregate row like `Africa (population-weighted),OWID_AFR,2025,-0.6`, (c) an empty-Code row like `Africa,,2025,-0.5`, (d) a row with an ISO3 not in countries.ts like `Atlantis,ATL,2025,0.5`, (e) two years for the same country (`USA,2022,0.7` and `USA,2024,0.8`), and (f) a row with non-numeric value — the parser must keep only (a) and pick the latest year per country in (e), filter (b)(c)(d)(f).
    - vdem.test.ts: normalize.ts INDICATOR_RANGES must contain `vdem_rule_of_law`, `vdem_gov_effectiveness`, `vdem_corruption_control` with exact bounds from RESEARCH.md table; must NOT contain `wb_political_stability`, `wb_rule_of_law`, `wb_gov_effectiveness`, `wb_corruption_control`.
    - vdem.test.ts: normalizeIndicators of `{indicatorName:'vdem_corruption_control', value: 0.0}` produces normalizedValue=1.0 (clean -> safe) and `value: 1.0` produces normalizedValue=0.0 (corrupt -> unsafe), confirming `inverse:true` path is correct and NOT double-inverted.
    - vdem.test.ts case (g) — **V-Dem year boundary (added per checker W7):** Given a fake CSV with only one country `USA,USA,2025,0.8` (no 2026 row), and `parseVdemCsv(text, 2026, 'vdem_rule_of_law')` is called (i.e., we are in calendar year 2026 but V-Dem v16's latest year is 2025), the parser must emit one RawIndicator with `year: 2025` and `value: 0.8`. Then in the backfill verify (Task 3), assert that every `data/scores/2026-*.json` snapshot's vdem indicator records reference V-Dem `year: 2025` (use `jq '.. | objects | select(.source=="vdem") | .year' data/scores/2026-05-11.json | sort -u` and verify only `2025` appears).
  </behavior>
  <action>
    Create `src/pipeline/fetchers/vdem.ts` modeled on `src/pipeline/fetchers/worldbank.ts:87-190`:
    - Module-level INDICATORS array of three entries: `{ slug: 'rule-of-law-index', name: 'vdem_rule_of_law' }`, `{ slug: 'political-corruption-index', name: 'vdem_corruption_control' }`, `{ slug: 'rigorous-and-impartial-public-administration-score', name: 'vdem_gov_effectiveness' }`.
    - Helper `fetchIndicator(slug, name, targetYear)`: GET `https://ourworldindata.org/grapher/${slug}.csv?v=1&csvType=full&useColumnShortNames=false` with `signal: AbortSignal.timeout(60_000)` and `Accept: text/csv` header.
    - Parse CSV by splitting on `\n`, skipping the header line, then per-line `line.split(',')`. Use `parts[1]` for Code, `parts[2]` for Year, `parts[3]` for Value (matches research note F — never use parts[0] because Entity may rarely contain commas in future CSV revisions).
    - Filter rule (mirrors worldbank.ts:61-62): skip row if Code is empty, starts with `OWID_`, fails `getCountryByIso3(code)`, or Value is not `Number.isFinite()`.
    - Group by ISO3 and keep latest year <= targetYear (same algorithm as worldbank.ts:67-71). For targetYear=2026 and V-Dem v16 max year=2025, the algorithm naturally picks 2025 (covers case (g) in behavior).
    - Emit `RawIndicator[]` with `source: 'vdem'`.
    - Top-level `fetchVdem(date: string): Promise<FetchResult>` mirrors `fetchWorldBank` exactly: `Promise.allSettled` over 3 indicators, partial-failure cache fallback via `findLatestCached('vdem-parsed.json')`, writes `data/raw/{date}/vdem-parsed.json`, returns `FetchResult` with `source: 'vdem'`.
    - Do NOT cache the raw CSV string to disk. Do NOT depend on papaparse. No comment-only "wb_*" leftover references.
    Wire into `src/pipeline/fetchers/index.ts`:
    - Add `import { fetchVdem } from './vdem.js';` after the worldbank import (around line 2-3).
    - Add `export { fetchVdem } from './vdem.js';` next to the worldbank re-export (around line 15).
    - In `fetchAllSources` Batch 1 (the `settleBatch([...])` array starting around line 83-89), add `{ name: 'vdem', fn: () => fetchVdem(date) }` immediately after the `worldbank` entry. Update the Batch 1 console.log label to include `vdem`.
    Modify `src/pipeline/scoring/normalize.ts`:
    - DELETE lines 28-33 entries: `wb_political_stability`, `wb_rule_of_law`, `wb_gov_effectiveness`, `wb_corruption_control`.
    - KEEP `wb_child_mortality` and `wb_air_pollution`.
    - ADD three new entries directly above `wb_child_mortality`:
      - `vdem_rule_of_law: { min: 0, max: 1, inverse: false }`
      - `vdem_gov_effectiveness: { min: -3.5, max: 3.5, inverse: false }`
      - `vdem_corruption_control: { min: 0, max: 1, inverse: true }`
    - Update the section comment (currently "// World Bank Worldwide Governance Indicators (higher = better)") to "// V-Dem v16 governance indicators (CC-BY-SA, fetched from OWID grapher CSVs)" and keep the next sub-section "// World Bank health & environment (lower = better)".
    Modify `src/pipeline/config/source-tiers.json`:
    - Add a new key `"vdem": { "tier": "baseline", "maxAgeDays": 730, "decayHalfLifeDays": 365 }` immediately after the `"worldbank"` line (line 5), formatted to match surrounding indentation.
    Modify `.gitignore`:
    - After existing `data/raw/**/*.partial` line (line 35), append `data/raw/**/*.csv` to prevent any accidental raw CSV commits. (Defensive — current implementation does not write CSV, but this future-proofs the rule.)
    Create `src/pipeline/scoring/__tests__/vdem.test.ts`:
    - Use `node:test` + `node:assert` (match the style of `engine.test.ts`).
    - Stub the parser by exporting a `parseVdemCsv(text: string, targetYear: number, indicatorName: string): RawIndicator[]` helper from vdem.ts; assert the 7 cases in `<behavior>` (a)-(g).
    - Add an INDICATOR_RANGES dictionary contract test that imports the (file-private) ranges via re-export — if normalize.ts does not currently re-export INDICATOR_RANGES, add a named export `export const __INDICATOR_RANGES_FOR_TESTS = INDICATOR_RANGES;` at the bottom of normalize.ts and use that. Verify the three vdem_* keys exist, the four wb_* WGI keys are absent, and a direct `normalizeIndicators` call on a `vdem_corruption_control` raw of 0.0 yields 1.0 (not 0.0 — confirms single-inversion).
    Add an `npm script` entry `"test:vdem": "node --import tsx --test src/pipeline/scoring/__tests__/vdem.test.ts"` is OPTIONAL; the verify step runs the test file directly.
  </action>
  <verify>
    <automated>node --import tsx --test src/pipeline/scoring/__tests__/vdem.test.ts &amp;&amp; npx tsc --noEmit -p . &amp;&amp; ! grep -v '^\s*//' src/pipeline/scoring/normalize.ts | grep -qE 'wb_political_stability|wb_rule_of_law|wb_gov_effectiveness|wb_corruption_control' &amp;&amp; [ "$(grep -v '^\s*//' src/pipeline/scoring/normalize.ts | grep -cE 'vdem_rule_of_law|vdem_gov_effectiveness|vdem_corruption_control')" = "3" ] &amp;&amp; jq -e '.sources.vdem.tier == "baseline"' src/pipeline/config/source-tiers.json</automated>
  </verify>
  <done>
    vdem.ts exports `fetchVdem` and `parseVdemCsv`; the vdem test file passes the 7 parsing cases (a-g) + range contract + corruption single-inversion check + year-boundary case (g); `npx tsc --noEmit -p .` is green; normalize.ts contains 3 vdem_* entries and zero retired wb_* WGI entries; source-tiers.json has the vdem baseline entry; .gitignore contains `data/raw/**/*.csv`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: weights.json restructure + engine.ts indicator/source maps + INDICATOR_SOURCE_MAP test</name>
  <files>src/pipeline/config/weights.json, src/pipeline/scoring/engine.ts, src/pipeline/scoring/__tests__/engine.test.ts</files>
  <behavior>
    - engine.test.ts: weights.json parses to a structure where `pillars.find(p => p.name === 'conflict').indicators` does NOT contain `wb_political_stability` and contains all 37 advisory_level_* entries + the three gpi_* entries.
    - engine.test.ts: `pillars.find(p => p.name === 'conflict').indicatorWeights` has values that sum to exactly 1.0 within 1e-9 tolerance.
    - engine.test.ts: `pillars.find(p => p.name === 'crime').indicators` equals `['vdem_rule_of_law']` exactly.
    - engine.test.ts: `pillars.find(p => p.name === 'governance').indicators` equals `['vdem_gov_effectiveness', 'vdem_corruption_control', 'inform_governance']` (order tolerated; set-equality is fine).
    - engine.test.ts: `weights.json.version === '8.1.0'`.
    - engine.test.ts: INDICATOR_SOURCE_MAP from engine.ts has keys `vdem_rule_of_law`, `vdem_gov_effectiveness`, `vdem_corruption_control` all mapped to `'vdem'`, and does NOT have any of the four retired wb_* WGI keys.
    - engine.test.ts: SOURCE_CATALOG has a `vdem` entry whose `url` includes `v-dem.net` and whose `description` mentions `CC-BY-SA` AND `Coppedge` AND `10.23696/vdemds26` (DOI).
  </behavior>
  <action>
    Modify `src/pipeline/config/weights.json`:
    - Bump `"version"` from `"8.0.0"` to `"8.1.0"`.
    - In the `conflict` pillar:
      - Remove `"wb_political_stability"` from the `indicators` array.
      - Remove the `"wb_political_stability": 0.17` line from `indicatorWeights`.
      - Update `gpi_overall: 0.17 -> 0.22`, `gpi_safety_security: 0.16 -> 0.20`, `gpi_militarisation: 0.14 -> 0.17`.
      - Update every advisory_level_* weight to the redistributed values listed in `<conflict_pillar_redistribution>` above. Final `advisory_level_us` must be `0.0293` (not `0.0296`) to compensate for cumulative rounding so the dictionary sums to exactly `1.0000` when parsed by `JSON.parse`.
    - In the `crime` pillar: replace the `indicators` value `["wb_rule_of_law"]` with `["vdem_rule_of_law"]`.
    - In the `governance` pillar: replace `["wb_gov_effectiveness", "wb_corruption_control", "inform_governance"]` with `["vdem_gov_effectiveness", "vdem_corruption_control", "inform_governance"]`.
    - Do NOT touch the `health` or `environment` pillars (they keep `wb_child_mortality` and `wb_air_pollution`).
    Modify `src/pipeline/scoring/engine.ts` (the `INDICATOR_SOURCE_MAP` at line 229-286):
    - Delete the four lines: `wb_political_stability:`, `wb_rule_of_law:`, `wb_gov_effectiveness:`, `wb_corruption_control:`.
    - Add three new entries grouped together (after the remaining `wb_air_pollution` line at line 235): `vdem_rule_of_law: 'vdem',`, `vdem_gov_effectiveness: 'vdem',`, `vdem_corruption_control: 'vdem',`.
    Modify `src/pipeline/scoring/engine.ts` (the `SOURCE_CATALOG` at line 382-407):
    - Add a new entry between `worldbank` and `advisories`:
      ```json
      vdem: {
        url: 'https://www.v-dem.net/data/the-v-dem-dataset/',
        description: 'V-Dem Institute Country-Year Dataset v16 (CC-BY-SA 4.0) -- Rule of Law, Political Corruption, and Public Administration indices (Coppedge et al., DOI 10.23696/vdemds26)',
      },
      ```
    - Update the `worldbank` description if it still mentions "governance" — change to: `'World Bank Development Indicators -- child mortality and PM2.5 air pollution'` (governance moved to vdem).
    Extend `src/pipeline/scoring/__tests__/engine.test.ts` with the 7 assertions in `<behavior>` above. Use `JSON.parse(readFileSync(...))` for the weights.json checks. Import `INDICATOR_SOURCE_MAP` and `SOURCE_CATALOG` from engine.ts — if not already exported, add `export` to their declarations.
  </action>
  <verify>
    <automated>node --import tsx --test src/pipeline/scoring/__tests__/engine.test.ts &amp;&amp; jq -e '.version == "8.1.0"' src/pipeline/config/weights.json &amp;&amp; jq -e '[.pillars[]|select(.name=="conflict").indicatorWeights|values[]]|add | (.*1e9|round)/1e9 == 1.0' src/pipeline/config/weights.json &amp;&amp; jq -e '.pillars[]|select(.name=="conflict").indicators|index("wb_political_stability") == null' src/pipeline/config/weights.json &amp;&amp; jq -e '.pillars[]|select(.name=="crime").indicators == ["vdem_rule_of_law"]' src/pipeline/config/weights.json &amp;&amp; ! grep -qE "wb_political_stability|wb_rule_of_law|wb_gov_effectiveness|wb_corruption_control" src/pipeline/scoring/engine.ts &amp;&amp; grep -q "Coppedge" src/pipeline/scoring/engine.ts &amp;&amp; grep -q "10.23696/vdemds26" src/pipeline/scoring/engine.ts &amp;&amp; grep -q "CC-BY-SA" src/pipeline/scoring/engine.ts</automated>
  </verify>
  <done>
    weights.json version is 8.1.0, conflict.indicatorWeights sums to 1.0 exactly, no `wb_political_stability` / WGI references remain anywhere in engine.ts or weights.json. engine.test.ts passes the 7 new assertions plus the pre-existing tests. INDICATOR_SOURCE_MAP has the 3 vdem_* entries, SOURCE_CATALOG has the vdem entry with CC-BY-SA + Coppedge + DOI 10.23696/vdemds26 in description.
  </done>
</task>

<task type="auto">
  <name>Task 3: Historical backfill script + execute full rescore (2012-03-19 -> 2026-05-11, 611 snapshots)</name>
  <files>scripts/backfill-vdem-rescore.ts, data/raw/{year}/vdem-parsed.json (generated), data/raw/{date}/vdem-parsed.json (copied), data/scores/*.json (rescored), data/scores/history-index.json (regenerated), public/scores.json (regenerated by pipeline)</files>
  <action>
    Create `scripts/backfill-vdem-rescore.ts` modeled on `scripts/backfill-historical.ts` with these specific behaviors (do NOT replace the existing backfill-historical.ts):

    Step A — Validate fetcher end-to-end against today (2026-05-11):
    - First action of the script: call `runPipeline('2026-05-11')`. This invokes the modified Batch 1 which now includes `fetchVdem`. Confirm `data/raw/2026-05-11/vdem-parsed.json` exists afterward and contains a `vdem_rule_of_law` indicator for `USA`. If missing, abort with a clear error.

    Step B — Year-grouped V-Dem fetch (idempotent):
    - Enumerate all `data/scores/*.json` matching `/^\d{4}-\d{2}-\d{2}\.json$/` (expect **611**).
    - Group by year. For each year Y in 2012..2025 with snapshots:
      - Pick the first snapshot date of that year `firstDate`.
      - If `data/raw/{firstDate}/vdem-parsed.json` does not exist OR contains data with `year > Y` (sanity check), invoke a year-targeted fetch by temporarily calling `fetchVdem(firstDate)` with the snapshot's date — the fetcher's `targetYear = parseInt(date.slice(0,4))` logic at vdem.ts (mirrors worldbank.ts:91) will pick the right V-Dem year automatically. Persist to `data/raw/{firstDate}/vdem-parsed.json`.
      - Copy that file to every other snapshot date in the same year: `for date in yearDates[1:]: copyFileSync(data/raw/firstDate/vdem-parsed.json, data/raw/date/vdem-parsed.json)`.
    - For 2026 snapshots, use the 2026-05-11 fetch from Step A (V-Dem returns its latest 2025 data — verifies the Task 1 case (g) year-boundary behavior at runtime).
    - Idempotency rule: copying an existing identical file is allowed; do NOT delete other `*-parsed.json` files in the snapshot dir (we are only adding/refreshing vdem-parsed.json).

    Step C — Rescore each snapshot **(resumable per checker W5):**
    - For every snapshot date D (chronological):
      - **Resume check (added per checker W5):** Read `data/scores/{D}.json` if it exists. If its top-level `weightsVersion` field already equals `"8.1.0"`, log `SKIP: {D} already at 8.1.0` and continue to the next date. This makes the script resumable: on retry after a mid-run failure, it skips already-completed snapshots and only re-runs failures / unprocessed dates. (If `weightsVersion` field doesn't exist in the current snapshot schema, the rescore engine writes it as part of the standard score envelope — check `src/pipeline/scoring/engine.ts` / the snapshot writer to confirm; if not present, ADD `weightsVersion: weights.version` to the written envelope so this resume gate works.)
      - Otherwise call `runPipeline(D)`. The pipeline detects existing `*-parsed.json` files and skips fetches (this is how `backfill-historical.ts` works today — confirm by reading `src/pipeline/run.ts`). With `vdem-parsed.json` now present for every D, the rescore picks up the new indicator and writes a fresh `data/scores/D.json` with `weightsVersion: "8.1.0"`.
    - Print progress every 25 snapshots; on error, log the date and continue (do not abort the whole run — a single bad snapshot must not lose 610 others).

    Step D — Regenerate the history index:
    - At the end, call `writeHistoryIndex()` from `src/pipeline/scoring/history.js`.
    - Print a final summary line: `Rescored {N}/611 snapshots, skipped {S} already-8.1.0, failed {M}, took {seconds}s`.

    Sanity gate (run AFTER backfill, as part of the verify step):
    - Spot-check Algeria (DZA), United States (USA), Japan (JPN), Norway (NOR), Brazil (BRA), India (IND) in `public/scores.json` (the live current-snapshot file): each must have a non-null `crime` pillar AND a non-null `governance` pillar, each with `dataCompleteness >= 0.30`.

    Runtime expectation (state in the script's top comment): ~70s for fetch step + ~30-50 minutes for 611 rescores = total wall time ~30-55 min. Single-threaded only (the snapshot writer is not concurrency-safe per RESEARCH.md §4). On resume (second run with all snapshots already at 8.1.0), runtime drops to ~10s (just the Step A live fetch + 611 skip-log iterations).

    Idempotency proof: running the script a second time after a successful first run must produce zero diff in `data/scores/*.json` (verified by the weightsVersion skip path + manually with `git status` after the second run).

    Document the run command in the script's docstring:
    ```
    # First run (full backfill):
    npx tsx scripts/backfill-vdem-rescore.ts

    # Re-run (idempotent, fast — skips snapshots already at weightsVersion=8.1.0):
    npx tsx scripts/backfill-vdem-rescore.ts

    # Force full re-rescore (bypass skip — delete weightsVersion field or manually edit script):
    # (Not needed in normal flow; only for debugging.)
    ```
    Execute the script once as part of this task. Then `git add data/scores/*.json data/scores/history-index.json data/raw/*/vdem-parsed.json public/scores.json` (the rescored snapshots are intentionally committed because this project deploys static scored data).
  </action>
  <verify>
    <automated>test -f scripts/backfill-vdem-rescore.ts &amp;&amp; npx tsx scripts/backfill-vdem-rescore.ts &amp;&amp; test -f data/raw/2026-05-11/vdem-parsed.json &amp;&amp; test -f data/scores/history-index.json &amp;&amp; [ "$(ls data/scores/*.json | grep -E '/[0-9]{4}-[0-9]{2}-[0-9]{2}\.json$' | wc -l | tr -d ' ')" = "611" ] &amp;&amp; ! grep -l "wb_political_stability\|wb_rule_of_law\|wb_gov_effectiveness\|wb_corruption_control" data/scores/*.json | head -1 &amp;&amp; for iso in DZA USA JPN NOR BRA IND; do jq -e ".countries[]|select(.iso3==\"$iso\")|.pillars.crime.score != null and .pillars.governance.score != null and .pillars.crime.dataCompleteness >= 0.30 and .pillars.governance.dataCompleteness >= 0.30" public/scores.json || { echo "FAIL: $iso"; exit 1; }; done &amp;&amp; YEARS_2026=$(jq -r '.. | objects | select(.source=="vdem"?) | .year' data/scores/2026-05-11.json 2>/dev/null | sort -u | tr '\n' ',') &amp;&amp; echo "2026-05-11 vdem years referenced: $YEARS_2026"</automated>
  </verify>
  <done>
    - scripts/backfill-vdem-rescore.ts exists and is idempotent **with weightsVersion resume gate**.
    - Running once produces 611 rescored snapshots in data/scores/ + regenerated history-index.json.
    - Zero data/scores/*.json files contain any of the four retired WGI indicator names.
    - public/scores.json has DZA, USA, JPN, NOR, BRA, IND each with non-null crime + governance pillars at >= 30% coverage.
    - The script's docstring documents the run command and expected ~30-55 min initial runtime + ~10s resume runtime.
    - 2026-* snapshots reference V-Dem `year: 2025` exclusively (year-boundary verified).
  </done>
</task>

<task type="auto">
  <name>Task 4: UI updates — methodology + sources pages + source enumeration across all 7 locales (V-Dem added to the 7-source list)</name>
  <files>src/pages/en/sources/index.astro, src/pages/it/fonti/index.astro, src/pages/pt/fontes/index.astro, src/pages/es/fuentes/index.astro, src/pages/fr/sources/index.astro, src/pages/de/quellen/index.astro, src/pages/zh/sources/index.astro, src/i18n/ui.ts</files>
  <action>
    **READ `<source_count_rule>` AT THE TOP OF THIS PLAN BEFORE EDITING.** The locked rule: enumerate exactly **7 sources** by name (World Bank, V-Dem, INFORM, GPI, ReliefWeb, GDACS, government travel advisories) across all locales. The pre-revision draft's "7 → 8" bump is REVERTED — we keep "7" but tighten the enumeration so it matches the empirical 7-entry SOURCE_CATALOG.

    Sources pages — for each of the 7 source pages, replace the entry at line 57 (en/it/pt/es/fr/zh) and line 59 (de):
    - Current: `{ name: 'World Bank Governance Indicators', measures: 'Political Stability, Rule of Law, Government Effectiveness, Corruption Control', frequency: 'Annual', url: 'https://info.worldbank.org/governance/wgi/' }`
    - Replace with: `{ name: 'V-Dem Institute (v16)', measures: 'Rule of Law, Political Corruption, Public Administration Quality', frequency: 'Annual', url: 'https://www.v-dem.net/data/the-v-dem-dataset/', citation: 'Coppedge et al. — V-Dem Country-Year Dataset v16, DOI 10.23696/vdemds26, CC-BY-SA 4.0' }`
    - Keep the surrounding WB entries for `wb_child_mortality` and `wb_air_pollution` if present (they are still active — verify by reading each source page; do not delete them).
    - If any of the 7 source pages list "World Bank" as a single combined entry (no separate child mortality / air pollution entries), split it into: (a) the new V-Dem entry above, (b) keep "World Bank Development Indicators" with measures `'Child Mortality, PM2.5 Air Pollution'` (the still-live WB metrics).
    - At least the `src/pages/en/sources/index.astro` page MUST render visible CC-BY-SA attribution text (e.g. include the `citation` field in the source-row template, or add a footer paragraph: "V-Dem data: Coppedge et al., V-Dem Institute v16 (DOI 10.23696/vdemds26, CC-BY-SA 4.0)."). The verify step greps for `CC-BY-SA` in this file.

    `src/i18n/ui.ts` — apply the same conceptual edit in every locale (en/it/es/fr/pt/zh/de). Concrete edits:

    For each locale block, find and replace these 4 indicator label keys (line numbers below are for `en`; the same keys exist in each locale at offsets +477 (it), +952 (es), +1427 (fr), +1902 (pt), +2374 (zh), +2845 (de) — verify by grep before editing):
    - `methodology.indicator.wb_political_stability: '...'` -> **delete the entire line** (indicator removed).
    - `methodology.indicator.wb_rule_of_law: '...'` -> rename key to `methodology.indicator.vdem_rule_of_law` and change the label from "World Bank ..." to "V-Dem Rule of Law Index" (en) and equivalent localized versions for the other 6 locales (use the existing translation as a guide — replace "World Bank" with "V-Dem"; e.g. it: `'Indice dello Stato di Diritto V-Dem'`, de: `'V-Dem Rechtsstaatlichkeitsindex'`, zh: `'V-Dem 法治指数'`).
    - `methodology.indicator.wb_gov_effectiveness: '...'` -> rename to `methodology.indicator.vdem_gov_effectiveness`, label "V-Dem Public Administration Quality" (and localized equivalents).
    - `methodology.indicator.wb_corruption_control: '...'` -> rename to `methodology.indicator.vdem_corruption_control`, label "V-Dem Political Corruption (inverted)" (and localized equivalents).

    For the `methodology.source.wb` key (en line 225 and same offsets across locales): rename to `methodology.source.vdem` and change the value from "World Bank Worldwide Governance Indicators" to "V-Dem Institute v16 — Coppedge et al., CC-BY-SA 4.0, DOI 10.23696/vdemds26". (If both keys are useful — i.e. there is also a `methodology.source.wb` entry that should remain to describe WB's child mortality / air pollution role — instead add a new sibling key `methodology.source.vdem` and rewrite `methodology.source.wb` to `'World Bank Development Indicators'`. Grep for usages first; if `methodology.source.wb` is only consumed in a single Astro page that was about governance, the rename is correct.)

    For each `methodology.pillar.conflict.sources` string (en line 338 + 6 locale equivalents): replace the leading mention of "World Bank Political Stability Index" with "Global Peace Index" (just drop the WB clause — political_stability is gone). Keep the existing GPI + advisories tail intact.

    For each `methodology.pillar.crime.sources` string (en line 343 + 6 locale equivalents): replace "World Bank Rule of Law Index" with "V-Dem Rule of Law Index (Coppedge et al., V-Dem Institute v16, CC-BY-SA 4.0)" — localized where needed (the citation phrasing stays in English; only the leading label translates).

    For each `methodology.pillar.governance.sources` string (en line 353 + 6 locale equivalents): replace "World Bank Government Effectiveness, World Bank Control of Corruption" with "V-Dem Public Administration Quality, V-Dem Political Corruption Index (inverted)" (localized leading nouns; "V-Dem" stays as-is).

    For each `methodology.tiered_text` (en line 162 + 6 locale equivalents): replace the parenthetical baseline list to enumerate **exactly 4 baseline sources** including V-Dem. English: `(World Bank, V-Dem, INFORM, GPI)`. Italian: `(Banca Mondiale, V-Dem, INFORM, GPI)`. Spanish: `(Banco Mundial, V-Dem, INFORM, GPI)`. French: `(Banque Mondiale, V-Dem, INFORM, GPI)`. Portuguese: `(Banco Mundial, V-Dem, INFORM, GPI)`. Chinese: `（世界银行、V-Dem、INFORM、GPI）`. German: `(Weltbank, V-Dem, INFORM, GPI)`. The signal-tier list (`ReliefWeb, GDACS, 37 government travel advisories`) stays unchanged — already 3 entries. **Total enumerated providers across both tiers = 4 + 3 = 7, matching the locked rule.**

    For each `methodology.inform_text` (en line 222 + 6 locale equivalents): replace "World Bank governance data" with "V-Dem governance data" since the WB no longer provides our governance indicators.

    **Source count harmonization (revised per checker B2 — single rule: enumerate 7 sources):**
    - `about.project_text` and `about.sources_text` strings: every loose count phrasing must be replaced with **exactly "7"** and the named-source list inside the paragraph must reference V-Dem alongside the existing names. Specific edits per locale:
      - **en (lines ~386, ~392):** `about.project_text` says "from **7+** trusted public sources including the Global Peace Index, INFORM Risk Index, ReliefWeb, GDACS, and government travel advisories" — change to "from **7** trusted public sources: World Bank, V-Dem Institute (v16), INFORM Risk Index, Global Peace Index, ReliefWeb, GDACS, and government travel advisories from the US, UK, Canada, and Australia". `about.sources_text` says "from **9+** public sources" — change to "from **7** public sources".
      - **it (lines ~863, ~869):** `about.project_text` "oltre 7 fonti pubbliche affidabili tra cui..." — change to "7 fonti pubbliche affidabili: Banca Mondiale, V-Dem Institute (v16), INFORM, Global Peace Index, ReliefWeb, GDACS, e gli avvisi di viaggio governativi di USA, UK, Canada e Australia". `about.sources_text` "oltre 7 fonti pubbliche" — change to "7 fonti pubbliche".
      - **es (lines ~1338, ~1344):** `about.project_text` "mas de 7 fuentes publicas confiables..." — change to "7 fuentes publicas confiables: Banco Mundial, V-Dem Institute (v16), INFORM, Global Peace Index, ReliefWeb, GDACS y avisos de viaje gubernamentales de EE.UU., Reino Unido, Canada y Australia". `about.sources_text` "mas de 7 fuentes publicas" — change to "7 fuentes publicas".
      - **fr (lines ~1813, ~1819):** `about.project_text` "plus de 7 sources publiques fiables..." — change to "7 sources publiques fiables: Banque Mondiale, V-Dem Institute (v16), INFORM, Global Peace Index, ReliefWeb, GDACS et les avis de voyage gouvernementaux des USA, du Royaume-Uni, du Canada et de l'Australie". `about.sources_text` "plus de 7 sources publiques" — change to "7 sources publiques".
      - **pt (lines ~2288, ~2294):** `about.project_text` "mais de 7 fontes publicas confiaveis..." — change to "7 fontes publicas confiaveis: Banco Mundial, V-Dem Institute (v16), INFORM, Global Peace Index, ReliefWeb, GDACS e avisos de viagem governamentais dos EUA, Reino Unido, Canada e Australia". `about.sources_text` "mais de 7 fontes publicas" — change to "7 fontes publicas".
      - **zh (lines ~2759, ~2765):** `about.project_text` `7+ 个可信公开来源` — change to `7 个可信公开来源：世界银行、V-Dem Institute (v16)、INFORM 风险指数、全球和平指数、ReliefWeb、GDACS 以及来自美国、英国、加拿大和澳大利亚的政府旅行建议`. `about.sources_text` `9+ 个公开来源` — change to `7 个公开来源`.
      - **de (lines ~3230, ~3236):** `about.project_text` `7+ vertrauenswürdigen öffentlichen Quellen` — change to `7 vertrauenswürdigen öffentlichen Quellen: Weltbank, V-Dem Institute (v16), INFORM-Risikoindex, Globaler Friedensindex, ReliefWeb, GDACS sowie staatliche Reisehinweise aus den USA, dem Vereinigten Königreich, Kanada und Australien`. `about.sources_text` `9+ täglich aktualisierten öffentlichen Quellen` — change to `7 täglich aktualisierten öffentlichen Quellen`.
    - Confirm afterwards: zero loose-count phrasings survive. Re-grep negatives (verify uses `! grep -qE`).
    - Confirm afterwards: V-Dem appears in every locale's `about.project_text` (verify uses `grep -c "V-Dem"`).
    - If any pillar-description copy or `methodology.source.list` style enumeration mentions the source list explicitly with WGI, update it too.

    Do NOT touch the source name "World Bank" anywhere it still refers to wb_child_mortality / wb_air_pollution (those endpoints are still live).
  </action>
  <verify>
    <automated>! grep -qE "wb_political_stability|methodology\.indicator\.wb_rule_of_law|methodology\.indicator\.wb_gov_effectiveness|methodology\.indicator\.wb_corruption_control" src/i18n/ui.ts &amp;&amp; ! grep -rqE "World Bank Governance Indicators|Worldwide Governance Indicators" src/pages src/components src/i18n &amp;&amp; [ "$(grep -cE "vdem_rule_of_law|vdem_gov_effectiveness|vdem_corruption_control" src/i18n/ui.ts)" -ge "21" ] &amp;&amp; ! grep -qE "7\+ |9\+ |7\+个|9\+个|mais de 7|plus de 7|oltre 7|mas de 7|8 sources|8 fontes|8 fonti|8 fuentes|8 个|8 Quellen" src/i18n/ui.ts &amp;&amp; [ "$(grep -lE "V-Dem|vdem" src/pages/en/sources/index.astro src/pages/it/fonti/index.astro src/pages/pt/fontes/index.astro src/pages/es/fuentes/index.astro src/pages/fr/sources/index.astro src/pages/de/quellen/index.astro src/pages/zh/sources/index.astro | wc -l | tr -d ' ')" = "7" ] &amp;&amp; [ "$(grep -c "V-Dem" src/i18n/ui.ts)" -ge "7" ] &amp;&amp; [ "$(grep -c "CC-BY-SA" src/pages/en/sources/index.astro)" -ge "1" ] &amp;&amp; grep -qE "Coppedge|V-Dem Institute|10\.23696/vdemds26" src/i18n/ui.ts &amp;&amp; for loc in en it es fr pt zh de; do grep -qE "World Bank|Banca Mondiale|Banco Mundial|Banque Mondiale|世界银行|Weltbank" src/i18n/ui.ts &amp;&amp; grep -qE "V-Dem" src/i18n/ui.ts &amp;&amp; grep -qE "INFORM" src/i18n/ui.ts &amp;&amp; grep -qE "GPI|Global Peace Index|Friedensindex|和平指数|Indice mondial de la paix|Indice di pace globale|Indice de paz global|Indice de paz mundial" src/i18n/ui.ts &amp;&amp; grep -qE "ReliefWeb" src/i18n/ui.ts &amp;&amp; grep -qE "GDACS" src/i18n/ui.ts &amp;&amp; grep -qE "advisor|avvis|aviso|avis|建议|Reisehinweis" src/i18n/ui.ts || { echo "FAIL: 7-source enumeration missing"; exit 1; }; done</automated>
  </verify>
  <done>
    - Zero references to retired WGI indicator names (wb_political_stability, wb_rule_of_law, wb_gov_effectiveness, wb_corruption_control as i18n keys) remain in src/i18n/ui.ts.
    - Zero references to "World Bank Governance Indicators" / "Worldwide Governance Indicators" remain in src/pages, src/components, src/i18n.
    - Every locale block in ui.ts contains 3 new vdem_* indicator i18n keys (7 locales × 3 indicators = 21+ occurrences).
    - All 7 source pages list V-Dem Institute (v16) as a separate source.
    - **Source-count copy harmonized: every locale's `about.project_text` enumerates the 7 sources (WB, V-Dem, INFORM, GPI, ReliefWeb, GDACS, advisories) by name, every `about.sources_text` uses "7" not "7+" / "9+", every `methodology.tiered_text` baseline parenthetical names exactly 4 sources including V-Dem.**
    - CC-BY-SA + Coppedge + DOI citation verifiable in `src/pages/en/sources/index.astro` AND in `src/i18n/ui.ts`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Final acceptance gate — tests, build, visual sanity</name>
  <what-built>
    Tasks 1-4 produced: new V-Dem fetcher with tests, weights.json v8.1.0 with redistributed conflict weights, 611 rescored historical snapshots, updated methodology+source UI in all 7 locales, source count harmonized to exactly 7 enumerated sources (WB, V-Dem, INFORM, GPI, ReliefWeb, GDACS, advisories), CC-BY-SA attribution visible on the English sources page. The full pipeline now uses V-Dem v16 governance indicators with no surviving WGI references.
  </what-built>
  <how-to-verify>
    Run these commands locally and confirm each is green:

    1. **Pipeline tests** — `npm run test:pipeline` (must include the new engine.test.ts assertions for weights.json + INDICATOR_SOURCE_MAP).
    2. **V-Dem unit tests** — `node --import tsx --test src/pipeline/scoring/__tests__/vdem.test.ts` (must include the 7 parse cases a-g, with case (g) confirming the 2026→2025 year-boundary fallback).
    3. **Static build** — `npm run build` (Astro build + OG generation + llms.txt + SEO validation). Must complete without errors.
    4. **Idempotency** — re-run `npx tsx scripts/backfill-vdem-rescore.ts` once more and then `git status data/scores/` — output must show zero modified files in `data/scores/` (proves idempotency via the weightsVersion=8.1.0 skip gate). Expected log output should include 611 `SKIP: {date} already at 8.1.0` lines.
    5. **Spot-check the live current snapshot** — open `dist/` after build and inspect or run:
       ```bash
       jq '.countries[] | select(.iso3 == "DZA") | {iso3, score, crime: .pillars.crime, governance: .pillars.governance}' public/scores.json
       ```
       Algeria should now have non-null crime + governance pillars with `dataCompleteness >= 0.30`. Repeat for USA, JPN, NOR, BRA, IND.
    6. **UI sanity check** — `npm run dev`, then visit:
       - `http://localhost:4321/en/methodology/` — Crime + Governance pillar descriptions reference V-Dem (not WB WGI). The `tiered_text` paragraph names exactly 4 baseline sources: World Bank, V-Dem, INFORM, GPI. About text says "7 sources" and enumerates them.
       - `http://localhost:4321/en/sources/` — V-Dem Institute (v16) appears as a separate source row. CC-BY-SA + Coppedge + DOI 10.23696/vdemds26 attribution is visible somewhere on the page.
       - `http://localhost:4321/zh/sources/` — same, in Chinese (count text reads `7 个公开来源`).
       - `http://localhost:4321/de/quellen/` — same, in German (count text reads `7 Quellen`).
       - Any country page (e.g. `/en/country/algeria/`) — Crime + Governance pillar bars render; no longer greyed out.
    7. **Grep gate** — final sweep:
       ```bash
       grep -rE "wb_political_stability|wb_rule_of_law|wb_gov_effectiveness|wb_corruption_control" \
         src/ data/scores/*.json public/scores.json \
         | grep -v "//.*\(deprecated\|removed\)" | head -5
       ```
       Should return zero matches.
    8. **Source-count consistency** — final sweep for stale count phrasings:
       ```bash
       ! grep -rqE "7\+ |9\+ |mais de 7|plus de 7|oltre 7|mas de 7|7\+个|9\+个|8 sources|8 个" src/i18n/ui.ts
       ```
       Should return zero matches (exit code 0 because of `!`).

    Type "approved" if all 8 checks pass. If any fails, describe the failure (which check, which file, what output) so the executor can repair.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found.</resume-signal>
</task>

</tasks>

<verification>
End-to-end gates:
- `npm run test:pipeline` — green (includes new vdem assertions in engine.test.ts).
- `node --import tsx --test src/pipeline/scoring/__tests__/vdem.test.ts` — green (7 parse cases including year-boundary case (g)).
- `npm run build` — green (Astro build + OG + llms + validate:seo).
- Conflict pillar `indicatorWeights` sum = 1.000000 exactly (jq assertion).
- 611 files in `data/scores/` match `YYYY-MM-DD.json`; none contain any retired WGI indicator name.
- `data/scores/history-index.json` exists and was last modified during this task.
- DZA / USA / JPN / NOR / BRA / IND in `public/scores.json` have non-null crime + governance pillars with `dataCompleteness >= 0.30`.
- Idempotency: rerunning `backfill-vdem-rescore.ts` produces zero git diff in `data/scores/` AND prints 611 `SKIP: ... already at 8.1.0` lines (resume gate working).
- Zero grep matches for retired WGI names in src/, data/scores/, public/scores.json (except for explicitly-deprecated comments).
- All 7 source pages list V-Dem Institute (v16) as a row; en/sources/index.astro contains the CC-BY-SA attribution string.
- Source count harmonized to "7" across `methodology.tiered_text` (baseline: 4 named, signal: 3 named — total 7), `about.project_text` (enumerates all 7 by name including V-Dem), and `about.sources_text` ("7" not "7+" / "9+") in all 7 locale blocks.
- V-Dem CC-BY-SA citation (Coppedge / V-Dem Institute / DOI 10.23696/vdemds26) appears in both `src/pages/en/sources/index.astro` and `src/i18n/ui.ts`.
- weights.json `version === "8.1.0"`.
- `MIN_PILLAR_COVERAGE === 0.30` (unchanged) at src/pipeline/scoring/engine.ts:49.
- `.gitignore` contains `data/raw/**/*.csv`.
</verification>

<success_criteria>
- All 5 tasks complete with their verify steps green.
- 611 rescored snapshots committed to git (pattern: project ships static scored data — confirmed by inspecting prior backfill commits b2ef9ad, 3bce773).
- Pipeline succeeds end-to-end against today's date with `vdem` reporting in the Batch 1 summary alongside `worldbank` / `gpi` / `inform` / `reliefweb` / `gdacs`.
- Methodology, sources, and about copy in all 7 locales references V-Dem v16 with CC-BY-SA attribution and the harmonized 7-source enumeration (WB + V-Dem + INFORM + GPI + ReliefWeb + GDACS + advisories).
- Backfill is idempotent (verified by re-running; weightsVersion=8.1.0 resume gate prevents double-rescoring).
- Algeria + the 5 sanity countries have working crime + governance pillars.
- 2026 snapshots reference V-Dem `year: 2025` (no fictional 2026 V-Dem rows — year-boundary correctly handled).
- No regressions to existing WB indicators (wb_child_mortality + wb_air_pollution still feed Health and Environment pillars).
- weights.json version bumped to 8.1.0; conflict pillar weights sum to exactly 1.0.
</success_criteria>

<runtime_notes>
**Expected wall-time budget per task** (context cost, not human time):
- Task 1: ~25% context — new file (vdem.ts ~150 lines) + small edits to 3 existing files + new test file with 7 parse cases.
- Task 2: ~15% context — JSON edits + 7 test assertions + engine.ts edits.
- Task 3: ~25% context (most of it spent waiting on the rescore loop — initial runtime ~30-55 min wall-clock; resumed runs ~10s due to weightsVersion=8.1.0 skip gate; the executor's context cost is mainly writing the script).
- Task 4: ~30% context — 7 locale blocks × ~10 edits each in ui.ts plus 7 .astro file edits. Largest single task by edit count; offset by mechanical/repetitive nature.
- Task 5: ~5% context — verification only.

**Sequential ordering is mandatory:**
- Task 1 must finish before Task 2 (Task 2's tests import INDICATOR_SOURCE_MAP and need vdem_* in normalize.ts).
- Tasks 1+2 must finish before Task 3 (the rescore depends on the new fetcher, weights, and normalize ranges).
- Task 4 (UI) can be done in parallel with Task 3 in principle (no file overlap), but recommend running it after Task 3 starts so the executor can spot-check rendered pages against fresh scored data once the rescore completes.
- Task 5 (checkpoint) is last.

**Backfill command (paste-ready):**
```bash
npx tsx scripts/backfill-vdem-rescore.ts
```
Runtime estimate: 30-55 minutes for first run, ~10s for subsequent runs (resume via weightsVersion=8.1.0 skip gate). Single-threaded. Safe to interrupt and resume.
</runtime_notes>

<output>
After completion, create `.planning/quick/260511-k2m-replace-retired-wb-wgi-with-v-dem-instit/260511-k2m-SUMMARY.md` documenting: files changed, rescore stats (N snapshots, M failures, S skips), final conflict pillar weight table, locales touched, the single source-count rule applied ("7 sources" enumerated explicitly), and a short note on the CC-BY-SA attribution added to the methodology and English sources pages.
</output>
