---
phase: quick-260706-x81
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline/scoring/engine.ts
  - src/pipeline/config/weights.json
  - src/pipeline/types.ts
  - src/lib/regions.ts
  - src/pipeline/backfill.ts
  - src/pipeline/fetchers/gpi.ts
  - src/pipeline/scoring/__tests__/engine.test.ts
  - src/pipeline/__tests__/crisis-validation.test.ts
  - src/pipeline/__tests__/weights-config.test.ts
  - package.json
  - scripts/verify-formula-v9-parity.ts
  - scripts/fixtures/formula-v9-parity-2026-07-06.csv
  - data/scores/**            # regenerated by backfill (never hand-edited)
  - public/scores.json        # regenerated via cp from latest.json (never hand-edited)
autonomous: true
requirements:
  - SHIP-SPEC-1.1-engine
  - SHIP-SPEC-1.2-config
  - SHIP-SPEC-1.3-backfill-gpi-vintage
  - SHIP-SPEC-1.4-parity-gate
  - SHIP-SPEC-1.5-tests
  - SHIP-SPEC-2.3-items-1-3

must_haves:
  truths:
    - "The engine reproduces new-scores.csv within |Δscore| < 0.02 for all 248 countries (freshness-decay deviations documented)"
    - "npm test (pipeline + engine + weights + crisis) passes green under v9"
    - "backfill --dry-run reports 667/667 OK; live backfill rewrites 667 snapshots + latest.json + history-index.json"
    - "public/scores.json is regenerated from data/scores/latest.json (not hand-edited)"
    - "Every ScoredCountry carries a numeric confidence field; no score falls outside [1,10]"
    - "weights.json version is 9.0.0 with gpi_safety_security under crime and vdem_rule_of_law under governance"
  artifacts:
    - path: "src/pipeline/scoring/engine.ts"
      provides: "v9 computeCountryScore: Bayesian shrinkage, advisory consensus, geometric+acute aggregation, confidence output"
      contains: "confidence"
    - path: "src/pipeline/config/weights.json"
      provides: "v9.0.0 pillar recomposition + per-indicator precision + formulaV9 constants"
      contains: "9.0.0"
    - path: "src/pipeline/types.ts"
      provides: "confidence field on ScoredCountry; WeightsConfig extended for v9 shape"
      contains: "confidence"
    - path: "src/lib/regions.ts"
      provides: "region mapping extended to all 248 iso3 (adds ~50 territories incl. RUS)"
      contains: "RUS"
    - path: "scripts/verify-formula-v9-parity.ts"
      provides: "parity gate vs committed new-scores.csv fixture"
    - path: "data/scores/latest.json"
      provides: "regenerated 2026-07-06 snapshot under v9"
  key_links:
    - from: "src/pipeline/scoring/engine.ts"
      to: "src/lib/regions.ts"
      via: "getRegion(iso3) for region prior offset"
      pattern: "getRegion"
    - from: "src/pipeline/backfill.ts"
      to: "src/pipeline/fetchers/gpi.ts"
      via: "per-year GPI vintage selection from newest gpi.xlsx"
      pattern: "gpi"
    - from: "scripts/verify-formula-v9-parity.ts"
      to: "src/pipeline/scoring/engine.ts"
      via: "computeAllScores on 2026-07-06 raw data diffed against fixture"
      pattern: "computeAllScores"
---

<objective>
Implement Formula v9 PART 1 (pipeline core) EXACTLY as specified in the frozen SHIP-SPEC
sections 1.1–1.5 and validation gates 2.3 items 1–3. This replaces the current hard-cap /
critical-floor / eligibility-gate engine with a Bayesian-shrinkage, uncertainty-weighted
geometric-mean scorer ported faithfully from the frozen reference implementation, evolves
weights.json to v9.0.0, fixes historical backfill vintages (vdem year-based + GPI per-year),
proves parity against the frozen ground-truth CSV, and regenerates all historical scores.

Purpose: Ship the new scoring core and consistent historical data so PART 2 (product surface:
bands, verdicts, methodology, i18n, SEO, llms, OG, deploy) can build on stable numbers.
Output: rewritten engine + v9 config + fixed backfill/GPI parser + rewritten tests + parity
harness + regenerated data/scores/** and public/scores.json.

SCOPE BOUNDARY — PART 1 ONLY. This plan MUST NOT touch: methodology pages, i18n strings,
README, FAQ/SEO copy, hub-faq, ApiDocs, band/verdict recalibration, llms regeneration, OG
images, `npm run build`, `npm run validate:seo`, or any git push. Those are ALL PART 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
# FROZEN CONTRACT + REFERENCE (read fully before coding — all under the session scratchpad)
@/private/tmp/claude-501/-Users-riccardo-Developer-VibeCoding-Isitsafetotravel/dda1ae2e-57b7-4d23-9610-4f92d047c45e/scratchpad/SHIP-SPEC.md
@/private/tmp/claude-501/-Users-riccardo-Developer-VibeCoding-Isitsafetotravel/dda1ae2e-57b7-4d23-9610-4f92d047c45e/scratchpad/prototype-scorer.mjs
@/private/tmp/claude-501/-Users-riccardo-Developer-VibeCoding-Isitsafetotravel/dda1ae2e-57b7-4d23-9610-4f92d047c45e/scratchpad/final-constants.json

# GROUND TRUTH (parity target + region source) — absolute scratchpad paths:
#   new-scores.csv     (parity target; column `new_score` for 248 rows)
#     /private/tmp/claude-501/-Users-riccardo-Developer-VibeCoding-Isitsafetotravel/dda1ae2e-57b7-4d23-9610-4f92d047c45e/scratchpad/new-scores.csv
#   scores-extract.csv (iso3→region source of truth that extends regions.ts)
#     /private/tmp/claude-501/-Users-riccardo-Developer-VibeCoding-Isitsafetotravel/dda1ae2e-57b7-4d23-9610-4f92d047c45e/scratchpad/scores-extract.csv

# PROJECT STATE + GUIDELINES
@.planning/STATE.md
@./CLAUDE.md

# CODE TO REWRITE / EVOLVE
@src/pipeline/scoring/engine.ts
@src/pipeline/config/weights.json
@src/pipeline/backfill.ts
@src/pipeline/fetchers/gpi.ts
@src/pipeline/types.ts
@src/lib/regions.ts
</context>

<interfaces>
<!-- Load-bearing facts the executor needs; extracted from the codebase. Do NOT re-derive. -->

FROZEN MATH — the prototype's arithmetic is FINAL. Port it faithfully. Any numeric constant,
tier membership, exclusion-set member, region offset, sub-weight, precision (rho), K/lambda/q/gamma/
S_MAX/N_SEV, prior clamp, or floor MUST equal the prototype / final-constants.json EXACTLY. No
"cleanups", no rounding changes, no re-tuning. If a forced deviation is unavoidable, STOP, document
it in the commit body, and re-run the parity gate. Improvements are prohibited (SHIP-SPEC Non-negotiables).

Engine data contract (unchanged plumbing):
- computeCountryScore(iso3, allIndicators, weightsConfig, countryEntry, advisories, sources,
  sourcesConfig?) returns ScoredCountry. Region is NOT a parameter — the engine resolves it via
  getRegion(iso3) from src/lib/regions.ts. Add the confidence field to the returned object.
- Indicators reach the engine as raw values; normalizeIndicators() (normalize.ts, UNCHANGED)
  produces normalizedValue (0-1, higher = safer). The prototype's ind.normalizedValue maps to this.
  gpi_safety_security and vdem_rule_of_law already have normalization ranges — do NOT touch normalize.ts.
- computeAllScores(rawDataMap, weightsConfig) is the pipeline/backfill entry point and loads advisory
  levels from data/raw/<date>/advisories*-info.json. Preserve this loading logic verbatim.

Prototype → engine mapping (SHIP-SPEC 1.1; all constants in final-constants.json):
- Advisory consensus A, nAdv, severeShare, severeEff, rho_A over INCLUDED fresh sources only.
  FROZEN_EXCLUDED_SOURCES = {jp,be,ie,ch,hk,sk,es,ar,cn} NEVER enter A/severeShare/nAdv.
  ADVISORY_REBASE {at,nz}: L -> max(1, L-1). ADVISORY_TIER importance table (tier4/3/2/1) FROZEN.
- Pillar composition per PILLAR_SPEC: conflict = gpi_overall(sub .45, rho 2.0), gpi_militarisation
  (.15, rho 1.0), A(.40, rho_A = nAdv/(nAdv+6)); crime = gpi_safety_security(1.0, rho 2.5); health =
  wb_child_mortality(.38), inform_health(.31), inform_epidemic(.31) each rho 1; governance =
  vdem_rule_of_law(.25), vdem_gov_effectiveness(.25), vdem_corruption_control(.25),
  inform_governance(.25) each rho 1; environment = wb_air_pollution(.35,rho1), inform_natural(.25,rho1),
  inform_climate(.20,rho1), reliefweb_active_disasters(.10,rho0), gdacs_disaster_alerts(.10,rho0).
- Precision n_i = sum of rho_eff over present indicators, where rho_eff = rho * freshnessWeight()
  (the prototype hard-coded fw=1.0 because the snapshot was uniformly fresh; the engine MUST multiply
  rho by the existing freshnessWeight() using source-tiers.json decay params — reliefweb/gdacs keep rho=0).
- Prior mu = clamp(0.45 + regionOffset + (A-0.5)*0.35*nAdv/(nAdv+5), 0.32, 0.60).
- Shrinkage p_hat_i = (n_i*p_i + K*mu)/(n_i+K), K=1.0. REMOVE ALL of: eligibility/coverage gates,
  neutral-0.5 pillar defaults, low-data advisory blend, critical floor, majority-L4 hard cap.
- Aggregation: G = exp(sum w_i*ln(max(0.02,p_hat_i))); acuteRaw = ((.30*conflict^-3 + .25*crime^-3)/.55)^(-1/3);
  acute = min(acuteRaw, G) [down-only]; composite = G^0.75 * acute^0.25 * (1 - 0.25*severeEff).
  score = 1 + 9*composite^0.79; scoreDisplay = round(score).
- confidence = sum w_i * n_i/(n_i+K)  (NEW field). dataCompleteness: KEEP existing presence-ratio
  semantics UNCHANGED. globalScore: UNCHANGED (arithmetic mean, snapshot.ts). weightsVersion -> "9.0.0".
  Bump pipelineVersion per convention.

Region source of truth (SHIP-SPEC 1.1 item 4): regions.ts currently maps 199/248 iso3; 50 are
missing (incl. RUS -> europe, which today wrongly returns 'other'). Extend regions.ts so getRegion()
returns the SAME region as scores-extract.csv for all 248 iso3. REGION_OFFSET (europe +.05,
oceania 0, americas -.02, asia -.02, middle_east -.05, africa -.05, other 0) is FROZEN and lives in
the engine / formulaV9 config — do NOT bake offsets into regions.ts.

Backfill / GPI vintage facts (SHIP-SPEC 1.3):
- backfill.ts YEAR_BASED_SOURCES currently = {worldbank}. vdem is vintage-per-file (each historical
  dir e.g. 2015-01-05 has its own vdem-parsed.json at that year's vintage) but is being SKIPPED by the
  fetchedAt-consistency gate. Fix: add 'vdem' to YEAR_BASED_SOURCES so historical vdem is used.
- Only 26 of 667 raw dirs contain gpi.xlsx; historical dirs have NO gpi at all. gpi.xlsx holds the
  FULL 2008-2023 history across the Overall Scores / Safety and Security / Militarisation sheets. The
  current parser emits ONLY the latest year (one value/country). Add an EXPORTED per-year parse
  function that emits indicators for every year column 2008-2023 across the 3 sheets.
- Backfill GPI injection: for each snapshot date, select gpi values at year = min(snapshotYear, 2023)
  from the date's own gpi.xlsx if present else the NEWEST gpi.xlsx across data/raw/*/ (all are the
  same full-history file, so selection is deterministic). Inject ONE value per country per gpi
  indicator into that date's rawDataMap (preserve the engine's one-value-per-indicator contract).
- Do NOT change fetchGpi's LIVE output shape (gpi-parsed.json stays latest-year, one/country) so
  run.ts / the daily pipeline are unaffected. Per-year extraction is an additive helper for backfill.
- advisories/inform/reliefweb/gdacs: keep the existing consistency gate unchanged (absent for
  historical dates => nAdv=0, severeEff=0; formula degrades gracefully by design).

public/scores.json production (verified): it is NOT written by the pipeline. deploy.yml /
data-pipeline.yml do `cp data/scores/latest.json public/scores.json`. That copy IS the canonical
generation path — reproduce it with `cp` (Bash), never with the Write/Edit tools.

Generated-file hook: .claude/hooks/block-generated.sh matches ONLY Edit|Write|MultiEdit|
NotebookEdit. It does NOT intercept Bash or node fs writes. So backfill (node writes via writeJson)
and `cp` are allowed; editing data/scores/** or public/scores.json with Write/Edit is BLOCKED.

Test runner: there is NO `npm test` script yet. Tests run via `node --import tsx --test <glob>`.
Task 2 adds a `test` script covering the pipeline + scoring suites
(src/pipeline/**/__tests__/*.test.ts and src/pipeline/scoring/__tests__/*.test.ts).
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Port v9 engine + weights.json v9.0.0 + confidence type + regions extension</name>
  <files>src/pipeline/scoring/engine.ts, src/pipeline/config/weights.json, src/pipeline/types.ts, src/lib/regions.ts</files>
  <action>
Rewrite computeCountryScore in engine.ts to faithfully port prototype-scorer.mjs per SHIP-SPEC 1.1
(full mapping is in this plan's <interfaces>). Implement, in order: advisory consensus (frozen
excluded set, importance tiers, at/nz rebase, A / nAdv / severeShare / severeEff / rho_A); per-pillar
value p_i (sub-weight-renormalized mean over present indicators) and precision n_i = sum of
(rho * freshnessWeight()) — integrate the EXISTING freshnessWeight() so rho_eff = rho*fw
(reliefweb/gdacs stay rho=0; the synthetic advisory indicator A uses rho_A = nAdv/(nAdv+6)); the
conservative prior mu using the FROZEN region offset from getRegion(iso3); Bayesian shrinkage
p_hat_i = (n_i*p_i + K*mu)/(n_i+K) with K=1.0; geometric aggregate G; down-only acute soft-min
(acute = min(acuteRaw, G)); composite = G^0.75 * acute^0.25 * (1 - 0.25*severeEff); score =
1 + 9*composite^0.79; scoreDisplay = round(score); and the NEW confidence = sum w_i*n_i/(n_i+K).
DELETE every removed mechanism: MIN_PILLAR_COVERAGE gating, the low-data advisory blend, neutral-0.5
pillar defaults, the critical floor, and the majority-L4 hard cap. KEEP the existing dataCompleteness
presence-ratio computation and semantics UNCHANGED. KEEP computeAllScores' advisory-info loading and
iso3 collection verbatim. The math is FROZEN — implement acuteDownOnly and the count-damped severe
modifier EXACTLY as in the prototype; add NO improvements. Prefer reading pillar sub-weights,
per-indicator precision rho, and all formulaV9 constants (K, lambda, q, gamma, S_MAX, N_SEV, muBase,
clamps, nudgeScale, nudgeDenom, rhoADenom, floorP, acuteDownOnly, advisory importance tiers,
FROZEN_EXCLUDED_SOURCES, at/nz rebase list, regionOffset table) from weights.json v9 so it stays the
single source of truth — but their VALUES must equal the prototype exactly.

Evolve weights.json to version "9.0.0" per SHIP-SPEC 1.2: recompose pillars so crime's indicator is
gpi_safety_security and governance gains vdem_rule_of_law; conflict indicators are gpi_overall +
gpi_militarisation + the synthetic advisory-consensus indicator A (handle A as an engine-injected
indicator; do NOT leave the 37 advisory_level_* entries in conflict.indicators). Add per-indicator
subWeight + precision (rho) matching PILLAR_SPEC, and a formulaV9 section holding every constant
listed above. Keep pillar weights 30/25/20/15/10 and the 5 pillar names UNCHANGED (they must still
sum to 1.0).

Add confidence: number to ScoredCountry in types.ts, populate it from the engine, and extend
WeightsConfig / PillarWeight types to accommodate the v9 shape (per-indicator precision map + the
formulaV9 section) so the whole repo type-checks.

Extend src/lib/regions.ts so getRegion() returns the SAME region as scores-extract.csv for all 248
iso3 (add the ~50 missing territories, including RUS -> europe; use scores-extract.csv as the source
of truth for each mapping). Do NOT put REGION_OFFSET values in regions.ts — offsets stay frozen in
the engine / formulaV9 config.

NOTE: after this task the OLD tests will not compile (they import MIN_PILLAR_COVERAGE and assert the
v8 structure) — that is expected and is fixed in Task 2. Do not weaken the engine to satisfy old tests.
  </action>
  <verify>
    <automated>npx tsx -e "import{computeAllScores}from'./src/pipeline/scoring/engine.ts';import{readJson}from'./src/pipeline/utils/fs.ts';import{readdirSync}from'node:fs';import{join}from'node:path';const dir='data/raw/2026-07-06';const m=new Map();for(const f of readdirSync(dir).filter(x=>x.endsWith('-parsed.json'))){const d=readJson(join(dir,f));if(d)m.set(d.source,d);}const w=readJson('src/pipeline/config/weights.json');const r=computeAllScores(m,w);const bad=r.filter(c=>!(c.score>=1&&c.score<=10)||typeof c.confidence!=='number');console.log('scored',r.length,'version',w.version,'ITA',r.find(c=>c.iso3==='ITA')?.score?.toFixed(2),'bad',bad.length);if(w.version!=='9.0.0'||bad.length||r.length<248)process.exit(1);"</automated>
  </verify>
  <done>engine.ts is the v9 port (no coverage gate / floor / hard cap / advisory-blend); weights.json version is 9.0.0 with crime=gpi_safety_security and governance including vdem_rule_of_law + a formulaV9 constants section; ScoredCountry has a numeric confidence; getRegion returns a non-'other' region for every iso3 mapped in scores-extract.csv (incl. RUS); the smoke run scores 248 countries, all in [1,10], ITA ~ 7.9. Commit atomically: "feat(scoring): port Formula v9 engine + weights v9.0.0 + confidence + region map".</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Backfill vdem/GPI vintage fixes + rewrite engine/crisis/weights tests for v9</name>
  <files>src/pipeline/backfill.ts, src/pipeline/fetchers/gpi.ts, src/pipeline/scoring/__tests__/engine.test.ts, src/pipeline/__tests__/crisis-validation.test.ts, src/pipeline/__tests__/weights-config.test.ts, package.json</files>
  <action>
Backfill (SHIP-SPEC 1.3): in backfill.ts add 'vdem' to YEAR_BASED_SOURCES so historical
vintage-per-file vdem data is no longer skipped by the fetchedAt gate. In gpi.ts add an EXPORTED
per-year parser that emits gpi_overall / gpi_safety_security / gpi_militarisation indicators for
every year column 2008-2023 across the three sheets (with the correct year on each indicator); leave
fetchGpi's live gpi-parsed.json output as latest-year one-value-per-country so the daily pipeline
(run.ts) is unaffected. In backfill's per-date load path, inject GPI: locate the date's own gpi.xlsx
if present else the NEWEST gpi.xlsx across data/raw/*/, parse per-year, select year =
min(snapshotYear, 2023), and add ONE value per country per gpi indicator to that date's rawDataMap
(one-value-per-indicator contract preserved). Keep the existing advisories/inform/reliefweb/gdacs
consistency gate unchanged (graceful degradation for historical dates).

Rewrite engine.test.ts for v9 mechanisms (SHIP-SPEC 1.5), keeping the synthetic-config style:
shrinkage monotonicity (more / higher-precision data moves the score away from the prior); a fully
missing pillar collapses to p_hat = mu; the severe modifier is bounded and count-damped; continuity —
a single advisory-level step changes the score by < 0.5 on a synthetic sparse country; no score
outside [1,10]; the acute term is down-only (good conflict/crime cannot inflate above G). DELETE tests
that assert removed mechanisms (the MIN_PILLAR_COVERAGE / coverage-gate suite, neutral-0.5 defaults,
hard-cap behavior, the "neutral 5.5 with zero indicators" and fixed "score 8.2" v8-arithmetic
assertions). Update the V-Dem migration structural block: crime.indicators is no longer
["vdem_rule_of_law"] — assert the v9 recomposition (crime = gpi_safety_security; governance includes
vdem_rule_of_law) and version "9.0.0". Keep the normalize / normalizeInverse / normalizeIndicators
tests as-is. Update crisis-validation.test.ts fixtures to the v9 config structure (v9 pillar
composition; drop retired wb_* WGI indicators; keep it validating that active-conflict countries
score below natural-disaster countries under v9). Update weights-config.test.ts structural assertions
to the v9 shape (still exactly 5 pillars, weights still sum to 1.0, version non-empty). Do NOT edit
score-drift-guard.test.ts — its thresholds are unchanged and the Task 3 regen keeps it green.

Add a `test` script to package.json that runs the pipeline + scoring test suites via
`node --import tsx --test` (glob src/pipeline/**/__tests__/*.test.ts and
src/pipeline/scoring/__tests__/*.test.ts) so `npm test` exists and is green.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -25 && npm test >/dev/null 2>&1</automated>
  </verify>
  <done>backfill.ts treats vdem as year-based; gpi.ts exports a per-year parser and backfill injects year=min(snapshotYear,2023) GPI from the newest xlsx; fetchGpi live output unchanged; engine.test.ts + crisis-validation.test.ts + weights-config.test.ts assert v9 behavior and structure; `npm test` runs the pipeline+scoring suites and passes green (exit 0). Commit atomically: "test(scoring): v9 backfill vintages + rewritten engine/crisis/weights tests".</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Parity gate + backfill dry-run/live regen + public/scores.json + sanity checks</name>
  <files>scripts/verify-formula-v9-parity.ts, scripts/fixtures/formula-v9-parity-2026-07-06.csv, data/scores/**, public/scores.json</files>
  <action>
Copy the frozen ground-truth CSV into the repo as a committed fixture with Bash cp (NOT the Write
tool): copy the scratchpad new-scores.csv to scripts/fixtures/formula-v9-parity-2026-07-06.csv.
Create scripts/verify-formula-v9-parity.ts: load data/raw/2026-07-06 parsed data exactly as the
pipeline does (including the Task 2 GPI year-selection path), run computeAllScores with weights.json
v9, and diff each country's score against the fixture's new_score column. GATE: max |Δscore| < 0.02
across all 248 countries EXCEPT deviations attributable to freshness decay — the script must classify
and PRINT any country exceeding 0.02, labelling whether the residual is freshness-attributable (a
signal/baseline source with fw<1 on 2026-07-06 changing n_i), and exit non-zero ONLY on
non-freshness parity failures. Print the count of deviations and the max |Δ|. If parity fails for a
non-freshness reason, fix the PORT (Task 1), never the prototype or the fixture.

Run the validation gates (SHIP-SPEC 2.3 items 1-3), in order, all must pass:
1. `npm test` green (from Task 2).
2. Parity gate: `npx tsx scripts/verify-formula-v9-parity.ts` passes (< 0.02, freshness deviations
   documented in the run output and echoed into the SUMMARY).
3. Backfill dry-run: `npx tsx src/pipeline/backfill.ts --dry-run` — expect 667/667 OK (Failed: 0).
   Then LIVE backfill: `npx tsx src/pipeline/backfill.ts` — rewrites data/scores/*.json (667 files) +
   latest.json + history-index.json (node fs writes; the generated-file hook does not block these).
Regenerate public/scores.json the canonical way: `cp data/scores/latest.json public/scores.json`
(Bash cp — never Write/Edit; this mirrors deploy.yml).

Sanity spot-checks on the regenerated data/scores/latest.json (assert programmatically, do not
eyeball): global mean score ~ 6.75; min ~ 3.72 (Yemen) and max ~ 8.89 (Iceland) — full range
~ [3.7, 8.9]; ITA ~ 7.91, FRA ~ 7.80, GRC ~ 7.96, USA ~ 7.06, NIC ~ 6.00, JAM ~ 7.36; YEM /
AFG(~3.86) / SYR(~4.19) clearly low; every country has a numeric confidence and score in [1,10];
public/scores.json.date equals latest.json.date. (All match new-scores.csv.)

DO NOT run `npm run build`, `npm run generate:llms`, `npm run generate:og`, or `npm run validate:seo`,
and DO NOT push — all PART 2. Commit atomically (data + parity harness) with the SHIP-SPEC 2.4
message: "data: recompute all historical scores under formula v9".
  </action>
  <verify>
    <automated>npx tsx scripts/verify-formula-v9-parity.ts && npx tsx src/pipeline/backfill.ts --dry-run 2>&1 | grep -E "Failed: 0" && node -e "const l=require('./data/scores/latest.json');const c=l.countries;const m=c.reduce((s,x)=>s+x.score,0)/c.length;const bad=c.filter(x=>!(x.score>=1&&x.score<=10)||typeof x.confidence!=='number');const ita=c.find(x=>x.iso3==='ITA').score;const pub=require('./public/scores.json');if(bad.length||Math.abs(m-6.75)>0.15||Math.abs(ita-7.91)>0.1||pub.date!==l.date){console.error('SANITY FAIL mean',m.toFixed(3),'ITA',ita,'bad',bad.length,'pubDate',pub.date,'latestDate',l.date);process.exit(1)}console.log('OK mean',m.toFixed(3),'ITA',ita.toFixed(2),'countries',c.length)"</automated>
  </verify>
  <done>Parity gate passes (< 0.02, any freshness deviations documented); backfill dry-run is 667/667 (Failed: 0); live backfill rewrote 667 snapshots + latest.json + history-index.json; public/scores.json regenerated via cp and its date matches latest.json; sanity asserts pass (mean ~6.75, ITA ~7.91, all scores in [1,10] with confidence). No build/llms/og/seo run, no push. Data-regen committed with "data: recompute all historical scores under formula v9".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen reference (prototype/CSV) → engine port | The port could silently drift from the frozen math; parity gate is the guard |
| raw data (data/raw/**) → backfill → data/scores/** | Vintage mis-selection (wrong GPI/vdem year) corrupts historical scores |
| generated files (data/scores/**, public/scores.json) → tool writes | Hook blocks Write/Edit; must regen via node/cp only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-x81-01 | Tampering | engine port vs frozen math | mitigate | Parity gate (Task 3) asserts max abs delta < 0.02 vs committed new-scores.csv fixture; port is fixed, prototype is not |
| T-x81-02 | Tampering | GPI/vdem vintage selection in backfill | mitigate | Deterministic year = min(snapshotYear, 2023) from newest gpi.xlsx; vdem year-based gate; backfill --dry-run 667/667 before live |
| T-x81-03 | Information disclosure | generated data files | mitigate | Regenerate only via node backfill + `cp`; Write/Edit on data/scores/** & public/scores.json is hook-blocked |
| T-x81-04 | Denial of service | score-drift-guard on inconsistent history | mitigate | Live backfill rewrites ALL 667 snapshots consistently so day-over-day drift guard stays green |
| T-x81-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in PART 1 (xlsx/tsx already present); no package legitimacy gate needed |
</threat_model>

<verification>
- `npm test` exits 0 (pipeline + scoring + engine + crisis + weights + drift-guard suites).
- `npx tsx scripts/verify-formula-v9-parity.ts` exits 0 with max abs delta < 0.02 (freshness
  deviations, if any, printed and documented in the SUMMARY).
- `npx tsx src/pipeline/backfill.ts --dry-run` reports Failed: 0 over 667 dates.
- After live backfill: data/scores/latest.json global mean ~ 6.75, range ~ [3.7, 8.9], anchor
  countries within tolerance, every country carries a numeric confidence, no score outside [1,10].
- public/scores.json.date == data/scores/latest.json.date.
- No PART 2 side effects: no build, no llms/og regen, no validate:seo, no git push.
</verification>

<success_criteria>
- Engine is the faithful v9 port; weights.json is v9.0.0 with the recomposed pillars + formulaV9
  constants; ScoredCountry.confidence exists.
- Parity holds against the frozen ground-truth (< 0.02, freshness-decay deviations documented).
- Historical data fully regenerated under v9 (667 snapshots + latest.json + history-index.json);
  public/scores.json regenerated from latest.json.
- All tests green; three atomic commits (engine+config / tests+backfill / data regen); nothing pushed.
</success_criteria>

<output>
Create `.planning/quick/260706-x81-formula-v9-part-1-engine-rewrite-weights/260706-x81-SUMMARY.md`
when done. In it, record: the parity result (max abs delta + any freshness-attributable deviations
with country codes and root cause), the backfill dry-run/live counts, the sanity-check anchor scores,
and any FORCED deviation from the frozen math (there should be none) with justification.
</output>
