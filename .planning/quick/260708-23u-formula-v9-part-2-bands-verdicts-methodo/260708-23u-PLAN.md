---
phase: quick-260708-23u
plan: 01
type: execute
wave: 1
depends_on: [260706-x81-01]
autonomous: true
requirements: [SHIP-SPEC-2.1-bands, SHIP-SPEC-2.2-docs, SHIP-SPEC-2.3-gates, SHIP-SPEC-2.4-ship]
files_modified:
  # T1 — band / verdict / color recalibration (1–10 country score thresholds)
  - src/components/country/ScoreHero.astro
  - src/components/country/AnswerFirstParagraph.astro
  - src/components/country/AuthorityLinks.astro
  - src/components/country/ComparisonTable.astro
  - src/components/country/NeighborComparison.astro
  - src/components/country/RelatedCountries.astro
  - src/components/hub/CountryRankRow.astro
  - src/lib/seo.ts
  - src/lib/map-utils.ts
  - src/pages/badge/[iso3].svg.ts
  - scripts/generate-llms-full.ts
  - scripts/generate-og-images.ts
  # T2 — documentation & strings (v9 mechanism, ×7 locales)
  - src/i18n/ui.ts
  - src/pages/en/methodology/index.astro
  - src/pages/it/metodologia/index.astro
  - src/pages/es/metodologia/index.astro
  - src/pages/fr/methodologie/index.astro
  - src/pages/pt/metodologia/index.astro
  - src/pages/zh/methodology/index.astro
  - src/pages/de/methodik/index.astro
  - README.md
  - src/lib/country-faq-copy.ts
  - src/lib/hub-faq.ts
  - src/components/ApiDocs.astro
  - data/SCORE-VALIDATION.md
  # T3 — regenerated artifacts (via scripts/build only — never hand-edited)
  - public/llms.txt
  - public/llms-full.txt
  - public/og/**

must_haves:
  truths:
    - "Every 1–10 country-score threshold in src/ + scripts/ reflects the new v9 range (~[3.72, 8.89], mean 6.75); Italy/France/Greece read clearly positive, Yemen/Afghanistan/Syria clearly negative"
    - "No user-facing or docs string claims a Level-4 hard cap, a 0.25 critical floor, a sparse-data advisory blend, or that crime = rule of law — all four are gone in v9"
    - "Methodology (×7 locales), README, ApiDocs describe v9: uncertainty-weighted (Bayesian shrinkage) weighted geometric mean, crime = GPI Safety & Security, new confidence output, new 5-band legend"
    - "npm run validate:seo is all-pass against a fresh full build with regenerated OG images and llms files"
  artifacts:
    - path: "data/SCORE-VALIDATION.md"
      provides: "Dated v9 validation section (what changed + known limitations from final-constants residuals)"
      contains: "v9"
    - path: "public/llms-full.txt"
      provides: "v9 mechanism sentence (regenerated, not hand-edited)"
  key_links:
    - from: "src/lib/seo.ts (getCountryFaqData verdict thresholds)"
      to: "src/lib/country-faq-copy.ts (a1Verdict low/moderate/high copy)"
      via: "riskLevel band selection"
      pattern: "score >= "
---

<objective>
Ship Formula v9 PART 2 — the product surface. PART 1 already shipped the v9 engine and
regenerated all 668 snapshots + public/scores.json (parity exact). This plan recalibrates every
1–10 band/verdict/color threshold to the new compressed scale, rewrites all methodology / FAQ /
SEO / README / API docs strings to the v9 mechanism (uncertainty-weighted geometric mean; crime =
GPI Safety & Security; no hard caps/floors/blends; new confidence field; new band legend), then
regenerates llms + OG images, passes the SEO gate, and ships.

Purpose: the on-disk scores are already v9, but the UI still buckets/verdicts them against the OLD
scale (top 9.93 / bottom 1.11) and every doc still describes the removed v8 mechanisms. This closes
the gap so the site tells the truth about how it now scores.

Output: recalibrated UI + docs across 7 locales; regenerated public/llms*.txt + public/og/**;
validate:seo all-pass; committed and pushed to master (production deploy).

FROZEN CONTRACT — read before editing. Do NOT duplicate; reference by path:
- /private/tmp/claude-501/-Users-riccardo-Developer-VibeCoding-Isitsafetotravel/dda1ae2e-57b7-4d23-9610-4f92d047c45e/scratchpad/SHIP-SPEC.md  (this plan = PART 2, sections 2.1 / 2.2 / 2.3 items 4–7 / 2.4)
- /private/tmp/claude-501/-Users-riccardo-Developer-VibeCoding-Isitsafetotravel/dda1ae2e-57b7-4d23-9610-4f92d047c45e/scratchpad/final-constants.json  (source for SCORE-VALIDATION "known limitations": PSE stale-GPI, GUM ca=4 data bug, ESH single-source step, RUS-vs-UKR)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260706-x81-formula-v9-part-1-engine-rewrite-weights/260706-x81-SUMMARY.md
@./CLAUDE.md

**Ground truth for verdict recalibration** — read the live v9 output, already on disk:
`public/scores.json` (v9.0.0). Range ≈ [3.72 YEM, 8.89 ISL], global mean 6.75. Canonical NEW
bands (SHIP-SPEC 2.1): `<5.0 danger` · `5.0–6.0 high caution` · `6.0–7.0 moderate` ·
`7.0–8.0 good` · `≥8.0 excellent` (distribution ≈ 17 / 33 / 100 / 72 / 26). Anchors to preserve:
Italy 7.91 · France 7.80 · Greece 7.96 → clearly positive; Yemen 3.72 / Afghanistan 3.86 /
Syria 4.19 → clearly negative; Jamaica 7.36 must NOT read as unreservedly safe.

**Weights are UNCHANGED** (30/25/20/15/10) — every percentage in every string STAYS TRUE.
What changed vs v8 prose: crime pillar = GPI Safety & Security (homicide / violent crime /
perceived criminality), NOT rule of law; rule of law moved to governance; the L4 hard cap, the
0.25 critical floor, and the sparse-data advisory blend are ALL REMOVED; add uncertainty
weighting (Bayesian shrinkage toward a conservative region-anchored prior), calibrated
importance-weighted advisory consensus, the new `confidence` output, and the new band legend.

<interfaces>
<!-- v9 pillar composition the docs must now describe (from weights.json v9.0.0, PART 1). -->
conflict (30%) = gpi_overall + gpi_militarisation + advisory-consensus A (synthetic)
crime    (25%) = gpi_safety_security   <-- MOVED here from conflict; this is "societal safety & security", NOT rule of law
health   (20%) = inform_health + inform_epidemic + wb_child_mortality
governance(15%)= vdem_rule_of_law (MOVED here) + vdem_corruption_control + vdem_gov_effectiveness + inform_governance
environment(10%)= inform_natural + inform_climate
score = 1 + 9·composite^0.79 ; confidence = Σ w_i·n_i/(n_i+K) (NEW ScoredCountry field, K=1.0)
<!-- pillar.score (PillarScore.score) is the SHRUNK 0–1 value — do NOT recalibrate 0–1 pillar thresholds. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recalibrate every 1–10 band/verdict/color threshold to the v9 scale</name>
  <files>src/components/country/ScoreHero.astro, src/components/country/AnswerFirstParagraph.astro, src/components/country/AuthorityLinks.astro, src/components/country/ComparisonTable.astro, src/components/country/NeighborComparison.astro, src/components/country/RelatedCountries.astro, src/components/hub/CountryRankRow.astro, src/lib/seo.ts, src/lib/map-utils.ts, src/pages/badge/[iso3].svg.ts, scripts/generate-llms-full.ts, scripts/generate-og-images.ts</files>
  <action>
Recalibrate all overall-score (1–10) thresholds to the canonical v9 bands. Before editing, re-run
the grep gate below and confirm the site:line inventory matches; if the grep surfaces a threshold
NOT in this list, recalibrate it too and record it in the summary.

Grep gate (must be re-run, output pasted into the task summary):
`grep -rnE "(country\.)?score\s*(>=|<=|>|<)\s*[0-9]" src/ scripts/ | grep -vE "\.test\.|__tests__|pipeline/"`

Recalibration map (preserve semantics; verify each against public/scores.json):
1. THREE-WAY low/moderate/high (positive / mid / negative). Old thresholds `>=7 / >=4`.
   NEW: positive/low = `>= 7`, negative/high = `< 5`, moderate = 5–7 (i.e. replace the `>= 4`
   split with `>= 5`). Apply at: seo.ts:109 (riskLevel), seo.ts:302 (bandIdx), seo.ts:559
   (riskBand), AnswerFirstParagraph.astro:55, CountryRankRow.astro:20-21, NeighborComparison.astro:25-26,
   RelatedCountries.astro:30-31, ComparisonTable.astro:20-21 (getScoreColor), badge/[iso3].svg.ts:21-24,
   generate-llms-full.ts:102-103 (riskLabel) and 225-227 (verdict). This keeps Italy/France/Greece
   (7.8–8.0) positive and Yemen/Afghanistan/Syria (3.7–4.2) negative; verify Jamaica 7.36 lands in
   the positive band and confirm the "low" a1Verdict copy is not absolute ("unreservedly safe") —
   if it is, soften it in Task 2 and note the coupling.
2. ScoreHero.astro:54 FIVE-band verdict. Old `>=8 excellent / >=6 good / >=4.5 caution / >=3 risky / else danger`.
   NEW canonical: `>=8 excellent / >=7 good / >=6 caution / >=5 risky / else danger` (keys unchanged).
3. AuthorityLinks.astro:77 `country.score >= 6` (safest-first ordering) → `>= 7` to align with the
   positive band under the new mean (6.75); document the choice.
4. generate-og-images.ts:29-30 OG color: `>=7 green / >=4 amber / else red` → `>=7 green / >=5 amber / else red`.
5. map-utils.ts:68-72 d3 colorScale `.domain([1, 5.5, 10])`: the yellow/MODERATE anchor at 5.5 is
   now below the real distribution centre, so the whole world renders too blue. Re-anchor the middle
   stop to the new distribution centre (~6.5) — e.g. `.domain([3.7, 6.5, 8.9])` clamped — so red maps
   to the <5 danger band, yellow to the 6–7 moderate centre, blue to ≥8 excellent. Keep the two hex
   anchors; adjust the DANGER/MODERATE/SAFE comment lines to the new anchor scores. Document the exact
   domain chosen.

EXCLUDE (do NOT change) — these are 0–1 pillar-score thresholds, not the 1–10 country score:
PillarBreakdown.astro:70 and PillarDetailTable.astro:36 (`pillar.score >= 0.7 / >= 0.4`). Confirm
`PillarScore.score` is still the shrunk 0–1 value (per PART 1 SUMMARY) and leave them; note the
exclusion + rationale in the summary. Also leave map-utils SafetyMap tooltip's 0-1→1-10 pillar
conversion untouched.

Report a before/after verdict-count table in the summary: for the 3-way and the 5-band schemes,
how many of the ~248 countries fall in each bucket under OLD vs NEW thresholds (compute from
public/scores.json). Flag any Sì→Ni-style flips at the political anchors named above.
  </action>
  <verify>
    <automated>grep -rnE "(country\.)?score\s*(>=|<=|>|<)\s*[0-9]" src/ scripts/ | grep -vE "\.test\.|__tests__|pipeline/" | grep -vE ">= ?7|< ?5|>= ?5|>= ?8|>= ?6|0\.7|0\.4" ; echo "exit_scan_done"</automated>
  </verify>
  <done>Every 1–10 threshold in the grep inventory is recalibrated to {5,6,7,8} boundaries (or documented exclusion); `npx tsc --noEmit -p .` has no NEW errors vs the PART 1 baseline; before/after verdict-count table produced; political anchors verified against public/scores.json.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite v9 documentation & strings across 7 locales + validation report</name>
  <files>src/i18n/ui.ts, src/pages/en/methodology/index.astro, src/pages/it/metodologia/index.astro, src/pages/es/metodologia/index.astro, src/pages/fr/methodologie/index.astro, src/pages/pt/metodologia/index.astro, src/pages/zh/methodology/index.astro, src/pages/de/methodik/index.astro, README.md, src/lib/seo.ts, src/lib/country-faq-copy.ts, src/lib/hub-faq.ts, src/components/ApiDocs.astro, data/SCORE-VALIDATION.md</files>
  <action>
Rewrite every string describing the scoring mechanism to v9, per SHIP-SPEC 2.2 (read it). Weights
30/25/20/15/10 stay — keep all percentages. Translate each locale PROPERLY per language; the zh
methodology page is a KNOWN untranslated EN copy — update its EN content identically (do NOT
translate it to Chinese in this task). All 7 locales: en, it, es, fr, pt, zh, de.

A. src/i18n/ui.ts methodology.* strings (×7 locales — each locale block repeats the keys):
   - `methodology.pillar.crime.description`: currently "strength of rule of law and legal
     protections" — REWRITE to GPI Safety & Security (homicide, violent crime, perceived
     criminality / societal safety). This is the single most important correctness fix.
   - `methodology.pillar.governance.description`: ADD rule of law (moved here) alongside
     institutional effectiveness + corruption.
   - `methodology.pillar.conflict.description`: keep advisories + militarisation; it no longer
     carries societal safety/crime.
   - `methodology.overview_text`: REMOVE "hard cap … Do Not Travel", "critical floor … danger
     threshold", and "advisory-based blending for … limited data". REPLACE with v9: uncertainty-
     weighted (Bayesian shrinkage toward a conservative prior), weighted geometric mean of 5
     pillars, smooth acute-risk emphasis on conflict & crime, calibrated importance-weighted
     government-advisory consensus; no hard caps or floors.
   - `methodology.formula_text`: REMOVE the three numbered safety mechanisms (majority-L4 cap,
     critical threshold cap, low-coverage advisory blend). REPLACE with the shrinkage formula
     `p̂ = (n·p + K·μ)/(n+K)` in words + geometric aggregation + down-only acute soft-min +
     count-damped severe-advisory modifier.
   - `methodology.tiered_text` / `methodology.weights_text` / `methodology.decay_text`: keep true
     statements; fix any that assert crime=rule-of-law or the removed caps/floors/blend or a rigid
     70/30 tiering if it no longer holds verbatim (freshness decay via ρ_eff still applies — keep).
   - ADD new keys (×7 locales) rendered by the methodology page: a band-legend, a "What changed in
     v9 and why" changelog paragraph (old failures: rule-of-law-as-crime, hard floors/caps,
     sparse-data inflation), a short "Known limitations" note (annual GPI lag for active conflicts
     e.g. Palestine; thin-advisory micro-territories carry low confidence; Guam ca-advisory data
     bug pending), and a one-line `confidence` output description.
B. The 7 methodology twin .astro pages each hardcode an inline formula box (en page ≈ lines 171–182):
   `Composite = exp(Σ(w_i·ln(Pillar_i)))` (KEEP) followed by three lines — "Hard cap: …",
   "Critical floor: … < 0.25 …", "Sparse data: … blend …" (DELETE all three). Add render calls for
   the new ui.ts keys from (A) (band legend + changelog + known limitations + confidence) into each
   twin's body. Verify the pillar-weights table still renders v9 from weights.json. Edit all 7 twins
   (en/it/es/fr/pt/zh/de) identically in structure.
C. README.md "Scoring Methodology" section (currently cites v5.3.0): full v9 rewrite — bump version,
   keep the weights table, replace tiered "~70% / up to 30%" + any hard-cap/floor prose with the v9
   mechanism list (advisory calibration, Bayesian shrinkage, bands, confidence). Fix line ~24 and
   line ~62 (signal-influence-cap phrasing) if no longer accurate.
D. src/lib/seo.ts:
   - measurementTechniqueByLang (7 locales, ~lines 227–234) + the 2 inline English literals at
     ~717 and ~795: replace all THREE identical English strings (and the 6 translations) with one
     consistent v9 description: "Uncertainty-weighted (Bayesian shrinkage) weighted geometric mean
     of 5 category scores from 40+ public sources including government advisories, World Bank, INFORM
     and GPI indices."
   - Q3 FAQ hard-cap branch (~line 666–668): the comment "Level-4 hard cap fires only on a strict
     MAJORITY" and the `a3Parts.push(copy.a3Cap)` no longer describe a cap. Keep the majority-L4
     trigger but its appended text (a3Cap, see E) must describe severe-advisory DOWN-WEIGHTING toward
     a low score, not a cap to 2/10.
E. src/lib/country-faq-copy.ts (×7 locales): `a1Formula` — keep geometric-mean-of-5-pillars +
     weights, ADD uncertainty weighting + advisory consensus wording. `a3Cap` — reword away from
     "caps the score at 2/10" to "weighs heavily toward a low score" (no hard cap in v9). Fix the
     interface JSDoc at line ~24/40 that references the removed cap. Soften the "low" a1Verdict copy
     if Task 1 flagged Jamaica-7.36 reading as unreservedly safe.
F. src/lib/hub-faq.ts (×7 locales): line ~159 ("a Level 4 advisory caps a country's overall score
     at 2 out of 10") and line ~286 ("automatically caps a country's score at 2") are now FALSE —
     reword to v9 severe-advisory down-weighting. Percentages stay. Fix any crime=rule-of-law phrasing.
G. src/components/ApiDocs.astro (×7 locales): `fScore` description → "uncertainty-weighted geometric
     mean"; `fPillars` description → crime = GPI Safety & Security; ADD a `confidence` field entry
     (0–1, precision-weighted data density) to the fields list + example. Match the JSON field-name
     convention (identifiers stay English).
H. data/SCORE-VALIDATION.md: add a dated v9 section (per SHIP-SPEC 2.2) — "What changed in v9"
     (weights recomposition, shrinkage, removed caps/floors/blend, advisory calibration, new range
     ~[3.72, 8.89] mean 6.75, confidence) + "Known limitations" sourced from final-constants.json
     `residual_failures`: PSE stale-GPI / moderate-consensus (5.63), GUM ca=4 pipeline data bug
     (5.70), ESH single-source advisory step, RUS-vs-UKR ordering. Keep the old dated section above/
     below; append, don't overwrite.

Do NOT hand-edit generated files (public/llms*, public/scores.json, data/scores/**, public/og/**);
those are Task 3. SCORE-VALIDATION.md and README.md are source docs — edit directly.
  </action>
  <verify>
    <automated>grep -rniE "rule of law" src/i18n/ui.ts | grep -i crime | grep -v '^#' | grep -c . | grep -qx 0 && grep -riE "caps? .{0,20}(score|country).{0,20}(at )?2" src/lib/hub-faq.ts src/lib/country-faq-copy.ts | grep -v '^ *//' | grep -c . | grep -qx 0 && echo PASS || echo "FAIL: stale cap/rule-of-law strings remain"</automated>
  </verify>
  <done>No string asserts crime=rule-of-law, a Level-4 hard cap, a 0.25 critical floor, or a sparse-data blend; measurementTechnique consistent v9 ×7; new confidence + band legend + "what changed" + "known limitations" present in methodology ×7 and SCORE-VALIDATION.md; `npx tsc --noEmit -p .` no new errors; all 7 locales edited (zh methodology kept as EN copy).</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate artifacts, pass SEO gate, spot-check, ship</name>
  <files>scripts/generate-llms-full.ts, public/llms.txt, public/llms-full.txt, public/og/**</files>
  <action>
Run the gates in order (SHIP-SPEC 2.3 items 4–7). All must pass before pushing.

1. scripts/generate-llms-full.ts: fix the hardcoded mechanism sentence (~line 201) — it still
   describes the geometric mean PLUS a "hard cap … 2/10" and a "critical floor … below 2.5/10".
   Rewrite to v9 (uncertainty-weighted geometric mean, advisory calibration, shrinkage, confidence,
   new range/bands). MIN_RANKING_SOURCES=4 (line ~135) UNCHANGED — and hub-data.ts hasSufficientData
   UNCHANGED — so zero-data prior-band territories stay OUT of rankings. Also update the riskLabel /
   verdict thresholds here to the Task 1 map if not already covered.
2. `npm run generate:llms` — regenerates public/llms.txt + public/llms-full.txt (fast). Confirm the
   new v9 sentence appears and no "hard cap"/"critical floor" text remains.
3. `npm run build` — FULL build (~400s; OG images MUST regenerate because scoreDisplay + band colors
   changed). Use timeout 600000. This runs generate:og → generate:llms → astro build → validate:seo.
4. `npm run validate:seo` — must be ALL-PASS. If build already ran it, re-run standalone to confirm.
   If it fails, fix the offending source (not dist) and rebuild.
5. Spot-check dist/client (per SHIP-SPEC 2.3 item 7):
   - Italy country page: verdict/band render positive, score 7.9 shown, methodology link works.
   - Nicaragua country page: renders with recalibrated verdict/colors.
   - `dist/client/trend/NIC.json` reflects the v9 history (recomputed scores).
   - global-safety page: global score ≈ 6.75 renders.
   - grep a built country page for absence of "hard cap"/"critical floor"/"rule of law" in the
     crime description.
6. Ship: atomic commits per task (T1 recalibration / T2 docs+strings / T3 llms+og+validation).
   Then `git pull --rebase --autostash` (daily bot commits often), then push to master
   (= production deploy, per project convention).

Never hand-edit public/llms*, public/og/**, public/scores.json, data/scores/** — only via the
scripts/build above.
  </action>
  <verify>
    <automated>test -f dist/client/trend/NIC.json && grep -L -riE "critical floor|hard cap" public/llms-full.txt >/dev/null && ! grep -riE "critical floor|hard cap" public/llms-full.txt && npm run validate:seo 2>&1 | tail -5</automated>
  </verify>
  <done>generate:llms + full build succeed; validate:seo all-pass; OG images regenerated (public/og mtimes fresh); dist spot-checks confirm v9 verdicts/scores; llms-full.txt has the v9 mechanism sentence and no removed-mechanism text; committed and pushed to master with rebase.</done>
</task>

</tasks>

<verification>
- `npm run validate:seo` all-pass against a fresh full build (OG + llms regenerated).
- `npx tsc --noEmit -p .` shows no NEW errors vs the PART 1 baseline (2 pre-existing astro.config/functions errors are acceptable).
- Grep gate: no 1–10 threshold left on the old {4, 4.5, 3} boundaries (except the documented 0–1 pillar exclusions); no string claims crime=rule-of-law, an L4 hard cap, a 0.25 critical floor, or a sparse-data blend.
- Political anchors verified against public/scores.json: Italy/France/Greece positive; Yemen/Afghanistan/Syria negative; before/after verdict-count table produced.
- All 7 locales edited (zh methodology kept as identical EN copy).
</verification>

<success_criteria>
- Every band/verdict/color threshold reflects the v9 range (~[3.72, 8.89], mean 6.75).
- Methodology ×7 + README + ApiDocs + SEO + FAQ describe v9 accurately (uncertainty-weighted geometric mean, crime = GPI Safety & Security, confidence, new bands; no removed v8 mechanisms).
- SCORE-VALIDATION.md has a dated v9 section incl. known limitations (PSE/GUM/ESH/RUS-UKR).
- public/llms*.txt + public/og/** regenerated via scripts/build (not hand-edited); validate:seo all-pass.
- Committed as atomic per-task commits and pushed to master.
</success_criteria>

<output>
Create `.planning/quick/260708-23u-formula-v9-part-2-bands-verdicts-methodo/260708-23u-SUMMARY.md` when done.
Include: the grep-gate threshold inventory (file:line), the before/after verdict-count table, the map-utils domain chosen, the AuthorityLinks threshold decision, and any Jamaica-7.36 copy softening.
</output>
