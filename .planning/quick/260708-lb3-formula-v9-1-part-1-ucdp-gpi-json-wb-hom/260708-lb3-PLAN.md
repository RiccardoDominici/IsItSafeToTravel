---
phase: quick-260708-lb3
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [SHIP-SPEC-V91-1.1-fetchers, SHIP-SPEC-V91-1.2-engine-config, SHIP-SPEC-V91-1.3-parity-tests-regen]
files_modified:
  - src/pipeline/fetchers/worldbank.ts
  - src/pipeline/fetchers/ucdp.ts
  - src/pipeline/fetchers/gpi.ts
  - src/pipeline/fetchers/index.ts
  - src/pipeline/fetchers/advisories-tier1.ts
  - src/pipeline/fetchers/advisories.ts
  - src/pipeline/run.ts
  - src/pipeline/backfill.ts
  - src/pipeline/config/weights.json
  - src/pipeline/scoring/normalize.ts
  - src/pipeline/scoring/engine.ts
  - src/pipeline/types.ts
  - scripts/verify-formula-v91-parity.ts
  - scripts/fixtures/formula-v91-parity.csv
  - scripts/correct-gum-ca-history.ts
  - src/pipeline/scoring/__tests__/engine.test.ts
  - src/pipeline/__tests__/weights-config.test.ts
  - src/pipeline/__tests__/crisis-validation.test.ts
  - data/scores/**

must_haves:
  truths:
    - "The pipeline fetches WB homicide (VC.IHR.PSRC.P5) + population (SP.POP.TOTL) + restored air pollution, all mrnev-gated"
    - "A new UCDP conflict-deaths signal (OWID mirror, interstate+intrastate+one-sided, excludes non-state) flows into the conflict pillar"
    - "GPI overall/safety/militarisation are sourced from the IEP GPI-2026 JSON manifest (xlsx fallback preserved)"
    - "The v9.1.0 engine reproduces the frozen prototype: max |Δ| < 0.02 across 248 countries vs new-scores-v91.csv (jp excluded)"
    - "The rewritten jp fetcher maps ≥180/206 MOFA pages to real 1-4 levels (RUS/UKR/IRQ/IRN=4, FRA/USA/SGP/AUS/CAN/GBR/VAT/CHN=1, MEX/ECU=3, BHR/XKX=2, BLR=4)"
    - "All ~669 historical snapshots + latest.json + history-index.json + public/scores.json are regenerated under v9.1.0"
    - "Sanity anchors hold on the live regen: mean ≈6.6, ISL top ≈8.87, YEM bottom ≈3.42, JAM ≈6.94, TCA ≈5.46, PSE ≈4.61, UKR ≈3.85"
    - "npm test is green"
  artifacts:
    - path: "src/pipeline/fetchers/ucdp.ts"
      provides: "UCDP consolidated all-years conflict-deaths fetcher (OWID mirror)"
      contains: "ucdp_conflict_deaths"
    - path: "src/pipeline/config/weights.json"
      provides: "v9.1.0 pillar recomposition + v9.1 formula constants"
      contains: "9.1.0"
    - path: "src/pipeline/scoring/engine.ts"
      provides: "v9.1 ramp gates + population-scaled homicide precision"
      contains: "rampFactor"
    - path: "scripts/fixtures/formula-v91-parity.csv"
      provides: "Frozen 248-row parity ground truth (v91_score column)"
      contains: "v91_score"
    - path: "src/pipeline/scoring/normalize.ts"
      provides: "wb_homicide {0,60,inverse} + ucdp_conflict_deaths log-decay branch"
      contains: "ucdp_conflict_deaths"
  key_links:
    - from: "src/pipeline/fetchers/worldbank.ts"
      to: "World Bank API"
      via: "mrnev=1 on every indicator query"
      pattern: "mrnev"
    - from: "src/pipeline/scoring/engine.ts"
      to: "weightsConfig.formulaV9.P_HALF"
      via: "wb_homicide rho *= pop/(pop+P_HALF)"
      pattern: "P_HALF"
    - from: "src/pipeline/backfill.ts"
      to: "ucdp-parsed.json all-years"
      via: "injectUcdpForDate vintage selection"
      pattern: "injectUcdpForDate"
    - from: "scripts/verify-formula-v91-parity.ts"
      to: "scripts/fixtures/formula-v91-parity.csv"
      via: "per-country diff < 0.02"
      pattern: "v91_score"
---

<objective>
Ship Formula v9.1 PART 1 — the pipeline core (SHIP-SPEC sections 1.1–1.3): new/hardened
data fetchers (WB homicide+population+restored-air, a NEW UCDP conflict-deaths fetcher, GPI
sourced from the 2026 JSON manifest, a rewritten Japan advisory fetcher, Canada parser
hardening, and a scripted GUM history correction), an exact port of the frozen v9.1 scoring
math into engine.ts/normalize.ts/weights.json, and a full historical regeneration gated by a
blocking parity check.

Purpose: v9.1 adds real crime signal (population-reliability-scaled homicide) and a real
conflict-deaths signal (UCDP GED) on top of v9's Bayesian-shrinkage core, resolving four v9
residuals (Caribbean deflation via pop-scaling, PSE/Gaza sink, Russia-vs-war-band, ESH
single-source cliff). The prototype math is FROZEN — this plan ports it faithfully and the
parity gate arbitrates.

Output: v9.1.0 engine + config, ~669 regenerated snapshots + latest + history-index +
public/scores.json, a committed parity fixture, updated tests. Docs/i18n/UI and push are
PART 2 — explicitly out of scope here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

# THE FROZEN CONTRACT — this plan implements PART 1 (sections 1.1–1.3) ONLY.
@/private/tmp/claude-501/-Users-riccardo-Developer-VibeCoding-Isitsafetotravel/dda1ae2e-57b7-4d23-9610-4f92d047c45e/scratchpad/V91-SHIP-SPEC.md

# Frozen reference implementation — port math VERBATIM; do not re-derive.
@/private/tmp/claude-501/-Users-riccardo-Developer-VibeCoding-Isitsafetotravel/dda1ae2e-57b7-4d23-9610-4f92d047c45e/scratchpad/prototype-scorer-v91.mjs

# Frozen FINAL constants (use the round-2 section: P_HALF=200000; reproduce flags therein).
@/private/tmp/claude-501/-Users-riccardo-Developer-VibeCoding-Isitsafetotravel/dda1ae2e-57b7-4d23-9610-4f92d047c45e/scratchpad/final-constants-v91.json

# Parity ground truth (248 rows, column `v91_score`) — copy verbatim into scripts/fixtures/.
@/private/tmp/claude-501/-Users-riccardo-Developer-VibeCoding-Isitsafetotravel/dda1ae2e-57b7-4d23-9610-4f92d047c45e/scratchpad/new-scores-v91.csv

# Staged v9.1 data inputs + the exact source URLs/columns the prep agent used.
# v91-data/{homicide,air,ucdp,gpi2026,pop}.csv + build_csvs.py (authoritative recipe);
# gpi-years/gpi-{2008..2026}.json (IEP manifest dumps); OWID grapher CSVs in scratchpad root.
@/private/tmp/claude-501/-Users-riccardo-Developer-VibeCoding-Isitsafetotravel/dda1ae2e-57b7-4d23-9610-4f92d047c45e/scratchpad/v91-data/build_csvs.py

# How the v9 ship worked — REUSE its parity-gate + backfill-injection + latest.json
# backup/restore patterns; heed its Issues Encountered (snapshot.test.ts latest.json clobber).
@.planning/quick/260706-x81-formula-v9-part-1-engine-rewrite-weights/260706-x81-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs; extracted from the codebase. Use directly, no exploration. -->

RawIndicator (src/pipeline/types.ts) ALREADY carries optional `fetchedAt?` and `dataDate?`
(both ISO 8601) — no type change needed for the 1.1a dataDate work.

GPI injection pattern to MIRROR for UCDP + WB-history (src/pipeline/fetchers/gpi.ts):
  export function parseGpiExcelAllYears(buffer): Map<number, RawIndicator[]>
  export function selectGpiIndicatorsForYear(byYear, snapshotYear): RawIndicator[]
    // year = largest available <= snapshotYear; else earliest.

backfill.ts ALREADY has (mirror these):
  const YEAR_BASED_SOURCES = new Set(['worldbank','vdem']);   // NOT gpi (injected)
  function injectGpiForDate(date, rawDataMap): void            // reads own/newest gpi.xlsx
  function loadRawDataForDate(date): Map<string,RawSourceData> // calls injectGpiForDate
  const gpiByYearCache = new Map<string, Map<number,RawIndicator[]>>();  // parse-once cache

run.ts Stage 2 loads *-parsed.json directly into rawDataMap and does NOT inject. Any
all-years consolidated source (UCDP) MUST have its vintage-selection injection called in
BOTH backfill.loadRawDataForDate AND run.ts Stage 2, or live scoring will pick an arbitrary
year (engine's rawByName Map.set keeps the last row per indicatorName).

engine.ts computeCountryScore builds `rawByName: Map<string,RawIndicator>` from the country's
indicators (line ~300). Population rides as a non-scored `wb_population` RawIndicator
(normalize.ts has no range for it → normalizeIndicators skips it), read via
rawByName.get('wb_population')?.value for P_HALF scaling.

engine.ts reads all constants from `weightsConfig.formulaV9 ?? DEFAULT_FORMULA_V9` (typed
FormulaV9Config). Add the 5 new v9.1 tunables (D_MAX, P_HALF, nudgeMinNAdv, severeMinNAdv,
gateRampWidth) to: weights.json formulaV9 section, DEFAULT_FORMULA_V9, and the FormulaV9Config
type. Keep the section KEY name `formulaV9` (engine consumes it) — only bump VALUES to v9.1.

Prototype exports to port (prototype-scorer-v91.mjs): rampFactor, advisoryConsensus (severeEff
ramp), computePrior (nudge ramp), homicidePrecisionFactor, ucdpLogNorm, resolveV91Indicator
(gpi/homicide/air ranges + ucdp branch), buildPillarSpec (crime/conflict compositions).

fetchers/index.ts registers base sources in `settleBatch([...])` at Batch 1/7 — add ucdp there.

OWID grapher CSV download URL: https://ourworldindata.org/grapher/<slug>.csv?csvType=full&useColumnShortNames=true
  interstate  slug deaths-in-interstate-conflicts-by-country   col number_deaths_ongoing_conflicts__conflict_type_interstate
  intrastate  slug deaths-in-intrastate-conflicts-by-country   col number_deaths_ongoing_conflicts__conflict_type_intrastate
  one-sided   slug deaths-from-one-sided-violence              col number_deaths_ongoing_conflicts__conflict_type_one_sided_violence
  CSV columns: entity,code,year,<col>  — `code` is iso3. NON-STATE is EXCLUDED. Sum the 3 per (iso3,year).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetchers — WB (homicide+population+air+dataDate), NEW UCDP, GPI-JSON, JP rewrite, CA hardening, GUM fix</name>
  <files>src/pipeline/fetchers/worldbank.ts, src/pipeline/fetchers/ucdp.ts, src/pipeline/fetchers/gpi.ts, src/pipeline/fetchers/index.ts, src/pipeline/fetchers/advisories-tier1.ts, src/pipeline/fetchers/advisories.ts, scripts/correct-gum-ca-history.ts</files>
  <action>
Implement all of SHIP-SPEC 1.1 (a–f). Read the spec + build_csvs.py (authoritative source recipe) + prototype resolveV91Indicator before writing; the staged v91-data/*.csv are the expected outputs and may be used to spot-check the live fetchers. Every fetcher keeps the existing try/cached-fallback shape (findLatestCached) and writes its RawSourceData to data/raw/&lt;date&gt;/&lt;source&gt;-parsed.json.

(1.1a) worldbank.ts: append `&mrnev=1` to EVERY indicator query URL (this is why wb_air_pollution is silently empty today and why homicide returns 0 rows). Add wb_homicide (VC.IHR.PSRC.P5) and wb_population (SP.POP.TOTL) to the INDICATORS list. wb_population is an INTERNAL engine input, NOT a scored indicator — it is persisted in worldbank-parsed.json as an ordinary RawIndicator named `wb_population` but has NO entry in normalize.ts (so normalizeIndicators skips it) and appears in NO pillar. Set RawIndicator.dataDate from each indicator's own vintage year (e.g. `${entryYear}-01-01T00:00:00.000Z`) so freshness honesty is possible. GATE the decay flip per 1.1a: dataDate is now populated, and engine.computeIndicatorFreshness reads `raw.dataDate ?? raw.fetchedAt`, so this WILL start decaying WB baseline unless prevented. Because the parity CSV assumes fw=1.0, the port must NOT let dataDate-based decay move scores vs the prototype: either (i) keep worldbank OUT of source-tiers.json's decay config (verify — if absent, fw stays 1.0), or (ii) if it decays and breaks parity &gt;0.02 in Task 3, keep persisting dataDate but make freshness fall back to fetchedAt for WB (documented follow-up). Decide via the Task 3 parity gate; ship dataDate persistence regardless.

(1.1b) NEW ucdp.ts (fetchUcdp(date)): fetch the 3 OWID grapher CSVs (interstate, intrastate, one-sided — see &lt;interfaces&gt; for slugs/columns; EXCLUDE non-state). Parse `code` as iso3, filter to project countries (getCountryByIso3), sum the 3 death counts per (iso3, year) for years 1989→latest. Write ONE consolidated data/raw/&lt;date&gt;/ucdp-parsed.json as RawSourceData{source:'ucdp'} with one RawIndicator per country-year (indicatorName 'ucdp_conflict_deaths', value=deaths, year=year). Attribution note in a comment: UCDP GED via OWID mirror, CC-BY. Retry/fallback: per-CSV timeout (AbortSignal.timeout) + findLatestCached('ucdp-parsed.json'). Register fetchUcdp in fetchers/index.ts Batch 1/7 settleBatch and add the `export`. (Live-scoring + backfill vintage SELECTION from this all-years file is wired in Task 3's backfill work.)

(1.1c) gpi.ts: add a JSON primary source. parseGpiJsonAllYears() returns Map&lt;year, RawIndicator[]&gt; (SAME shape as parseGpiExcelAllYears) by reading https://gpi.economicsandpeace.org/data/gpi/manifest.json then /data/gpi/{year}.json for 2008–2026, taking fields overall/safety/militarisation (JSON entries carry iso3 + overall/safety/militarisation — see gpi-years/gpi-2026.json). fetchGpi's live path: prefer JSON latest-year (persist gpi-parsed.json latest-per-country as today) AND persist the full all-years JSON for backfill; fallback chain → existing 2023 xlsx URL → last cached. Keep parseGpiExcelAllYears intact as the xlsx fallback. Document that retroactive vintage revisions are expected.

(1.1d) advisories-tier1.ts fetchJpAdvisories REWRITE: build a dynamic country index from https://www.anzen.mofa.go.jp/riskmap/ (regex href=/info/pcinfectionspothazardinfo_(\d+)\.html → {id, japaneseName}, ~206 entries); persist data/raw/&lt;date&gt;/advisories-jp-index.json. Per country page, SCOPE parsing to div#kikendetail only; read kiken_level_{chuui:1|kentou:2|enki:3|taihi:4}, take MAX across regional blocks. Build a careful Japanese-name→ISO3 mapping table; VALIDATE ≥180 of 206 mapped and spot-check 10 against page prefName; UNMATCHED names → skip with a warning, NEVER guess. The rewrite MUST reproduce these discovery anchors (staged jp_*.html pages in scratchpad are the reference fixtures): RUS/UKR/IRQ/IRN=4, FRA/USA/SGP/AUS/CAN/GBR/VAT/CHN=1, MEX/ECU=3, BHR/XKX=2, BLR=4. Keep the existing normalizeJpLevel/JP_LEVEL_TEXT output contract. (The excluded/re-included decision for jp is a Task 3 gate — this task only fixes the fetcher so it emits real 1-4 levels instead of the spurious constant-L4.)

(1.1e) advisories.ts parseCaAdvisoryLevel HARDENING: collect ALL banner-* matches (global regex), not the first. &gt;1 distinct banner type ⇒ ambiguous ⇒ use the previous day's level for that iso3 (read data/raw/&lt;prev&gt;/advisories-info.json), and log loudly. Dedupe entries by iso3 BEFORE fetchBatch. Persist data/raw/&lt;date&gt;/advisories-ca-debug.json as {iso3, allBannerMatches, chosenLevel}[].

(1.1f) scripts/correct-gum-ca-history.ts: a one-off tsx script that sets GUM's ca advisory level 4→1 in data/raw/2026-07-0{4,5,6,7}/advisories-info.json (verified transient misparse) via fs read/write — NEVER hand-edit (the PreToolUse hook blocks Edit/Write on data/raw/**; a script executed via Bash is fine). Run it once; the Task 3 backfill then cleans the 4-day blip.

Do NOT run the full daily pipeline blindly — run individual fetchers as needed to produce/refresh the 2026-07-08 raw override files (worldbank, ucdp, gpi) used by the parity gate. Network is available; use the staged v91-data CSVs as a fallback seed for the 2026-07-08 override raw if a live source is momentarily revised/unreachable (parity arbitrates the PORT, not intraday source drift).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p . 2>&1 | grep -v 'astro.config.mjs\|functions/' | grep -c 'error TS' | grep -qx 0 && echo TSC_CLEAN</automated>
  </verify>
  <done>worldbank-parsed.json for 2026-07-08 contains wb_homicide + wb_population + non-empty wb_air_pollution (mrnev); ucdp-parsed.json exists with 1989→ country-year rows; gpi JSON all-years persisted; fetchJpAdvisories reproduces all discovery anchors with ≥180/206 mapped; ca parser collects all banners + writes advisories-ca-debug.json; GUM correction script has run against the 4 dated files; tsc clean (excluding pre-existing astro.config/functions errors).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Engine / config / normalize v9.1.0 — exact port from the frozen prototype + tests</name>
  <files>src/pipeline/config/weights.json, src/pipeline/scoring/normalize.ts, src/pipeline/scoring/engine.ts, src/pipeline/types.ts, src/pipeline/scoring/__tests__/engine.test.ts, src/pipeline/__tests__/weights-config.test.ts, src/pipeline/__tests__/crisis-validation.test.ts</files>
  <behavior>
    - Crime pillar = gpi_safety_security (subweight 0.67, rho 2.5) + wb_homicide (subweight 0.33, rho 1.5); subweights sum to 1.0.
    - Conflict pillar = gpi_overall (0.3275, rho 2.0) + gpi_militarisation (0.1125, rho 1.0) + A (0.28, rho per-country) + ucdp_conflict_deaths (0.28, rho 2.5); subweights sum to 1.0.
    - wb_homicide normalizes {min 0, max 60, inverse}; ucdp_conflict_deaths uses norm(d)=clamp(1−log10(1+d)/log10(1+D_MAX),0,1) with D_MAX=24000, rawValue stays the death count.
    - Homicide precision is population-scaled: effective rho = 1.5 * pop/(pop+P_HALF), P_HALF=200000; missing population ⇒ factor 1 (LARGE). Monotonic: larger pop ⇒ homicide pulls the crime pillar harder off the prior. gpi_safety_security rho is NOT population-scaled.
    - Advisory nudge ramp gates the prior nudge (rampFactor(nAdv, nudgeMinNAdv=1, gateRampWidth=2.4) ⇒ 0.417 at nAdv=1) and severeEff (rampFactor(nAdv, severeMinNAdv=2, gateRampWidth=2.4) ⇒ 0 at nAdv≤1).
    - Constants: gamma 0.96, S_MAX 0.32, D_MAX 24000, P_HALF 200000, N_SEV 6 (K/lambda/q/muBase/clamps/nudgeScale/nudgeDenom/rhoADenom/floorP/acuteDownOnly carried from v9).
  </behavior>
  <action>
Port the v9.1 math VERBATIM from prototype-scorer-v91.mjs using final-constants-v91.json's round-2 section as the authoritative constant values (P_HALF=200000; reproduce-flag line at top of that file). Do NOT re-tune — the parity gate (Task 3) arbitrates.

weights.json: bump `version` to "9.1.0". Recompose crime + conflict pillars per &lt;behavior&gt; (indicators/indicatorWeights/indicatorPrecision arrays; 'A' stays with no precision entry — computed per-country). In the `formulaV9` section (KEEP the key name; engine reads it) set gamma=0.96, S_MAX=0.32, and ADD the 5 new tunables D_MAX=24000, P_HALF=200000, nudgeMinNAdv=1, severeMinNAdv=2, gateRampWidth=2.4; advisoryTiers/frozenExcludedSources/advisoryRebase/regionOffset UNCHANGED (frozenExcludedSources KEEPS 'jp' for now — Task 3's re-inclusion gate decides whether to remove it).

types.ts: add D_MAX, P_HALF, nudgeMinNAdv, severeMinNAdv, gateRampWidth to FormulaV9Config.

engine.ts: mirror DEFAULT_FORMULA_V9 to the new values + fields. Add rampFactor(nAdv, minNAdv, width) ported from the prototype. In advisoryConsensus, multiply severeEff by rampFactor(nAdv, f9.severeMinNAdv, f9.gateRampWidth). In computePrior, gate the advisory-nudge term by rampFactor(nAdv, f9.nudgeMinNAdv, f9.gateRampWidth). In the per-pillar loop, when indName==='wb_homicide', scale its rho by pop/(pop+f9.P_HALF) where pop=rawByName.get('wb_population')?.value (missing ⇒ factor 1); never scale the value, never scale any other indicator.

normalize.ts: add wb_homicide {min:0,max:60,inverse:true}. Add a custom code path in normalizeIndicators for ucdp_conflict_deaths → the log-decay above, keeping rawValue=deaths; put D_MAX as a module const with a comment to KEEP IT IN SYNC with weights.json formulaV9.D_MAX (same discipline as MIN_RANKING_SOURCES per CLAUDE.md). Do NOT add wb_population to INDICATOR_RANGES (it must stay unscored). wb_air_pollution range is already {0,100,inverse} — matches the prototype; leave it.

Tests (RED→GREEN): update engine.test.ts / weights-config.test.ts / crisis-validation.test.ts for v9.1 — new pillar compositions + subweight sums, new constants, ramp behavior (severeEff=0 at nAdv≤1; partial nudge at nAdv=1), homicide pop-scaling monotonicity (larger pop ⇒ larger effective rho ⇒ crime pillar closer to raw homicide). Preserve the latest.json backup/restore patterns already in the suite (per x81 SUMMARY's snapshot.test.ts clobber hazard — run `git checkout -- data/scores/latest.json` after any `npm test` before trusting it). Keep any ScoredCountry literal fixtures compiling.
  </action>
  <verify>
    <automated>node --import tsx --test src/pipeline/scoring/__tests__/engine.test.ts src/pipeline/__tests__/weights-config.test.ts src/pipeline/__tests__/crisis-validation.test.ts 2>&1 | tail -5 | grep -q '# fail 0' && echo ENGINE_TESTS_GREEN</automated>
  </verify>
  <done>weights.json version 9.1.0 with the two recomposed pillars (subweights each sum to 1.0) + the 5 new formulaV9 tunables; normalize.ts has wb_homicide range + ucdp log branch; engine.ts applies both ramps + population-scaled homicide rho reading wb_population; FormulaV9Config extended; engine/weights/crisis tests green; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Parity gate (BLOCKING) + jp re-inclusion gate + npm test + backfill regen + sanity anchors</name>
  <files>src/pipeline/backfill.ts, src/pipeline/run.ts, scripts/verify-formula-v91-parity.ts, scripts/fixtures/formula-v91-parity.csv, src/pipeline/config/weights.json, data/scores/**</files>
  <action>
Backfill injection wiring (mirror injectGpiForDate exactly):
  - UCDP: add selectUcdpIndicatorsForYear(byYear, snapshotYear) + injectUcdpForDate(date, rawDataMap) to backfill.ts. The consolidated ucdp-parsed.json holds ALL years; group by year (parse-once cache like gpiByYearCache), select year=min(snapshotYear, latest present), and REPLACE rawDataMap's 'ucdp' with a single-vintage-per-country set. Call injectUcdpForDate from loadRawDataForDate AND add the SAME call to run.ts Stage 2 (after the *-parsed.json load loop) so live daily scoring also selects the latest vintage — otherwise the engine's rawByName keeps an arbitrary year.
  - Homicide + population history: historical worldbank-parsed.json predate homicide/pop, so inject them. Persist a WB homicide+population FULL-history table (bulk WB call, all years — fetch fresh in backfill or reuse the latest raw worldbank all-years file; document which) and injectWorldbankHistoryForDate(date, rawDataMap): select year=min(snapshotYear, latest) per country and MERGE wb_homicide + wb_population into the date's 'worldbank' source WITHOUT clobbering its per-date wb_child_mortality/wb_air_pollution. Keep 'worldbank' in YEAR_BASED_SOURCES.

Parity gate (BLOCKING): create scripts/verify-formula-v91-parity.ts by adapting scripts/verify-formula-v9-parity.ts — SNAPSHOT_DATE='2026-07-08', fixture=scripts/fixtures/formula-v91-parity.csv (copy new-scores-v91.csv verbatim; fixture column is `v91_score`), assert weights.json version==='9.1.0', run with jp EXCLUDED (weights.json still lists jp) to match the CSV. Extend the freshness-attributable classifier to also treat documented base-data drift (advisory changes 2026-07-07→08) and dataDate-decay as non-failing, per SHIP-SPEC 1.1a; any UNEXPLAINED |Δ|>0.02 is a PORT BUG → exit 1 (fix the port, never the prototype/fixture). Gate must PASS across all 248 countries.

jp re-inclusion gate (AFTER parity passes): compute the 2026-07-08 day's scores with jp excluded vs included (remove "jp" from weights.json formulaV9.frozenExcludedSources for the trial). If max |Δscore| &lt; 0.25 AND no country's band flips absurdly (list every mover &gt;0.1) → SHIP re-included (commit weights.json with jp removed from the excluded set). Else → keep jp excluded (leave weights.json as-is) but the fixed fetcher from Task 1 still ships. Record the diff (movers) for the summary either way. Note: re-inclusion is a deliberate config change ON TOP of the parity-verified port, so post-re-inclusion scores may diverge from the jp-excluded CSV for jp-affected safe countries — that is expected, not a parity failure.

Full validation + regen sequence (per x81 SUMMARY):
  1. npm test fully green (guard data/scores/latest.json with git checkout as needed).
  2. Parity gate PASSES (blocking).
  3. jp re-inclusion gate → decide + commit final weights.json.
  4. `npx tsx src/pipeline/backfill.ts --dry-run` → all ~669 dates OK.
  5. Live `npx tsx src/pipeline/backfill.ts` → regenerate every data/scores/&lt;date&gt;.json (incl. UCDP 1989→ + GPI 2026 vintages + homicide/population history injection + the GUM-corrected 2026-07-0{4-7}) + latest.json + history-index.json.
  6. `cp data/scores/latest.json public/scores.json`.
  7. Sanity anchors from the regenerated latest/keyScores: global mean ≈6.6 (CSV baseline 6.596), ISL top ≈8.87, YEM bottom ≈3.42, JAM ≈6.94, TCA ≈5.46, PSE ≈4.61, UKR ≈3.85 (allow small shifts if jp was re-included — record actuals).

Commit atomically per SHIP-SPEC. Do NOT push (PART 2). Do NOT touch docs/i18n/UI.
  </action>
  <verify>
    <automated>npx tsx scripts/verify-formula-v91-parity.ts 2>&1 | grep -q 'PARITY GATE: PASSED' && node -e "const j=require('./data/scores/latest.json'); const m=new Map(j.countries.map(c=>[c.iso3,c.score])); const mean=j.countries.reduce((s,c)=>s+c.score,0)/j.countries.length; if(j.weightsVersion!=='9.1.0')throw new Error('wv '+j.weightsVersion); if(Math.abs(mean-6.6)>0.25)throw new Error('mean '+mean.toFixed(3)); for(const[k,lo,hi]of[['ISL',8.6,9.1],['YEM',3.1,3.7],['JAM',6.6,7.2],['TCA',5.1,5.8],['PSE',4.3,4.9]]){const v=m.get(k); if(v==null||v<lo||v>hi)throw new Error(k+' '+v);} console.log('ANCHORS_OK mean='+mean.toFixed(3))"</automated>
  </verify>
  <done>injectUcdpForDate + injectWorldbankHistoryForDate wired into both backfill.loadRawDataForDate AND run.ts Stage 2; parity gate PASSES &lt;0.02 across 248 countries vs the committed v91 fixture; jp re-inclusion gate evaluated + final weights.json committed; npm test green; ~669 snapshots + latest.json + history-index.json regenerated at weightsVersion 9.1.0; public/scores.json synced; sanity anchors hold (mean ≈6.6, ISL top, YEM bottom, JAM/TCA/PSE/UKR in band). No push; no docs/i18n/UI changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OWID/GPI/WB/MOFA/travel.gc.ca → pipeline parsers | Untrusted external HTTP responses (CSV/JSON/HTML) cross into scoring inputs |
| data/raw/** correction script → historical snapshots | Scripted mutation of raw advisory files feeds the regen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-lb3-01 | Tampering | ucdp.ts / gpi.ts / worldbank.ts external parse | mitigate | Filter rows to getCountryByIso3 project set; Number()-coerce + isNaN skip; clamp normalized values to [0,1]; per-source timeout + findLatestCached fallback |
| T-lb3-02 | Info disclosure / correctness | jp name→ISO3 mapping | mitigate | UNMATCHED Japanese names are skipped with a warning, NEVER guessed; require ≥180/206 mapped + reproduce all discovery anchors before trusting output |
| T-lb3-03 | DoS | slow/oversized external responses | mitigate | AbortSignal.timeout on every fetch; graceful cached fallback; fetchBatch concurrency caps unchanged |
| T-lb3-04 | Tampering | GUM raw correction + backfill regen | mitigate | Correction is a committed script (never a hand-edit); scoped to 4 named dates + GUM only; regen is deterministic and re-derivable |
| T-lb3-05 | Elevation / silent score drift | dataDate freshness-decay flip (1.1a) | mitigate | Decay flip GATED behind the parity gate: if it moves scores vs the frozen prototype &gt;0.02, keep fetchedAt fallback + document; never silently change scores |
| T-lb3-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed (xlsx already present; all sources are fetch() calls) — no supply-chain surface added this phase |
</threat_model>

<verification>
- `npx tsc --noEmit -p .` clean (excluding pre-existing astro.config.mjs / functions errors).
- `npm test` green (engine + weights + crisis + drift-guard suites; restore data/scores/latest.json after runs).
- Parity gate: `npx tsx scripts/verify-formula-v91-parity.ts` → PARITY GATE: PASSED (max |Δ| &lt; 0.02 / 248, jp excluded, freshness/base-drift documented).
- jp re-inclusion diff computed and recorded (movers &gt;0.1 listed); weights.json reflects the decision.
- `backfill --dry-run` all ~669 dates OK, then live regen; latest.json weightsVersion 9.1.0.
- Sanity anchors: mean ≈6.6, ISL ≈8.87, YEM ≈3.42, JAM ≈6.94, TCA ≈5.46, PSE ≈4.61, UKR ≈3.85.
</verification>

<success_criteria>
- v9.1.0 fetchers produce WB homicide+population+restored-air (mrnev), a consolidated UCDP all-years file, and GPI from the 2026 JSON manifest; jp emits real 1-4 levels reproducing all discovery anchors; ca collects all banners + debug file; GUM 4-day blip corrected via script.
- Engine/normalize/weights ported EXACTLY to the frozen prototype (parity &lt;0.02, BLOCKING) with population-scaled homicide precision + both advisory ramps + UCDP log-decay.
- UCDP + homicide/population vintage injection wired into backfill AND live run.ts.
- All ~669 snapshots + latest + history-index + public/scores.json regenerated under v9.1.0 with sane anchors; npm test green.
- No push; no docs/i18n/UI edits (PART 2). Commits atomic.
</success_criteria>

<output>
Create `.planning/quick/260708-lb3-formula-v9-1-part-1-ucdp-gpi-json-wb-hom/260708-lb3-SUMMARY.md` when done.
</output>
