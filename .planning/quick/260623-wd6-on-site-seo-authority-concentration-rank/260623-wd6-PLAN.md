---
phase: quick-260623-wd6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/country/AuthorityLinks.astro
  - src/pages/en/country/[slug].astro
  - src/pages/it/paese/[slug].astro
  - src/pages/es/pais/[slug].astro
  - src/pages/fr/pays/[slug].astro
  - src/pages/pt/pais/[slug].astro
  - src/pages/zh/country/[slug].astro
  - src/pages/de/land/[slug].astro
autonomous: false
requirements: [SEO-AUTHORITY-RANKLINKS, SEO-AUTHORITY-CITE]
must_haves:
  truths:
    - "Every country page (all 7 locales) shows a Rankings block with two internal links to that locale's safest-countries and most-dangerous-countries hub pages"
    - "The band-matching ranking link is listed first: safest-first when country.score >= 6, otherwise most-dangerous-first"
    - "Ranking and embed-badge URLs use the localized slugs from routes[lang], never hardcoded guesses"
    - "Every country page shows a Cite/Embed block linking to the locale embed-badge page plus a copy-paste citation line"
    - "All labels render in all 7 langs (en/it/es/fr/pt/zh/de) with no fallback and no missing-key build warning"
    - "npm run validate:seo stays all-pass and the country @graph invariant is unchanged"
  artifacts:
    - path: "src/components/country/AuthorityLinks.astro"
      provides: "Reusable ranking up-links + cite/embed CTA block for country pages"
      min_lines: 80
    - path: "src/pages/en/country/[slug].astro"
      provides: "EN country template wired to AuthorityLinks"
      contains: "AuthorityLinks"
  key_links:
    - from: "src/components/country/AuthorityLinks.astro"
      to: "src/i18n/ui.ts routes"
      via: "import { routes } and read routes[lang]['safest-countries' | 'most-dangerous-countries' | 'embed-badge']"
      pattern: "routes\\[lang\\]"
    - from: "src/pages/*/[slug].astro (all 7)"
      to: "src/components/country/AuthorityLinks.astro"
      via: "import + render with country/lang/canonicalUrl props"
      pattern: "AuthorityLinks"
---

<objective>
Concentrate internal authority on the under-linked ranking hub pages and seed external
citations, by adding ONE reusable Astro component to every country page across all 7 locales.

Two on-site SEO changes, delivered as a single `AuthorityLinks.astro` component wired into
all 7 locale `[slug].astro` templates:

CHANGE 1 — Ranking up-links: a "Rankings" block linking to the locale's two ranking hub
pages with localized keyword anchors (Safest countries 2026 / Most dangerous countries 2026).
The band-matching link is ordered first.

CHANGE 2 — Cite/Embed CTA: a "Cite this data / Embed badge" block linking to the locale
embed-badge page plus a copy-paste citation line with a copy button.

Purpose: site is fully indexed but ranks ~page 8 for head terms (low authority). These
internal links push crawl equity to the commercial ranking hubs; the cite/embed CTA seeds
external backlinks.

Output: 1 new component + 7 template edits. No schema, no generated files, no pipeline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# The component to mirror for container/heading styling and the localized-labels-map pattern:
@src/components/country/RelatedCountries.astro
@src/components/country/FaqSection.astro

# The EN template — the other 6 locales mirror it (only `lang` and the page dir differ):
@src/pages/en/country/[slug].astro

<interfaces>
<!-- Extracted from codebase. Use these directly — no exploration needed. -->

Props available in EVERY [slug].astro (identical across all 7 locales; only `lang` differs):
  country: ScoredCountry        // has .iso3, .score (1-10 number)
  lang: Lang                    // 'en' | 'it' | 'es' | 'fr' | 'pt' | 'zh' | 'de'
  canonicalUrl: string          // new URL(Astro.url.pathname, Astro.site).href  (already computed at line 65 of EN template)

Locale page dirs (where each [slug].astro lives):
  en -> src/pages/en/country/[slug].astro
  it -> src/pages/it/paese/[slug].astro
  es -> src/pages/es/pais/[slug].astro
  fr -> src/pages/fr/pays/[slug].astro
  pt -> src/pages/pt/pais/[slug].astro
  zh -> src/pages/zh/country/[slug].astro
  de -> src/pages/de/land/[slug].astro

Helpers:
  import { getLocalizedCountryName } from '../../lib/scores';   // getLocalizedCountryName(country, lang): string
  import { routes } from '../../i18n/ui';
  import type { Lang } from '../../i18n/ui';

EXACT slugs in routes[lang] (read these AT RUNTIME from the routes object — do NOT inline-hardcode
the strings; this table is only to confirm the keys exist):
  key 'safest-countries':
    en safest-countries | it paesi-piu-sicuri | es paises-mas-seguros | fr pays-les-plus-surs |
    pt paises-mais-seguros | zh safest-countries | de sicherste-laender
  key 'most-dangerous-countries':
    en most-dangerous-countries | it paesi-piu-pericolosi | es paises-mas-peligrosos |
    fr pays-les-plus-dangereux | pt paises-mais-perigosos | zh most-dangerous-countries |
    de gefaehrlichste-laender
  key 'embed-badge':
    en embed-badge | it badge-incorporabile | es insignia-incrustable | fr badge-integrable |
    pt badge-incorporavel | zh embed-badge | de einbettbares-abzeichen
  (embed-badge exists for ALL 7 locales — no en-fallback branch needed.)

Section styling pattern (copy verbatim from RelatedCountries/FaqSection):
  <section class="py-6 px-4 max-w-2xl mx-auto">
    <h2 class="text-xl md:text-2xl font-heading font-bold text-sand-800 dark:text-sand-100 mb-5"> ... </h2>

Anchor/link card styling for the ranking up-links (reuse RelatedCountries card style):
  class="flex items-center justify-between gap-2 rounded-lg border border-sand-200 dark:border-sand-700 bg-white dark:bg-sand-800 px-4 py-3 hover:border-terracotta-400 dark:hover:border-terracotta-500 transition-colors"

seeAll-style inline link (reuse for the embed-badge CTA link):
  class="inline-flex items-center gap-1 mt-3 text-sm font-medium text-terracotta-600 dark:text-terracotta-400 hover:underline"

Clipboard pattern reference: the embed-badge page (src/pages/en/embed-badge/index.astro) uses a
[data-copy] button + an inline <script> with navigator.clipboard.writeText. Astro scopes <script>
per-component, so put a self-contained copy handler inside AuthorityLinks.astro.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AuthorityLinks.astro component</name>
  <files>src/components/country/AuthorityLinks.astro</files>
  <action>
Create a new Astro component at src/components/country/AuthorityLinks.astro.

Props interface: { country: ScoredCountry; lang: Lang; canonicalUrl: string }.
Imports: import type ScoredCountry from '../../pipeline/types'; import type Lang from
'../../i18n/ui'; import { routes } from '../../i18n/ui'; import { getLocalizedCountryName }
from '../../lib/scores'. (Use the same import syntax/quote style as RelatedCountries.astro.)

Build URLs by READING routes[lang] (do NOT hardcode slug strings — satisfies the "pull slugs
from routes, do not invent them" constraint):
  const r = routes[lang];
  const safestUrl    = `/${lang}/${r['safest-countries']}/`;
  const dangerousUrl = `/${lang}/${r['most-dangerous-countries']}/`;
  const embedUrl     = `/${lang}/${r['embed-badge']}/`;
(embed-badge exists for all 7 locales, so use routes[lang]['embed-badge'] directly with no
en fallback branch.)

Define a SINGLE localized labels map typed as
Record<Lang, { rankingsHeading; safest; dangerous; citeHeading; embedCta; copy; copied }>
covering ALL 7 langs (en, it, es, fr, pt, zh, de) — NO fallbacks, NO missing keys. Suggested copy:
  - rankingsHeading: en "Rankings" / it "Classifiche" / es "Clasificaciones" / fr "Classements"
    / pt "Classificacoes" / zh "排行榜" / de "Ranglisten"
  - safest:    en "Safest countries 2026" / it "Paesi piu sicuri 2026" / es "Paises mas seguros 2026"
    / fr "Pays les plus surs 2026" / pt "Paises mais seguros 2026" / zh "2026 最安全的国家" / de "Sicherste Laender 2026"
  - dangerous: en "Most dangerous countries 2026" / it "Paesi piu pericolosi 2026"
    / es "Paises mas peligrosos 2026" / fr "Pays les plus dangereux 2026" / pt "Paises mais perigosos 2026"
    / zh "2026 最危险的国家" / de "Gefaehrlichste Laender 2026"
  - citeHeading: en "Cite this data" / it "Cita questi dati" / es "Cita estos datos"
    / fr "Citer ces donnees" / pt "Cite estes dados" / zh "引用此数据" / de "Diese Daten zitieren"
  - embedCta: en "Embed the safety badge" / it "Incorpora il badge di sicurezza"
    / es "Incrusta la insignia de seguridad" / fr "Integrer le badge de securite"
    / pt "Incorpore o badge de seguranca" / zh "嵌入安全徽章" / de "Sicherheitsabzeichen einbetten"
  - copy: en "Copy" / it "Copia" / es "Copiar" / fr "Copier" / pt "Copiar" / zh "复制" / de "Kopieren"
  - copied: en "Copied!" / it "Copiato!" / es "Copiado!" / fr "Copie !" / pt "Copiado!"
    / zh "已复制！" / de "Kopiert!"
Use \u-escapes for Latin accented chars exactly as RelatedCountries.astro does (Paises -> Países,
piu -> più, surs -> sûrs, donnees -> données, securite -> sécurité,
seguranca -> segurança, Laender -> Länder, etc.) so the source stays ASCII-clean. Match
RelatedCountries.astro's existing accented forms per language. CJK (zh) characters stay literal.

const L = labels[lang];

Band-matching order for the two ranking links (CHANGE 1):
  const safestFirst = country.score >= 6;
  const rankLinks = safestFirst
    ? [{ href: safestUrl, label: L.safest }, { href: dangerousUrl, label: L.dangerous }]
    : [{ href: dangerousUrl, label: L.dangerous }, { href: safestUrl, label: L.safest }];

Citation line string (CHANGE 2) — use em-dash separators exactly as specified:
  const citation = `${getLocalizedCountryName(country, lang)} safety score — isitsafetotravel.org — ${canonicalUrl} — updated daily`;

Markup: ONE <section class="py-6 px-4 max-w-2xl mx-auto"> (match RelatedCountries container).
Inside, two sub-blocks:

  (1) Rankings sub-block: an <h2> with the section-heading classes
      "text-xl md:text-2xl font-heading font-bold text-sand-800 dark:text-sand-100 mb-5"
      showing L.rankingsHeading. Below it render rankLinks IN ORDER as anchors using the
      RelatedCountries card anchor classes (the flex/border/hover string from <interfaces>).
      Each anchor's visible text is the localized label (this is the SEO keyword anchor — keep the
      full label as the link text). Lay out as e.g. grid grid-cols-1 sm:grid-cols-2 gap-3.

  (2) Cite/Embed sub-block (give the <section> some top margin between blocks, e.g. wrap block 2
      in a div with mt-6): an <h3 class="text-sm font-semibold text-sand-500 dark:text-sand-400
      uppercase tracking-wide mb-3"> showing L.citeHeading, then:
        - the citation line in a selectable element:
          <code id="cite-line" class="block text-xs text-sand-700 dark:text-sand-300 bg-sand-50
            dark:bg-sand-800 border border-sand-200 dark:border-sand-700 rounded px-3 py-2 break-all">{citation}</code>
        - a copy button:
          <button type="button" data-cite-copy data-copied={L.copied} class="mt-2 px-3 py-1 text-xs
            font-medium rounded bg-terracotta-500 text-white hover:bg-terracotta-600 transition-colors">{L.copy}</button>
        - an anchor to embedUrl using the seeAll-style inline-link classes from <interfaces>,
          showing L.embedCta followed by a &rarr; arrow.

Self-contained <script> at the bottom (Astro scopes it to this component):
  const btn = document.querySelector('[data-cite-copy]');
  const line = document.getElementById('cite-line');
  btn?.addEventListener('click', async () => {
    if (!line) return;
    try {
      await navigator.clipboard.writeText(line.textContent || '');
      const original = btn.textContent;
      btn.textContent = btn.getAttribute('data-copied') || 'Copied!';
      setTimeout(() => { btn.textContent = original; }, 1500);
    } catch {}
  });
(Mirror the embed-badge page's navigator.clipboard pattern. Optional chaining guards against a
missing element.)

Do NOT add any JSON-LD, schema, or structured data — plain HTML links only (the country @graph
invariant WebPage+Place+FAQPage+TouristDestination+Dataset must stay unchanged). Do NOT touch any
generated file.
  </action>
  <verify>
    <automated>test -f src/components/country/AuthorityLinks.astro && c=$(npx tsc --noEmit -p . 2>&1 | grep -i "AuthorityLinks" | grep -v '^#' | grep -c .); [ "$c" -eq 0 ] && echo OK || echo "TS errors referencing AuthorityLinks: $c"</automated>
  </verify>
  <done>AuthorityLinks.astro exists; labels defined for all 7 langs with no missing keys; safest/dangerous/embed URLs are built by reading routes[lang]; ranking links ordered band-first (safest-first when score>=6); renders a copy-paste citation line + working copy button + embed-badge link; no TS errors referencing the component.</done>
</task>

<task type="auto">
  <name>Task 2: Wire AuthorityLinks into all 7 locale country templates</name>
  <files>src/pages/en/country/[slug].astro, src/pages/it/paese/[slug].astro, src/pages/es/pais/[slug].astro, src/pages/fr/pays/[slug].astro, src/pages/pt/pais/[slug].astro, src/pages/zh/country/[slug].astro, src/pages/de/land/[slug].astro</files>
  <action>
In EACH of the 7 [slug].astro templates:

1. Add the import alongside the other country-component imports (right next to the existing
   RelatedCountries import). All 7 templates are three directory levels deep under src/pages, so:
     import AuthorityLinks from '../../../components/country/AuthorityLinks.astro';
   Copy the exact relative prefix the sibling RelatedCountries import already uses in that file to
   be safe (it is '../../../components/country/RelatedCountries.astro').

2. Render the component in the page body, immediately AFTER the <RelatedCountries ... /> line and
   BEFORE <FaqSection ... /> so the ranking up-links join the internal-linking cluster while the
   FAQ (which feeds FAQPage schema) stays untouched:
     <AuthorityLinks country={country} lang={lang} canonicalUrl={canonicalUrl} />
   canonicalUrl is already computed in every template (EN line 65:
   const canonicalUrl = new URL(Astro.url.pathname, Astro.site).href;). All 7 mirror EN, so it is
   present; confirm it exists in each before passing it.

Do not change lang (each template declares its own const lang: Lang = '<locale>';). Do not
duplicate region links (RelatedCountries already emits the regional-hub up-link — leave it). Touch
nothing else in these files. Do NOT edit any generated file.
  </action>
  <verify>
    <automated>miss=0; for f in src/pages/en/country/\[slug\].astro src/pages/it/paese/\[slug\].astro src/pages/es/pais/\[slug\].astro src/pages/fr/pays/\[slug\].astro src/pages/pt/pais/\[slug\].astro src/pages/zh/country/\[slug\].astro src/pages/de/land/\[slug\].astro; do c=$(grep -c "AuthorityLinks" "$f"); if [ "$c" -lt 2 ]; then echo "MISSING in $f (count=$c)"; miss=1; fi; done; [ "$miss" -eq 0 ] && echo "ALL 7 WIRED"</automated>
  </verify>
  <done>All 7 templates import AuthorityLinks and render it once with country/lang/canonicalUrl, placed after RelatedCountries and before FaqSection. grep finds AuthorityLinks >=2 times (import + render) in each of the 7 files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
A new AuthorityLinks.astro section on every country page (all 7 locales) with: (1) a "Rankings"
block of two localized keyword-anchor links to the locale's safest-countries and
most-dangerous-countries hubs, band-matching link first; (2) a "Cite this data" block with a
copy-paste citation line, a copy button, and a link to the locale embed-badge page. URLs derive
from routes[lang]; labels cover all 7 langs.
  </what-built>
  <how-to-verify>
1. Run the SEO gate and build (per CLAUDE.md, prefer the fast path):
     npx astro build && npm run validate:seo
   Expect: build succeeds with NO missing-i18n-key warnings, and validate:seo is ALL-PASS
   (the country @graph WebPage+Place+FAQPage+TouristDestination+Dataset assertion still passes).
2. npm run dev, then open a HIGH-score country, e.g. http://localhost:4321/en/country/che/
   (Switzerland, score >= 6): confirm the Rankings block shows "Safest countries 2026" FIRST,
   then "Most dangerous countries 2026". Confirm the links go to /en/safest-countries/ and
   /en/most-dangerous-countries/.
3. Open a LOW-score country, e.g. http://localhost:4321/en/country/afg/ (Afghanistan, score < 6):
   confirm "Most dangerous countries 2026" is listed FIRST.
4. Spot-check 2 non-English locales for localized slugs + localized labels:
   http://localhost:4321/it/paese/che/  → links to /it/paesi-piu-sicuri/ and
     /it/paesi-piu-pericolosi/, heading "Classifiche", cite heading "Cita questi dati".
   http://localhost:4321/de/land/che/   → links to /de/sicherste-laender/ and
     /de/gefaehrlichste-laender/, embed link to /de/einbettbares-abzeichen/.
5. Click the Copy button under the citation line → it should switch to the localized "Copied!"
   text briefly and the citation should be on the clipboard.
6. Confirm visual consistency: the new section matches RelatedCountries/FaqSection container and
   heading styling, and the regional-hub link from RelatedCountries is NOT duplicated.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. wrong slug, missing locale label, build warning)</resume-signal>
</task>

</tasks>

<verification>
- npx astro build succeeds with no missing-i18n / undefined-label warnings.
- npm run validate:seo is all-pass; country @graph invariant (WebPage+Place+FAQPage+TouristDestination+Dataset) unchanged.
- grep "AuthorityLinks" returns >=2 hits in each of the 7 [slug].astro templates.
- AuthorityLinks.astro references routes[lang] (no hardcoded ranking/embed slug literals).
- No generated file (public/llms*, public/scores.json, data/**, dist/**, public/og/**) modified.
</verification>

<success_criteria>
- One reusable component delivers BOTH changes; wired into all 7 locale country templates.
- Ranking up-links use localized keyword anchors and localized slugs from routes[lang]; band-matching link ordered first by score >= 6.
- Cite/Embed block links to the locale embed-badge page and shows a working copy-paste citation line.
- All labels render in all 7 langs with no fallback/missing-key warning.
- Build + validate:seo stay all-pass; @graph invariant intact; no generated files touched.
</success_criteria>

<output>
Create `.planning/quick/260623-wd6-on-site-seo-authority-concentration-rank/260623-wd6-SUMMARY.md` when done.
</output>
